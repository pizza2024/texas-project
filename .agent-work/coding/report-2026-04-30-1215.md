# Coding Report — 2026-04-30-1215

**时间:** 2026-04-30 12:15
**HEAD:** `34cd909` (develop)
**分支:** develop

---

## 本轮处理

### P2-NOTIFY-PUSH-WIRE — `pushEnabled` 未生效 ✅ 已修复

**问题:** `notification.service.ts:212` 的 `isPushAllowed()` 只检查 `doNotDisturb`，完全忽略 `pushEnabled` 字段。用户在前端关闭推送开关后，WebSocket push 仍会送达。

**修复:**
```typescript
// notification.service.ts:212
async isPushAllowed(userId: string): Promise<boolean> {
  const settings = await this.getSettings(userId);
+ if (!settings.pushEnabled) return false;  // ← 新增
  if (!settings.doNotDisturb) return true;
  ...
}
```

**提交:** `34cd909`
**验证:** TypeScript 编译 0 错误，后端 451 测试全部通过。

---

### P2-NOTIFY-EMAIL-WIRE — `emailEnabled` 未连线 ⚠️ 暂不实施

**问题:** `emailEnabled` 字段已存储但没有任何代码路径读取它来抑制邮件发送。当前代码库中无实际邮件发送服务（Resend 集成尚未实现），属于预埋字段。

**决策:** 等 Resend 邮件服务实际接入时再连线该字段，防止死代码。当前状态可接受。

---

## 调研结果（无代码改动）

### P2-TOURNAMENT-BLIND — 盲注可视化 UI 未实施
- `BlindLevel` 类型已定义于 `packages/shared/src/types/tournament.ts`
- 后端 `TournamentService` 已发送 `tournament_blind_level` WS 事件
- `app/web/app/schedule/[id]/page.tsx` **无盲注结构表格或时间轴组件**
- **结论:** 需要新建 UI 组件，Productor spec 就绪后 Coding 实施

### P2-TOURNAMENT-GTD — 保底奖池
- `isGuarantee` 字段已存在于 DTO + `TournamentCard` badge
- **GTD 强制逻辑未实现**（如参与人数不足时系统补足奖金）
- **结论:** 需 Productor 补充业务规则规格

---

## 遗留 P2（不阻塞发布）

| ID | 任务 | 状态 | 依赖 |
|----|------|------|------|
| P2-NOTIFY-EMAIL-WIRE | `emailEnabled` 未连线 | ⚠️ 暂不实施 | 等 Resend 接入 |
| P2-TOURNAMENT-BLIND | 盲注可视化 UI | 📋 待 Productor spec | spec 就绪后实施 |
| P2-TOURNAMENT-GTD | 保底奖池强制逻辑 | 📋 待 Productor 规则 | 业务规格待定 |
| P2-WITHDRAW-UX-004 | 提现历史追踪 | 📋 待实施 | |
| P2-ROOM-UX-003/004/005 | 空状态/收藏/邀请 | 📋 待实施 | |

---

_本轮无 P0/P1 紧急任务，所有 P0/P1 均清零。_
