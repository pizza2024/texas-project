# Coding Report — 第402轮

**时间:** 2026-04-30 08:15
**HEAD:** `4994a23`
**分支:** develop

---

## 处理任务

### P2-NOTIFY-001-001 — NotificationGateway handleConnection 缺少 client.join (P1)

**问题:** `handleConnection` 中从未调用 `client.join('user:${userId}')`，导致 `emitToUser()` 永远无法送达实时通知。

**修复:** `notification.gateway.ts:37` — 在 JWT 验证成功、设置 `client.data.userId` 之后，添加：
```typescript
await client.join(`user:${payload.userId}`);
```

**验证:** TypeScript 编译通过，无新增错误。

---

### P2-NOTIFY-001-002 — app.gateway.spec.ts 缺少 notificationService mock (P2)

**问题:** `AppGateway` 构造函数新增 `notificationService` 参数（第15个参数），但 spec 中 `new AppGateway(...)` 只有14个参数，导致 TS 编译错误 `error TS2554: Expected 15 arguments, but got 14`。

**修复:** `app.gateway.spec.ts:432` — 在 `friendService` mock 之后、`clubService` mock 之前，添加 notificationService mock：
```typescript
{
  markRead: jest.fn(),
  markAllRead: jest.fn(),
  getNotifications: jest.fn().mockResolvedValue([]),
} as any,
```

**验证:** `pnpm --filter backend exec tsc --noEmit` — `app.gateway.spec.ts` 错误清零。

---

## 提交

```
commit 4994a23
fix(notification): add client.join user room + spec mock

P2-NOTIFY-001-001: Add await client.join('user:${userId}') in
NotificationGateway.handleConnection so emitToUser() can reach
connected clients.

P2-NOTIFY-001-002: Add notificationService mock (markRead/markAllRead/
getNotifications) to app.gateway.spec.ts constructor — missing 15th
argument was causing TS2554.
```

---

## 遗留问题

| ID | 任务 | 优先级 | 状态 |
|----|------|--------|------|
| P2-NOTIFY-001-003 | 前端通知中心 UI (Bell Icon + Dropdown) | P2 | 待实施 |
| P2-TOURNAMENT-BLIND | 盲注结构可视化 | P2 | 待实施 |
| P2-WITHDRAW-UX-004 | 提现历史追踪 | P2 | 待实施 |
| P2-ROOM-UX-003 | tier 筛选无结果时无引导 CTA | P2 | 待实施 |
| P2-ROOM-UX-004 | 无收藏常玩房间功能 | P2 | 待实施 |
| P2-ROOM-UX-005 | 创建私人房间后无分享邀请链接 | P2 | 待实施 |

---

_Coding r402 — 2026-04-30 08:15 — P2-NOTIFY-001-001/001-002 修复 ✅ commit 4994a23_
