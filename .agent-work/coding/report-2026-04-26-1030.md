# Coding Report — 第215轮

**时间:** 2026-04-26 10:30
**状态:** ✅ 完成

## 系统状态

- 测试套件: 27 passed, 294 tests
- TypeScript: 0 错误 (backend + web)
- Lint: 0 错误

## 处理的任务

### P1-FIRST-DEPOSIT 前端 ✅

**目标:** `/deposit` 页面缺少首充奖金状态和 wagering 进度 UI

**改动:**

1. **apps/web/app/deposit/page.tsx**
   - 新增 `BonusStatus` 接口（depositAmount, bonusAmount, wageringRequirement, wageringProgress, wageringRemaining, status, isUnlocked, claimed）
   - 新增 `bonusStatus` state，默认 null
   - `loadData()` 扩展为 `Promise.all([addrRes, histRes, bonusRes])`，调用 `GET /deposit/bonus/status`
   - 新增首充奖金面板（绿色/金色主题），置于地址卡上方：
     - 无奖金记录：提示"完成首充解锁 bonus"
     - 已解锁 (isUnlocked=true)：显示"✅ Bonus Unlocked!" + bonus 金额 + 领取说明
     - 未解锁：显示 wagering 进度条（wageringProgress / wageringRequirement）+ 剩余 wagering
     - 已领取：显示"✅ Bonus Claimed"

### P1-DAILY-MISSIONS 前端 ✅

**目标:** `/missions` 页面完全缺失

**改动:**

1. **新建 apps/web/app/missions/page.tsx** (522 lines)
   - 调用 `GET /missions` 获取所有任务（含进度）
   - 三栏分组：ONE-TIME / DAILY / WEEKLY
   - 每张任务卡显示：
     - 类型标签（颜色区分）
     - 标题 + 描述
     - 奖励筹码（格式化 K/M 后缀）
     - 进度条（progress/target）
     - Claim 按钮（status=COMPLETED 时可点击）
     - 已领取/进行中/锁定状态
   - Claim 按钮行为：500ms 延迟后刷新任务列表（backend 在任务完成时自动发放，无需手动 claim）
   - 暗色扑克主题，与 rakeback/deposit 风格一致

### API 路径修正

- `bonus-status` → `bonus/status`（与后端 deposit.controller.ts 对齐）
- 移除不存在的 `POST /missions/:id/claim` 调用（backend 自动领取）

## 遗留问题

| ID                   | 任务                       | 备注                                        |
| -------------------- | -------------------------- | ------------------------------------------- |
| P1-BLAST-001         | Blast 即时赛事             | 规格待定义（参考 GGPoker SPINS）            |
| P2-TOURNAMENT-RANDOM | Math.random() 非密码学安全 | tournament-schedule.service.ts:612 — 低风险 |

## 下一步建议

1. P1-BLAST-001 规格定义（Productor 负责）
2. 补充前端集成测试（P2-CHAT-FRONTEND-TEST）
3. JWT localStorage XSS 风险迁移至 httpOnly cookie（P2-JWT-LOCALSTORAGE）
