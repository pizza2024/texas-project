# Coding Report — 第273轮

**时间:** 2026-04-27 01:05
**HEAD:** `f071ae3`
**状态:** 0 P0 / 0 P1 / ~5 P2

---

## 本轮处理任务

### ✅ P1-PRIZE-DISPLAY — TournamentCard 奖池 + GTD + 注册人数显示

**来源:** Test 第272轮（P1-PRIZE-DISPLAY 建议升 P1）

**改动文件 (3个):**

#### 1. `apps/backend/src/tournament-schedule/dto/schedule.dto.ts`
- `ScheduleEntryResponseDto` 新增字段:
  - `totalPrize: number` — 奖池总额（buyin × maxPlayers）
  - `isGuarantee?: boolean` — 是否 GTD（Guaranteed）
  - `registeredCount?: number` — 当前报名人数
- `CreateScheduleDto` 新增 `isGuarantee?: boolean`

#### 2. `apps/backend/src/tournament-schedule/tournament-schedule.service.ts`
- `ScheduleEntry` 接口新增 `totalPrize`、`isGuarantee`、`registeredCount`
- `createSchedule()` — 创建时自动计算 `totalPrize = buyin * maxPlayers`，`registeredCount = 0`，`isGuarantee = dto.isGuarantee ?? false`
- `toResponseDto()` — 透传三个新字段

#### 3. `apps/web/app/schedule/components/TournamentCard.tsx`
- `ScheduleEntry` 接口新增 `totalPrize`、`isGuarantee`、`registeredCount`
- 新增 **GTD badge**（橙色）：`isGuarantee=true` 时显示
- Info Grid 更新：
  - `Players:` 改为 `registeredCount ?? 0 / maxPlayers`（显示当前/最大）
  - `Prize:` 改为 `formatChips(totalPrize) chips`（显示总奖池而非分配百分比）

#### 4. `apps/web/app/schedule/[id]/page.tsx`
- 详情页类型+状态行增加 **GTD badge**
- Info Cards 更新：
  - `Max Players` → `Players: registeredCount / maxPlayers`
  - 新增 `Total Prize` 行（绿色高亮）

---

## 测试结果

- **Backend lint:** ✅ 0 errors
- **Backend tsc:** ✅ --noEmit clean
- **Backend tests:** ✅ 31 suites / 410 tests 全通过
- **Web lint:** ✅ 0 errors

---

## 遗留问题

| ID | 描述 | 状态 |
|----|------|------|
| P1-004 | Jest Worker 泄漏 | 🟡 监控中（已启用 --detectOpenHandles） |
| P2-WALLET-SPEC-TS | `Promise<unknown>` → `never` 类型 cast | 🔍 待认领（IDE 警告，不影响运行） |

---

*Coding 第273轮 — 2026-04-27 01:05*
