# Coding Report — 第103轮

> 时间: 2026-04-25 06:16

## 处理的任务

### P0-BRUTE-001 ✅ 已修复 — 密码暴力破解保护回归 (Critical)

**问题**: `checkPasswordAttemptLimit` 在 commit `3e9ee55` 中改为 `async`，但调用处未使用 `await`，导致暴力破解保护完全失效。

**根因**: `app.gateway.ts` 中该方法不是 `async`，但底层 `connectionState.checkPasswordAttemptLimit` 是 `async` 的。同步包装导致返回 Promise 对象而非实际结果。

**改动**:

1. **`app.gateway.ts:122`** — 方法改为 `async`:

   ```typescript
   // Before
   checkPasswordAttemptLimit(ip: string, roomId: string) {
   // After
   async checkPasswordAttemptLimit(ip: string, roomId: string) {
   ```

2. **`app.gateway.ts:126`** — `clearPasswordAttempts` 同步改 async:

   ```typescript
   // Before
   clearPasswordAttempts(ip: string, roomId: string) {
   // After
   async clearPasswordAttempts(ip: string, roomId: string) {
   ```

3. **`game.handler.ts:99`** — 调用处加 `await`:

   ```typescript
   // Before
   const bruteForceResult = gateway.checkPasswordAttemptLimit(ipHash, roomId);
   // After
   const bruteForceResult = await gateway.checkPasswordAttemptLimit(
     ipHash,
     roomId,
   );
   ```

4. **`game.handler.ts:123`** — `clearPasswordAttempts` 加 `await`:

   ```typescript
   // Before
   gateway.clearPasswordAttempts(ipHash, roomId);
   // After
   await gateway.clearPasswordAttempts(ipHash, roomId);
   ```

5. **`app.gateway.spec.ts:116`** — mock 修复为 `mockResolvedValue('ok')`:
   ```typescript
   // Before
   checkPasswordAttemptLimit: jest.fn(),
   // After
   checkPasswordAttemptLimit: jest.fn().mockResolvedValue('ok'),
   ```

**测试**: 239 tests pass, 24 suites, 0 errors

---

### P1-MOCK-001 ✅ 已修复

**问题**: `checkPasswordAttemptLimit` mock 缺少 `mockResolvedValue`，与新 async 签名不匹配。

**修复**: 同上 — spec 中已加 `.mockResolvedValue('ok')`。

---

## 遗留问题

无新遗留。P0/P1 全部清零。

---

_下一轮监控: 等待 Test 第103轮反馈_
