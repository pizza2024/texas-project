# Coding Report — 第136轮

> 更新时间: 2026-04-25 14:38

## 状态

| 状态 | P0   | P1   | P2  |
| ---- | ---- | ---- | --- |
| 数量 | 0 🟢 | 0 🟢 | 5   |

---

## HEAD Commit

`fix: 3 HIGH→P1 + P2-CLUB-004 WebSocket club权限校验`（未提交，待 Test 验证）

---

## 本轮处理任务

### ✅ NEW-HIGH-001 → P1（已修复）

**问题**: `club.service.ts:76` — 邀请码生成使用 `Math.random()`，非密码学安全  
**修复**: 改用 `crypto.randomInt()` 替代  
**改动**: `apps/backend/src/club/club.service.ts`

- 添加 `import { randomInt } from 'crypto'`
- `generateInviteCode()`: `Math.floor(Math.random() * ...)` → `randomInt(0, ...)`

---

### ✅ NEW-HIGH-002 → P1（已修复）

**问题**: `game.handler.ts:330` — `client.leave()` 在 `leaveCurrentRoom()` DB更新之后，存在竞态条件  
**修复**: 调整顺序，`client.leave()` 先于所有 DB 状态更新执行  
**改动**: `apps/backend/src/websocket/game.handler.ts:323-325`

- `client.leave(roomId)` 移到 `leaveCurrentRoom()` 之前

---

### ✅ NEW-HIGH-003 → P1（已修复）

**问题**: `game.handler.ts:284` — `table` 为 `null` 时静默返回，无错误事件通知客户端  
**修复**: 主动 emit `error` 事件并携带 `roomId`  
**改动**: `apps/backend/src/websocket/game.handler.ts:284-292`

```typescript
if (!table) {
  client.emit("error", { message: "Table not found", roomId });
  return;
}
```

---

### ✅ P2-CLUB-004（已修复）

**问题**: WebSocket `handleJoinRoom` 缺失 Club 专属房间权限校验，非俱乐部成员可加入 Club-only 房间  
**修复**: 在 `handleJoinRoom` 中，验证 `isClubOnly=true` 且 `clubId` 存在时，调用 `ClubService.isClubMember()` 检查成员身份  
**改动**:

1. `websocket.module.ts` — 添加 `ClubModule` + `RoomModule` imports
2. `app.gateway.ts` — 注入 `ClubService` + `RoomService`（`readonly` 公开访问）
3. `game.handler.ts:95-107` — 新增 Club 成员校验逻辑：
   ```typescript
   const room = await gateway.roomService.findOne(roomId);
   if (room?.isClubOnly && room.clubId) {
     const isMember = await gateway.clubService.isClubMember(
       userId,
       room.clubId,
     );
     if (!isMember) {
       client.emit("access_denied", {
         message: "This room is exclusive to club members",
         roomId,
       });
       return { event: "access_denied", data: { roomId } };
     }
   }
   ```
4. `app.gateway.spec.ts` — 添加 `clubService` + `roomService` mock（`isClubMember` 返回 `true`，`findOne` 返回 `null`）

---

## 测试状态

- **Backend Jest**: 270 tests pass（26 suites）✅
- 所有 spec 测试通过，无新增失败

---

## 遗留问题

| ID            | 问题                      | 状态                                    |
| ------------- | ------------------------- | --------------------------------------- |
| P1-004        | Jest Worker 泄漏          | 🟡 监控中，`--detectOpenHandles` 已启用 |
| P2→P1-NEW-001 | Club 邀请码 Math.random() | ✅ 已修复（本轮）                       |
| P2→P1-NEW-002 | client.leave() 竞态条件   | ✅ 已修复（本轮）                       |
| P2→P1-NEW-003 | table-not-found 静默失败  | ✅ 已修复（本轮）                       |
| P2-NEW-007    | 战后手牌复盘 UI           | 📋 调研完成，待 UI 规格文档             |

---

## 下轮行动

1. 等待 Test 代理验证本轮修复（270 tests pass）
2. 提交代码变更
3. P2-NEW-007 编写 Hand Replay UI 规格文档

---

_Coding 第136轮 — 3 个 HIGH 升 P1 已修复，P2-CLUB-004 已实现，270 tests pass_
