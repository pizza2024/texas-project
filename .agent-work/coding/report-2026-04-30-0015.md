# Coding Report — 第342轮

**时间:** 2026-04-30 00:15
**HEAD:** `1bffbae`（fix(P0-TS-001): add GameStage type to fix TS2345 includes() error）
**分支:** develop

---

## P0 — 本轮处理

| ID        | 任务                                               | 状态              | 备注                                                                                                                                   |
| --------- | -------------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| P0-TS-001 | `ACTIVE_BETTING_STAGES.includes()` TS2345 编译错误 | ✅ **已验证修复** | commit `1bffbae` — `GameStage` 类型从 `@texas/shared` 导入，`components/types.ts` 导入 `GameStage`，`page.tsx` 使用 `GameStage[]` 常量 |

**验证结果:**

- `npx tsc --noEmit -p apps/web/tsconfig.json` → **0 errors** ✅
- Backend Jest: **452/452 passing** ✅

---

## P0 — 验证：P0-TS-001 根因分析

**问题:** `page.tsx:35` 定义 `const ACTIVE_BETTING_STAGES: GameStage[] = ['PREFLOP', 'FLOP', 'TURN', 'RIVER']`
后，在 `page.tsx:95` 使用 `ACTIVE_BETTING_STAGES.includes(table.currentStage)` 时报错 TS2345。

**根因:** `TableState.currentStage` 在 `components/types.ts:36` 声明为 `GameStage` 类型，而 `GameStage` 是从 `@texas/shared` 正确导入的联合类型。问题出在 `packages/shared/src/index.ts` 的导出路径 — `GameStage` 需从 `@texas/shared` 的 `types/game.ts` 正确导出。

**修复 (commit `1bffbae`):**

- `apps/web/app/room/[id]/components/types.ts` — 添加 `import type { GameStage } from '@texas/shared';`
- `apps/web/app/room/[id]/page.tsx` — `import { calculateEquity, GameStage } from '@texas/shared';`（`GameStage` 已存在于 shared 包）

---

## 遗留 P2 状态

| ID             | 任务                    | 状态                                                                              |
| -------------- | ----------------------- | --------------------------------------------------------------------------------- |
| P2-NOTIFY-001  | 站内通知中心            | ✅ 规格已就绪 (`apps/docs/content/notification-center.mdx`)，建议下轮开始 Phase 1 |
| P2-RAKEBACK    | 忠诚积分/返水体系规格   | 📋 待 Productor 起草                                                              |
| P2-PROFILE-001 | 玩家资料页丰富化        | 📋 待细化                                                                         |
| P2-MTT-PHASE3  | MTT 延迟注册 + Re-Entry | 🔴 建议升 P1 — 竞品标配                                                           |

---

## 本轮验证命令

```bash
# TypeScript 编译
npx tsc --noEmit -p apps/web/tsconfig.json  # ✅ 0 errors

# Backend 测试
npm test  # ✅ 452/452 passing
```

---

_P0/P1 全部清零 ✅ — P0-TS-001 已确认修复 — Coding 第342轮 — 下轮建议：开始 P2-NOTIFY-001 Phase 1 实施_
