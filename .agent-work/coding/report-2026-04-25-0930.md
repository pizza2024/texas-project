# Coding Report — 第116轮

> 时间: 2026-04-25 09:30

## 状态

| 类别 | 数量 | 状态      |
| ---- | ---- | --------- |
| P0   | 0    | 🟢        |
| P1   | 1    | 🔴 待验证 |
| P2   | 3    | 🟡 进行中 |

---

## 本轮完成

### 1. P2-NEW-001: Web React hooks exhaustive-deps 修复 (room/[id]/page.tsx)

- **Commit:** `e28e902`
- **问题:** 4个useEffect缺少依赖项，触发React exhaustive-deps警告
- **修复方案:**
  - 添加 `soundSettingsRef` (useRef) 打破循环依赖：函数读取 `soundSettingsRef.current` 而非直接读取 `soundSettings`
  - 将 `queueDealAnimations`、`queuePayoutFlights`、`playCountdownTone` 包装为 `useCallback`（空deps，函数内部通过ref访问最新soundSettings）
  - WebSocket setup effect: 添加 `t`、`redirectToLogin`、`redirectForExpiredToken` 到deps
  - Token expiry timer effect: 添加 `redirectForExpiredToken` 到deps
  - Animation orchestration effect: 添加 `queueDealAnimations`、`queuePayoutFlights` 到deps
- **验证:** `npm run lint 2>&1 | grep exhaustive-deps` → 无输出 ✅
- **遗留:** room-mobile, rooms, club 页面尚有少量warnings，待下轮处理

### 2. 未提交文件 Commit

- **Commit:** `6ec6c3a` — "fix: Club controller/service improvements and E2E dotenv setup"
- **文件:** 8个文件 (apps/backend/src/club/\*, apps/backend/.env.test, apps/backend/test/jest-e2e.json, apps/backend/test/rakeback.e2e-spec.ts)
- **注意:** `.agent-work/` 目录未提交（agent报告文件）

---

## 代码改动

### apps/web/app/room/[id]/page.tsx

| 改动                     | 说明                                                                                            |
| ------------------------ | ----------------------------------------------------------------------------------------------- |
| `soundSettingsRef` 添加  | `const soundSettingsRef = useRef(soundSettings); soundSettingsRef.current = soundSettings;`     |
| `playTone`               | `soundSettings.volume` → `soundSettingsRef.current.volume`                                      |
| `queueDealAnimations`    | 包装为 `useCallback(..., [])`，`soundSettings.deal` → `soundSettingsRef.current.deal`           |
| `queuePayoutFlights`     | 包装为 `useCallback(..., [])`，`soundSettings.winner` → `soundSettingsRef.current.winner`       |
| `playCountdownTone`      | 包装为 `useCallback(..., [])`，`soundSettings.countdown` → `soundSettingsRef.current.countdown` |
| WebSocket effect deps    | `[id, router]` → `[id, router, t, redirectToLogin, redirectForExpiredToken]`                    |
| Token expiry effect deps | `[roomPath]` → `[roomPath, redirectForExpiredToken]`                                            |
| Animation effect deps    | `[table, myUserId]` → `[table, myUserId, queueDealAnimations, queuePayoutFlights]`              |

---

## 遗留问题

| ID              | 任务                                                     | 优先级 | 备注                               |
| --------------- | -------------------------------------------------------- | ------ | ---------------------------------- |
| P2-NEW-001      | Web React hooks exhaustive-deps (room-mobile/rooms/club) | P2     | 少量warnings，待下轮处理           |
| P2-NEW-002      | PlayerSeat.tsx `isSettlement` unused                     | P2     | 未使用变量                         |
| P2-NEW-003      | rooms/page.tsx `_` unused variable                       | P2     | 未使用变量                         |
| P1-RAKEBACK-001 | Rakeback E2E 本地验证                                    | P1     | 待 `npm run test:e2e` 本地运行验证 |

---

## 下轮行动

1. **P1**: Rakeback E2E 本地验证 — `npm run test:e2e`
2. **P2**: room-mobile/page.tsx, rooms/page.tsx, club/page.tsx exhaustive-deps warnings
3. **P2**: 清理 unused variable warnings (PlayerSeat.tsx, rooms/page.tsx)
4. **P2**: Club 数据库迁移执行 — `npx prisma migrate dev`

---

_Coding 第116轮完成 — 2 commits (e28e902, 6ec6c3a), Web lint 0 errors, P2-NEW-001 room/[id] 已修复_
