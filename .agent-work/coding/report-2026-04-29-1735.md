# Coding Report — 第332轮

**时间:** 2026-04-29 17:35
**HEAD:** `fdaecbe` → `HEAD` (未提交，留待下一轮 commit)
**测试:** 452 tests ✅ | Lint ✅

---

## 处理的任务

### P0 — 立即修复（来自 Test 第331轮）

#### ✅ P0-SEC-002: `verifyEmailCode` 无 OTP rate limit（暴力破解）
- **文件:** `apps/backend/src/auth/auth.controller.ts:101-110`
- **修复:** 添加 `@UseGuards(RateLimitGuard)` + `@ApplyRateLimit({ limit: 10, windowSeconds: 300, keyPrefix: 'rl:verify-email', keyType: 'emailOrIp' })`
- **说明:** 10次/5分钟，key=email 或 IP（email优先）。与 request-email-code 的 3次/小时 组成双层防护。

#### ✅ P0-SEC-003: `handlePlayerAction` 跨房间操作注入（fail-closed）
- **文件:** `apps/backend/src/websocket/game.handler.ts:290-306`
- **原问题:** `validated.roomId ?? null` → 允许客户端指定任意 roomId；index为null时客户端可绕过
- **修复:**
  - 强制使用 `getUserCurrentRoomId()` 权威索引（index 存在时）
  - index 为 null 时：拒绝操作（而非信任客户端的 roomId）
  - `return { event: 'error' }` → `client.emit('error', ...)` 统一错误通道

---

### P1 — 立即修复（来自 Test 第331轮）

#### ✅ P1-SEC-001: WebSocket session 验证 Redis 宕机绕过
- **文件:** `apps/backend/src/websocket/app.gateway.ts:301-311`
- **原问题:** `if (payload.sessionId && this.redisService.isAvailable)` — Redis 宕机时跳过 session 验证，允许任意 sessionId 连接
- **修复:** 改为 `if (payload.sessionId)` 先检查 sessionId 是否存在，然后单独检查 Redis 可用性：
  - Redis 不可用 → `SESSION_UNAVAILABLE` → `force_logout` → disconnect
  - Redis 可用 → 正常校验 sessionId

#### ✅ P1-SEC-002: RateLimitGuard Redis 宕机 bypass
- **文件:** `apps/backend/src/auth/rate-limit.guard.ts:91-92`
- **原问题:** `if (count === null) return true` — Redis 宕机时放行所有请求，等于绕过 rate limit
- **修复:** 改为 `throw new HttpException(..., HttpStatus.SERVICE_UNAVAILABLE)` — fail-closed

#### ✅ P1-SEC-003: `exchangeBalanceToChips` TOCTOU
- **文件:** `apps/backend/src/wallet/wallet.service.ts:352-360`
- **原问题:** `getRealBalance()` 在事务外读取 → 余额检查和扣减之间存在竞态窗口
- **修复:** 将 `balance` 读取移入 `$transaction` 内部（`tx.wallet.findUnique`），确保 check-and-debit 原子化

---

## 遗留问题

无。所有 P0/P1 安全问题已清空。

---

## 下一步

- **commit:** 等待下一轮 Coding 将上述 5 个 fix 打包提交
- **P1-BLAST-012:** Blast Phase 3 规格已就绪，等待 Productor 确认后实施
- **P2:** ~10 个 pending（P2-NEW-028 Late Registration、P2-NEW-030 实时人数、P2-NEW-031 Rebuy/Addon 等 Tournament 缺口）

---

*Coding 第332轮 — 2026-04-29 17:35*
