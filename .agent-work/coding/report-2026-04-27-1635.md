# Coding Report — 第241轮

**时间:** 2026-04-27 16:35
**状态:** ✅ 完成

---

## 处理的任务

### P2-NEW-001 — `checkAddressDeposits` 非原子余额更新（资金安全）
- **风险:** 真实资金竞态条件，并发充值可能丢失余额更新
- **修复:** `deposit.service.ts` — 将 `walletService.getBalance()` + `walletService.setBalance()` + `depositRecord.create()` + `transaction.create()` 全部封装进 `prisma.$transaction()` 回调内，以 `tx` 作为事务客户端
- **效果:** 余额读取和写入在同一快照事务内完成，消除并发充值时的 read-then-write 竞态窗口
- **Commit:** `8ce5e54`

### P2-NEW-002 — `verifyEmailCode` JSON.parse 无 try/catch（代码健壮性）
- **风险:** Redis 数据损坏时 `JSON.parse(stored)` 抛出未捕获异常导致 500
- **修复:** `auth.service.ts:200` — 用 try/catch 包裹 `JSON.parse`，异常时抛出 `BadRequestException('Verification data corrupted. Please request a new code.')`
- **Commit:** `8ce5e54`

### 测试更新 — `deposit.service.spec.ts`
- **问题:** 原测试 mock 了 `walletService.setBalance` 但不 mock `prisma.$transaction`，导致事务回调从未执行
- **修复:**
  1. 添加 `mockPrisma.$transaction` mock 实现（将 `mockPrisma` 作为 `tx` 传入回调）
  2. 添加 `mockPrisma.wallet.findUnique/upsert` mocks（事务内读取/写入 wallet）
  3. 将 `mockWalletService.setBalance` 断言替换为 `mockPrisma.user.update`（coinBalance 同步）
  4. 修复 `should NOT trigger mission` 测试的 `mockPrisma.user.findUnique` 返回值顺序（bonus 检查有两次查询）
- **结果:** 3/3 tests pass

---

## 遗留问题

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P2-NEW-003 | JWT 存 localStorage（XSS Token 盗窃） | 🔍 待实施 | auth.ts / api.ts / auth-context.tsx，迁移至 httpOnly Cookie |
| P2-NEW-004 | `getOrCreateDepositAddress` 索引竞态窗口 | 🔍 待实施 | deposit.service.ts:62-77，并发请求可能分配相同 index（已有 P2002 catch 但仍有竞态） |
| P2-DEPOSIT-ATOMIC | 同 P2-NEW-001 | 🔍 待认领 | Test 建议升 P1，建议评估是否升级 |
| P2-JWT-LOCALSTORAGE | 同 P2-NEW-003 | 🔍 待认领 | 同上 |

---

## 测试结果

```
28 suites / 323 tests — 全部通过 ✅
```

---

*Coding 第241轮 — 2026-04-27 16:35*
