# Coding Report — 第93轮

> 时间: 2026-04-25 03:45

## 状态

| 状态 | P0 | P1 | P2 |
|------|----|----|----|
| 数量 | 0 ✅ | 1 🟡 | 0 ✅ |

---

## 本轮处理

### P1-002 Jest Worker 泄漏 — 诊断增强

**背景**: Test 代理已定位 3 个可疑位置（afterInit 定时器、Set 超时、deposit setInterval），但 `--detectOpenHandles` 标志原本未启用。

**本轮动作**: 启用 `--detectOpenHandles` 到 Jest 配置。

**改动文件**: `apps/backend/package.json`

```diff
- "test": "NODE_ENV=test jest --forceExit",
+ "test": "NODE_ENV=test jest --forceExit --detectOpenHandles",
```

**效果**:
- 所有 238 tests 通过（1.96s）
- 0 leak warnings 出现（说明泄漏是偶发性的，非每次测试都触发）
- 下次泄漏出现时，Jest 会直接打印 "open handle" 堆栈，精确定位到源代码行号

**代码审查结论**: 现有代码清理逻辑正确，无需改动：
- `TimerService.onModuleDestroy()` — 正确清除所有 3 类定时器
- `ConnectionStateService.clearAllPendingDisconnects()` — 正确清除 pendingTimeout
- `WithdrawQueueService.onModuleDestroy()` — 正确 close worker/queue/connection
- `AppGateway.onModuleDestroy()` — 正确 off roomEvents

---

## 遗留问题

| ID | 问题 | 状态 | 说明 |
|----|------|------|------|
| P1-002 | Jest Worker 泄漏 | 🟡 非阻塞 | `--detectOpenHandles` 已启用，下次发生时自动定位 |

---

## 下轮关注

1. P1-002 — 等待泄漏下次触发，精确定位根因
2. P2-004 — Club 页面 `<img>` → `<Image />` 优化（Next.js Image 组件）
3. P3-008/009 — Rakeback + Tutorial 产品需求跟进

---

*最后更新: 2026-04-25 03:45 — Coding 第93轮 — P1-002 诊断增强，build + tests 全通过*
