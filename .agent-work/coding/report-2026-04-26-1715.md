# Coding Report — 第242轮

**时间:** 2026-04-26 17:15
**HEAD:** `632fb60` — fix: P0-TYPE-001/002/003 — rakeback rate unit, HandResultEntry nickname, FriendRequestPayload field
**上一轮 HEAD:** `8ce5e54`

---

## 处理任务

### P0-TYPE-001 ✅ 已修复 — Rakeback 利率单位不一致

**问题:** 后端 `getRakeback()` 返回 `rate: 0.15`（小数），前端 `rakeback/page.tsx:295` 显示 `{rakeback.rate}%`，期望值如 `15`。
**修复:** `rakeback.service.ts` 的 `getRakeback()` 返回值中，`rate` 乘以 100：`rate: rate * 100`。注释说明这是按百分比格式返回以匹配 shared types 契约。内部 `getRakebackRate()` 保持返回小数不变（供内部计算），测试仍然通过。
**文件:** `apps/backend/src/rakeback/rakeback.service.ts`

### P0-TYPE-002 ✅ 已修复 — HandResultEntry 缺少 nickname

**问题:** `matchmaking.service.ts` 中的 `HandResultEntry` 比 `table-state.ts` 中的少 `nickname` 和 `handName`/`bestCards` 字段。
**修复:** 在 `matchmaking.service.ts` 的 `HandResultEntry` 接口添加 `nickname: string`。
**说明:** `nickname` 字段在 `updateElo()` 中未使用但属于 API 契约一部分，保持与 `table-state.ts` 定义一致。
**文件:** `apps/backend/src/matchmaking/matchmaking.service.ts`

### P0-TYPE-003 ✅ 已修复 — FriendRequestPayload 字段名不一致

**问题:** `packages/shared/src/types/friend.ts` 定义 `toUsernameOrEmail`，但后端 `SendFriendRequestDto` 使用 `usernameOrEmail`，造成 WS 事件类型与 REST API DTO 不一致。
**修复:** `FriendRequestPayload.toUsernameOrEmail` → `usernameOrEmail`，与后端 DTO 对齐。
**文件:** `packages/shared/src/types/friend.ts`

---

## 验证

| 检查                       | 结果                                        |
| -------------------------- | ------------------------------------------- |
| TypeScript 编译（backend） | ✅ 通过                                     |
| TypeScript 编译（shared）  | ✅ 通过                                     |
| Rakeback 测试              | ✅ 26 passed                                |
| 全量 lint                  | ℹ️ MDX 类型警告（node_modules，与本轮无关） |

---

## 遗留问题

| ID                  | 任务                                           | 状态                |
| ------------------- | ---------------------------------------------- | ------------------- |
| P1-WALLET-001       | `exchangeBalanceToChips` 无原子事务            | 待处理              |
| P1-WITHDRAW-005     | `createWithdraw` transaction log 在原子块外    | 待处理              |
| P1-WALLET-002       | `setBalances` Phase 2 非bot失败静默吞掉        | 待处理              |
| P1-WITHDRAW-006     | `rejectWithdrawRequest` transaction log 原子性 | 待处理              |
| P1-WITHDRAW-007     | `executeChainWithdraw` 链上确认后DB更新不原子  | 待处理              |
| P1-WS-001           | `handleConnection` Redis会话检查时序问题       | 待处理              |
| P2-EMOJI-MOBILE     | 移动端 emoji 反应缺失                          | 待处理              |
| P2-MOBILE-RECONNECT | 移动端断线重连 UI                              | 待处理              |
| P2-JWT-LOCALSTORAGE | JWT 迁移 httpOnly Cookie                       | 待处理（建议升 P1） |

---

_Coding 第242轮 — 2026-04-26 17:15 — 3 P0 已修复_
