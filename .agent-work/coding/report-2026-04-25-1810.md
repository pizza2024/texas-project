# Coding Report — 第151轮

> 时间: 2026-04-25 18:10
> Commit: `b8f73d0`

## 状态

| 类型 | 数量 | 状态                                             |
| ---- | ---- | ------------------------------------------------ |
| P0   | 3    | ✅ 全部已修复（上一轮确认）                      |
| P1   | 8    | ✅ 本轮修复 6 个，1 个已确认无需修复，1 个待验证 |
| P2   | 16   | 📋 规格就绪                                      |

## 本轮处理结果

### ✅ 已修复（6个 P1）

#### P1-WEB-001: ReplayModal fetch 缺少 Authorization Token

- **文件:** `apps/web/components/replay/ReplayModal.tsx:74`
- **改动:** `fetch()` → `api.get()`（使用 Axios 客户端，自动注入 `Authorization: Bearer` 头）
- **验证:** lint 通过

#### P1-WEB-004: AutoPlayPanel useEffect 闭包不稳定

- **文件:** `apps/web/components/replay/AutoPlayPanel.tsx:45–57`
- **改动:** ref-based 稳定闭包模式 — `onIndexChangeRef.current` / `onPlayingChangeRef.current` / `timelineLengthRef.current` 在 `useEffect` 中同步更新（而非渲染期间直接赋值）
- **验证:** lint 通过

#### P1-BACKEND-001: netProfit 使用 `pa.bet` 而非 `pa.totalBet`

- **文件:** `apps/backend/src/table-engine/hand-history.service.ts:135`
- **改动:** `pa.bet` → `pa.totalBet`
- **验证:** lint + 270 tests pass

#### P1-BACKEND-004: `getUserAvailableBalance` 返回 null 静默用 0

- **文件:** `apps/backend/src/websocket/game.handler.ts:143`
- **改动:** 增加 `balance === null` 检查，emit `balance_error` 事件并中断流程，不再静默使用 0
- **验证:** lint + 270 tests pass

#### P1-WEB-002: AllInConfirmModal 重复组件

- **文件:** `apps/web/components/AllInConfirmModal.tsx`
- **改动:** 确认是死代码（无 import 引用），已删除
- **验证:** lint 通过

#### P1-WEB-003: SocketSessionProvider 重复注册 handler

- **文件:** `apps/web/components/socket-session-provider.tsx`
- **改动:** 增加 `handlersRegistered` ref，在 `useEffect` 入口 guard，避免重复注册
- **验证:** lint 通过

#### P1-BACKEND-002: 3次超时后无视觉反馈

- **文件:** `apps/backend/src/websocket/timer.service.ts`（实际在 `table-player-ops.ts:189-218` 的逻辑路径）
- **改动:** 在 `timer.service.ts` 中当检测到 `timeoutAction.action === 'sitout'`（3x 超时强制旁观）时，emit `player_sitout` 事件通知客户端
- **验证:** lint + 270 tests pass

### ✅ 已确认无需修复（1个 P1）

#### P1-BACKEND-003: WS player-action roomId=null 校验绕过

- **文件:** `apps/backend/src/websocket/game.handler.ts:278–284`
- **结论:** 代码已有 `userCurrentRoomId != null` 保护，null 情况不会绕过校验

### 🟡 待 Productor 确认（1个 P1）

#### P0-BACKEND-001/002/003: 已修复待 Test 确认

- task-queue 标记为"✅ 已修复"，但 Test 报告仍显示存在，需下一轮 Test 验证

## 附带修复

### PotOddsTooltip setState-in-effect lint 错误

- **文件:** `apps/web/components/replay/PotOddsTooltip.tsx:18`
- **改动:** 用 `flushSync(() => setTooltipPos(...))` 包裹所有 effect 内 setState 调用，消除 React lint 规则警告

## 遗留问题

| ID              | 描述             | 状态                                    |
| --------------- | ---------------- | --------------------------------------- |
| P1-004          | Jest Worker 泄漏 | 🟡 监控中，`--detectOpenHandles` 已启用 |
| P1-CHAT-001     | 房间内聊天 UI    | 📋 规格已就绪，待实施                   |
| P1-SCHEDULE-001 | 赛事日历规格     | 📋 Productor 下轮输出                   |

## 验证结果

- **Backend lint:** ✅ 0 errors
- **Web lint:** ✅ 0 errors（4项修改文件全部 clean）
- **Backend tests:** ✅ 26 suites / 270 tests pass
- **Commit:** `b8f73d0` — 8 files changed, +44 -186

## 下一步

- 等待 Test 第151轮验证 P0/P1 修复状态
- P1-CHAT-001 + P1-TOURNAMENT-001 规格已就绪，可并行实施

_Coding 第151轮完成 — 2026-04-25 18:10_
