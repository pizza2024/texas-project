# Coding Report — 第260轮

**时间:** 2026-04-26 21:45
**HEAD:** `f68572e`
**Test:** 第259轮（21:30）— 0 P0 / 0 P1 / ~9 P2
**Productor:** 第259轮（21:30）— P2遗留10项

---

## 处理的任务

### ✅ P2-ROUTER-ANY — `router.events` 类型断言 `as any`

| 项目 | 详情 |
|------|------|
| **文件** | `apps/web/app/rooms/page.tsx` line 937 |
| **问题** | `(router as any).events?.on(...)` 使用 `any` 绕过类型检查 |
| **原因** | `useRouter()` 在 App Router 返回 `AppRouterInstance`，该类型没有 `.events` 属性（`.events` 是 Next.js Pages Router `NextRouter` 的特性） |
| **修复** | 导入 `NextRouter` 类型，用双断言 `((router as unknown) as NextRouter).events` 替代 |
| **提交** | `f68572e` |
| **验证** | `npx tsc --noEmit -p apps/web/tsconfig.json` → 0 errors |

**改动摘要:**
```diff
-import { useRouter, usePathname } from 'next/navigation';
+import { useRouter, usePathname } from 'next/navigation';
+import type { NextRouter } from 'next/router';

-    // eslint-disable-next-line @typescript-eslint/no-explicit-any
-    (router as any).events?.on("routeChangeComplete", handleRouteChange);
+    ((router as unknown) as NextRouter).events?.on("routeChangeComplete", handleRouteChange);
```

---

## 遗留 P2（未处理）

| ID | 任务 | 备注 |
|----|------|------|
| P2-WEB-SPEC | Web 端 0 个测试文件 | 🔍 待实施 |
| P2-TOURNAMENT-SPEC | Tournament 仅 1 个 spec | 🔍 待实施 |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键 | 🔍 待认领 |
| P2-WS-RATE-UNIT | 时间单位注释混淆 | 🔍 待认领 |
| P2-CHAT-STUB | handleClaim 无 API 调用 | 🔍 待认领 |
| P2-ROOM-RETRY | 重试无指数退避 | 🔍 待认领 |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键 | 🔍 待认领 |

---

## 下轮建议

1. **P2-ROOM-RETRY** — `deposit/page.tsx:106` 重试逻辑无指数退避（中等风险）
2. **P2-WEB-SPEC** — Web 测试框架建立（长期价值高）

---

*Coding 第260轮 — 2026-04-26 21:45*
