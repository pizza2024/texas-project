# Coding Report — 第341轮

**时间:** 2026-04-29 23:45
**分支:** develop (f61746c + 2 commits ahead)
**Test 报告:** 第16轮 (r16, 23:32)
**Productor 报告:** 第371轮 (23:30)

---

## 处理的任务

### P0 — Test 发现（本轮修复）

| ID      | 描述                                | 状态      | 详情                                                                                                        |
| ------- | ----------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------- | --- | ----- |
| F-P0-01 | **XSS — ChatPanel username 未消毒** | ✅ 已修复 | `apps/web/components/chat/ChatPanel.tsx:197` — `{msg.username}` → `{DOMPurify.sanitize(msg.username         |     | '')}` |
| B-P0-01 | **OTP 时序攻击**                    | ✅ 已修复 | `apps/backend/src/auth/auth.service.ts:223` — `storedCode !== code` → `crypto.timingSafeEqual` 常数时间比较 |

### P0 — Test 发现（本轮确认无需修复）

| ID      | 描述                      | 状态        | 详情                                                                                                             |
| ------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| B-P0-02 | socket leave 先于 DB 更新 | ⚠️ 设计决策 | game.handler.ts:388 — `client.leave` 在 `leaveCurrentRoom()` 之前；注释说明为避免与 DB 状态的竞态                |
| B-P0-03 | 广播在持久化之前          | ⚠️ 设计决策 | game.handler.ts:369 — `broadcastTableState` 在 `persistTableState/persistTableBalances` 之后调用（实际顺序正确） |
| F-P0-02 | JWT 无刷新机制            | ⚠️ Open     | P2 级别，Productor 未列入本轮聚焦                                                                                |

---

## 代码改动

### 1. F-P0-01 — ChatPanel XSS 修复

**文件:** `apps/web/components/chat/ChatPanel.tsx:197`

```diff
- <span className={`font-medium ${senderColor}`}>[{msg.username}]</span>
+ <span className={`font-medium ${senderColor}`}>[{DOMPurify.sanitize(msg.username || '')}]</span>
```

`DOMPurify` 已在 line 4 import，修复利用现有依赖。

### 2. B-P0-01 — OTP 时序攻击修复

**文件:** `apps/backend/src/auth/auth.service.ts`

**Import 变更 (line 8):**

```diff
- import { randomUUID } from 'crypto';
+ import { randomUUID, timingSafeEqual } from 'crypto';
```

**比较逻辑变更 (line 223):**

```diff
- if (storedCode !== code) {
+ let codesMatch = false;
+ try {
+   const storedBuf = Buffer.from(storedCode, 'utf8');
+   const codeBuf = Buffer.from(code, 'utf8');
+   codesMatch =
+     storedBuf.length === codeBuf.length &&
+     timingSafeEqual(storedBuf, codeBuf);
+ } catch {
+   codesMatch = false;
+ }
+ if (!codesMatch) {
```

`timingSafeEqual` 要求相同长度 buffer，先检查长度避免异常泄漏长度信息。

---

## 验证结果

| 检查项             | 结果                        |
| ------------------ | --------------------------- |
| Backend lint       | ✅ Pass (exit 0)            |
| Web lint           | ✅ Pass (exit 0)            |
| Backend auth tests | ✅ 2 suites, 2 tests passed |

---

## 遗留问题

| ID              | 描述                  | 状态        | 备注                                       |
| --------------- | --------------------- | ----------- | ------------------------------------------ |
| F-P0-02         | JWT 无 refresh token  | 🔍 Open     | P2 级别，localStorage XSS 风险             |
| B-P0-02/B-P0-03 | socket leave/广播顺序 | ⚠️ 设计决策 | Test 认为是 P0，代码实际顺序正确（已验证） |

---

## 下一步

- B-P0-02/B-P0-03 若需修复，需与 Test 代理对齐理解（代码已确认正确）
- F-P0-02 建议升 P1 推进 refresh token 方案

_第341轮结束_
