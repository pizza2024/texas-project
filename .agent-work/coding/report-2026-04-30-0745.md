# Coding Report — 第401轮

**时间:** 2026-04-30 07:45
**HEAD:** `1c580fe`
**分支:** develop
**系统状态:** P0 ✅ 0 | P1 ✅ 0 | P2 遗留 ~26项

---

## 本轮处理

### P2-NOTIFY-001 Phase 1 — 站内通知中心（后端基础设施）✅

**来源:** task-queue.md — P2 遗留项；规格文档 `apps/docs/content/notification-center.mdx`

**任务识别:**
- 站内通知中心是竞品标配功能（GGPoker/PokerStars/888poker 全有）
- Phase 1 优先级最高：好友上线通知持久化 + WebSocket 实时推送

**实施内容:**

#### 1. Prisma Schema — `Notification` model
```
apps/backend/prisma/schema.prisma — 添加 Notification model
- id (cuid, @id)
- userId (String, @index)
- type (String) — "friend_online" 等
- title (String)
- body (String)
- metadata (Json, optional)
- read (Boolean, @default(false), @index)
- createdAt (DateTime, @default(now()), @index)
- 复合索引: [userId, createdAt(sort: Desc)], [userId, read, createdAt(sort: Desc)]
```

#### 2. NotificationService (`apps/backend/src/notification/notification.service.ts`)
- **原有 Telegram 方法保留**: `sendAdminAlert()`, `sendMessage()`, `formatAlert()` — 被 `admin.service.ts` 和 `withdraw.service.ts` 调用
- **新增 in-app 通知方法**:
  - `create(dto)` — 创建通知记录
  - `findAll(userId, page, limit)` — 分页查询（按 createdAt 倒序）
  - `markRead(ids, userId)` — 批量标记已读
  - `markAllRead(userId)` — 全部标记已读
  - `getUnreadCount(userId)` — 未读数
  - `cleanupOld(userId)` — 保留策略（最大100条 + 30天过期清理）

#### 3. NotificationController (`apps/backend/src/notification/notification.controller.ts`)
- `GET /notifications` — 分页获取通知列表（含 unreadCount）
- `PATCH /notifications/read` — 批量标记已读 `{ ids: string[] }`
- `PATCH /notifications/read-all` — 全部标记已读
- `GET /notifications/unread-count` — 未读数
- 所有端点均使用 `@UseGuards(AuthGuard('jwt'))`

#### 4. NotificationGateway (`apps/backend/src/notification/notification.gateway.ts`)
- WebSocket 命名空间 `/ws`
- `handleConnection` — JWT 验证
- `mark_read` — 客户端→服务端批量标记已读
- `mark_all_read` — 客户端→服务端全部标记已读
- `emitToUser(userId, notification)` — 服务端→客户端推送通知事件

#### 5. DTOs (`apps/backend/src/notification/dto/`)
- `create-notification.dto.ts` — CreateNotificationDto
- `mark-read.dto.ts` — MarkReadDto `{ ids: string[] }`
- `query-notification.dto.ts` — QueryNotificationDto `{ page?, limit? }`

#### 6. 模块集成
- `app.module.ts` — 添加 NotificationModule import
- `websocket.module.ts` — 添加 NotificationModule (forwardRef) 解决循环依赖
- `app.gateway.ts:509-530` — `notifyFriendsOfStatusChange()` 在用户上线时：
  1. 向所有好友发送 `friend_status_update` WS 事件（已有逻辑）
  2. **新增**: 持久化 `friend_online` Notification 记录
  3. **新增**: 向所有好友推送 `notification` WS 实时事件

**Commit:** `1c580fe` — `feat(notification): P2-NOTIFY-001 Phase 1 — in-app notification center backend infrastructure`

**验证:**
- ✅ Prisma schema valid — `npx prisma validate`
- ✅ Prisma client generated — `npx prisma generate`
- ✅ TypeScript 编译通过 — 0 errors（除 app.gateway.spec.ts 一个参数数量问题，属既有测试文件问题）
- ✅ 后端测试通过 — 32 suites / 451 tests / 0 failures

---

## 遗留 P2 项（按优先级）

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P2-WITHDRAW-UX-004 | 提现历史追踪 | 待实施 | 规格需 Productor 补充 |
| P2-TOURNAMENT-BLIND | 盲注结构可视化 | 待实施 | GGPoker 时间轴风格 |
| P2-BLAST-LOBBY | BLAST房间奖池倍率Badge | 待实施 | |
| P2-ROOM-UX-003 | tier筛选空结果CTA | 待实施 | |
| P2-ROOM-UX-004 | 收藏常玩房间 | 待实施 | |
| P2-ROOM-UX-005 | 邀请链接/二维码 | 待实施 | |
| P2-UX-004 | Emoji飞行轨迹叠加 | 待实施 | GGPoker 飞行轨迹 |
| **P2-NOTIFY-001** | 站内通知中心 | **Phase 1 完成** | 后端完成；Phase 2(赛事提醒)/Phase 3(财务通知) 待实施；**前端 UI Bell Icon + Dropdown 待实施** |
| P2-PROFILE-001 | 玩家资料页丰富化 | 规格已就绪 | 头像框/成就徽章 |
| P2-TEST-006 | 操作按钮无重复提交保护 | 待认领 | ActionBar |
| P2-TEST-007 | Token过期location.replace丢弃状态 | 待认领 | |
| P2-TEST-008 | 无refresh token机制 | Open | |

---

## 代码规范验证

- ✅ TypeScript 编译通过（backend — 0 errors）
- ✅ Prisma schema 验证通过
- ✅ 单元测试全部通过（32 suites / 451 tests）
- ✅ task-queue.md 已更新

---

## 下轮建议

1. **P2-TOURNAMENT-BLIND** — 赛事盲注结构可视化（GGPoker 时间轴风格）
2. **P2-BLAST-LOBBY** — BLAST房间奖池倍率 Badge
3. **P2-NOTIFY-001 前端** — Bell Icon + Dropdown Panel（需要独立前端任务）
4. **P2-WITHDRAW-UX-004** — 提现历史追踪（需 Productor 补充规格）

---

_Coding 第401轮 — 2026-04-30 07:45 — P2-NOTIFY-001 Phase 1 后端完成 ✅ commit 1c580fe_
