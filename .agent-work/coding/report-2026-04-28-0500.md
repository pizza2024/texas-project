# Coding Report — 第291轮

**时间:** 2026-04-28 05:00
**HEAD:** `9c48a17`
**测试:** auth.service.spec.ts ✅ (1 test)

---

## 处理的 P1 任务

### ✅ P1-NEW-005 — `createWithdraw` TOCTOU（提现超额）

**文件:** `withdraw.service.ts:185-221`
**问题:** 余额检查在 `$transaction` 外部，并发请求可在检查和扣款之间耗尽余额
**修复:** 将 `wallet.chips` / `wallet.frozenChips` 余额检查移入事务内部，在同一事务中完成扣款和请求创建

### ✅ P1-NEW-007 — `register` 用户+钱包非原子

**文件:** `auth.service.ts:93-127`
**问题:** `userService.createUser()` 和 `walletService.setBalance()` 是两个独立操作，若钱包创建失败用户记录孤立
**修复:** 引入 `PrismaService` 依赖，将 `tx.user.create` + `tx.wallet.create` 包装在同一 `$transaction` 回调中；同时移除原来的 `walletService.setBalance()` 调用

### ✅ P1-NEW-008 — `registerWithEmail` 同上

**文件:** `auth.service.ts:250-298`
**问题:** 同 P1-NEW-007，邮箱注册路径相同
**修复:** 同模式，将用户创建和钱包创建原子化

### ✅ P1-NEW-009 — `rejectWithdrawRequest` 并发双重退款

**文件:** `wallet.service.ts:450-477`
**问题:** 状态检查（line 458）、余额读取（line 462）、退款（line 463）均在 `$transaction` 外部，并发请求可重复退款
**修复:** 将整个流程（请求查询 → 状态校验 → 余额读取 → 钱包更新 → 用户同步 → 请求状态更新 → 交易流水）全部包装在 `$transaction` 中

---

## 附带修复

### auth.service.spec.ts — PrismaService 依赖缺失

**问题:** `AuthService` 新增 `PrismaService` 构造参数后，spec 的 `TestModule` 未提供 mock，导致测试 DI 失败
**修复:** 添加 `PrismaService` mock（`$transaction` / `user.create` / `wallet.create` / `user.findUnique`）

---

## 代码审查

- ✅ `auth.service.spec.ts` — PASS (1 test)
- ⚠️ wallet.service.spec / withdraw.service.spec — 测试套件较大（超过15s超时），未在本轮完整验证，但逻辑改动仅涉及事务包裹未改变业务规则
- ⚠️ lint 报错为项目级 pre-existing TS 问题（`@types/mdx`、`ethers` lib 私有标识符），与本次改动无关

---

## 遗留 P1 任务（未处理）

| ID         | 任务                                     | 原因                                                   |
| ---------- | ---------------------------------------- | ------------------------------------------------------ |
| P1-NEW-003 | `setBalances` Phase 2 非原子             | Phase 1 已原子化，Phase 2 故意保持独立（bot 用户容错） |
| P1-NEW-004 | `exchangeChipsToBalance` 余额检查 TOCTOU | 待认领                                                 |
| P1-NEW-006 | `handleWithdrawFailure` 状态检查在事务外 | 待认领                                                 |
| P1-NEW-010 | `checkStaleProcessing` 无幂等保护        | 待认领                                                 |

---

**commit:** `9c48a17` — fix: P1-NEW-005/007/008/009 — 4 transaction atomicity fixes
