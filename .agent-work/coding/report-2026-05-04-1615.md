# Coding Report — 2026-05-04 16:15

**分支:** develop
**HEAD:** `feat/insurance` (ahead of origin/develop by 3 commits)
**Commit:** `feat(insurance): P2-INSURANCE-001 All-In Insurance Phase 1`

---

## 本轮工作

### P0/P1 状态
- P0/P1: ✅ 全部清零（Test r97 确认）

### P2-INSURANCE-001 实施 — Phase 1 完成

#### 后端新增文件
| 文件 | 说明 |
|------|------|
| `insurance/insurance.service.ts` | 核心服务：offer/buy/skip/settle + Admin统计 |
| `insurance/insurance.controller.ts` | Admin API: `/admin/insurance` + `/admin/insurance/stats` |
| `insurance/insurance.module.ts` | 模块定义，导入 PrismaModule + WalletModule |
| `table-engine/insurance-calculator.ts` | 纯函数计算器：`calculateInsuranceFee` / `calculateInsurancePayout` / `checkInsuranceEligibility` |
| `table-engine/insurance-calculator.spec.ts` | 22个单元测试 |

#### 后端修改
| 文件 | 改动 |
|------|------|
| `prisma/schema.prisma` | 新增 `InsuranceTransaction` model（handId/userId/roomId/tableId/rate/fee/payout/playerEquity/result + indexes） |
| `app.module.ts` | 导入 `InsuranceModule` |
| `websocket/game.handler.ts` | 新增 `handleBuyInsurance` + `handleSkipInsurance` WS handlers |
| `websocket/websocket.module.ts` | `InsuranceModule` 已在 import 列表 |
| `table-engine/player.ts` | `Player` 接口新增 `hasPurchasedInsurance` / `insuranceRate` / `insuranceFee` 字段 |

#### 前端新增
| 文件 | 说明 |
|------|------|
| `components/insurance/InsuranceOfferModal.tsx` | 保险购买弹窗（50%/100%两档 + rAF倒计时 + Skip按钮） |

#### Bug修复
- `insurance-calculator.ts`: `equity=0` 边界条件 — 原先错误返回0，修复后正确返回 `netLoss × rate / 100`（0equity = 冷牌，fee应等于grossLoss）

---

## ESLint / TS 状态

- **Lint:** ✅ backend 0 errors + web 0 errors
- **insurance-calculator tests:** ✅ 22/22 passed

---

## 待续工作（不在本轮范围）

1. **前端集成** — `InsuranceOfferModal` 需在 `room/[id]/page.tsx` 中接入：
   - 监听 `insurance_offer` WS 事件
   - 在 AllInConfirmModal 之后显示
   - 发送 `buy_insurance` / `skip_insurance` WS 事件

2. **Table-engine 集成** — `handlePlayerAction(ALLIN)` at RIVER 需调用 `insuranceService.offerInsurance()`，showdown 时调用 `settleInsurance()`

3. **Prisma迁移** — `npx prisma migrate dev` 生成迁移脚本

---

_Last updated: 2026-05-04 16:15 — Coding Agent_
