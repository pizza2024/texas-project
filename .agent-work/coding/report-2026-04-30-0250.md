# Coding 报告 — 第346轮

**时间:** 2026-04-30 02:50
**HEAD:** `a19ded8`
**分支:** develop

---

## 处理的任务

### P2-UX-001: ActionBar Fold/Call 按钮内显示倒计时秒数 ✅
- **文件:** `apps/web/app/room/[id]/components/ActionBar.tsx`
- **改动:**
  - Fold 按钮: 当 myTurn && canCheck 时显示 `Fold ·{n}s`
  - Call 按钮: 当 myTurn 时显示 `Call ${amount} ·{n}s`
  - Check 按钮保持原有 `Check ·{n}s` 逻辑
- **参考:** GGPoker 标配

### P2-UX-002: Raise Input 默认值同步 minRaiseTo + autoFocus ✅
- **文件:** `apps/web/app/room/[id]/components/ActionBar.tsx`
- **改动:**
  - 添加 `useRef<HTMLInputElement>()` + `useEffect` 在 `isMyTurn` 变化时 auto-focus + select
  - Input value 在 myTurn 且 raiseAmount === 0 时默认填充 `minRaiseTo`
  - 用户手动输入后保持用户输入值
- **参考:** GGPoker 自动 focus + 预填

### P2-UX-003: All-in 独立金色大按钮（WSOP 风格）✅
- **文件:** `apps/web/app/room/[id]/components/ActionBar.tsx`
- **改动:**
  - 新增独立 `h-14` 按钮，紧邻 raise 区
  - 金色渐变背景 + 白色文字 + glow shadow + 悬停放大
  - 仅在 `isMyTurn && myPlayerStack > 0` 时显示
  - 原 All-in 快捷按钮保留在 presets 组内
- **参考:** WSOP 独立金色大按钮

### P2-UX-004: Emoji 飞行轨迹叠加到 GameTable ⏭️ 跳过
- 经分析：`EmojiOverlay` 已存在于 `GameTable` 外层（page.tsx:882-886）
- `emojiFloatUp` 关键帧已实现 float-up 轨迹动画（从 seat 位置向上飘散）
- 完整 GGPoker 风格的"头像飞向中央"轨迹需要 GameTable 内部坐标系统改造，工程量较大
- **结论:** 当前实现已满足 P2 级别，建议 Productor 重新评估是否保持 P2 或关闭

---

## 代码质量
- TypeScript 编译: ✅ 0 errors
- Lint/Format: ✅ 通过

---

## 遗留问题
- P2-UX-004: Emoji 飞行轨迹叠加（EmojiOverlay 已在 table 外层，需评估是否关闭）

---

*报告生成: 2026-04-30 02:50 — Coding 第346轮*
