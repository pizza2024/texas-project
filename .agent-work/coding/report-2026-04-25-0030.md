# Coding Report — 第80轮

> 时间: 2026-04-25 00:30

---

## 处理的任务

### P2-NEW-005: All-in 弹窗 equity 硬编码 ✅ 已修复

**问题**: `apps/web/app/room/[id]/page.tsx` line 823，`equity={50}` 硬编码为 50%，非真实胜率。

**改动**:

1. **新建 `packages/shared/src/equity.ts`** — 纯函数蒙特卡洛模拟:
   - 复制了 `RANK_VALUES` 常量（与后端 `table-state.ts` 一致）
   - 实现了 `evaluate5()`, `bestHandFrom()`, `compareScores()`, `combinations()` 等手牌评估逻辑
   - `calculateEquity(holeCards, communityCards, opponentCount?, simulations?)` — 2000次蒙特卡洛迭代，返回 0-100% 胜率
   - preflop（无社区牌）时 Hole Cards 验证失败返回 50

2. **`packages/shared/src/index.ts`** — 新增 `export * from './equity';`

3. **`apps/web/app/room/[id]/page.tsx`**:
   - `import { calculateEquity } from '@texas/shared';`
   - `const equity = myHoleCards.length === 2 && opponentCount >= 1 ? calculateEquity(myHoleCards, communityCards, opponentCount) : 50`
   - 替换 `equity={50}` → `equity={equity}`

**Lint**: ✅ 通过（0 errors, 11 pre-existing warnings）

---

## 遗留问题（未处理）

| ID         | 问题                              | 原因                                                                         |
| ---------- | --------------------------------- | ---------------------------------------------------------------------------- |
| P0-001     | Redis宕机时JWT验证被绕过          | 设计决策：TaskQueue 标注"已验证无需修复"，日志标记 `[SECURITY-AUTH-BYPASS]`  |
| P0-002     | Withdraw cooldown bypass          | TaskQueue 标注"代码已正确throw"，`withdraw.service.ts` line 129 已正确 throw |
| P0-003     | processWithdraw 服务层无Admin验证 | TaskQueue 标注"AdminGuard在Controller层执行，服务层为私有"                   |
| P1-002     | Jest Worker 泄漏                  | 非阻塞，215 tests 全部通过                                                   |
| P2-002     | 牌局恢复时双重余额恢复            | 待分析 `table-manager.service.ts`                                            |
| P2-NEW-003 | 浮点数芯片金额                    | `game.handler.ts` 中 `z.number()` — 待查是否实际存在问题                     |
| P2-NEW-004 | 反向代理真实IP                    | nginx X-Forwarded-For 未解析                                                 |

---

## 状态摘要

- **Backend HEAD**: `d6e18df`（无新提交）
- **本轮新增提交**: 1个（equity.ts + page.tsx 改动）
- **Backend Jest**: 待本轮运行确认（上次 215 tests 通过）
- **Lint**: ✅ 通过
- **P0/P1**: 均标注为"已验证无需修复"或非阻塞

---

_P80 完成 — P2-NEW-005 equity 硬编码已修复交付_
