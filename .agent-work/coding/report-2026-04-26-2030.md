# Coding Report — 第257轮

**时间:** 2026-04-26 20:30
**HEAD:** `31f4bba` — fix(deposit): P2-DEPOSIT-TOCTOU
**上一轮:** 88af108 (P1-CHAT-INJECTION)

## 状态

| 优先级 | 修复前 | 修复后 |
| ------ | ------ | ------ |
| P0     | 0      | 0      |
| P1     | 0      | 0      |
| P2     | 11     | 7      |

---

## 本轮修复（3项）

### ✅ P2-CHAT-DUP — 删除重复 ChatPanel 文件

**文件:** `apps/web/app/room/[id]/components/ChatPanel.tsx` (已删除)

**分析:**

- `page.tsx` 从 `@/components/chat/ChatPanel` 导入（共用组件）
- `./components/ChatPanel.tsx` 是重复副本，无任何文件引用
- mobile 页面不使用 ChatPanel
- diff 对比确认两文件仅差 1 行 rate-limit 注释（web 版有 `// Client-side rate limit: min 500ms between messages`）

**操作:** 删除重复文件，保留共用 `apps/web/components/chat/ChatPanel.tsx`

**Commit:** `c7c9be8`

---

### ✅ P2-ROOM-PASSWORD — sessionStorage 密码未清除

**文件:** `apps/web/app/room/[id]/page.tsx:334-337`

**问题:** Web room 页面读取密码后从未清除，XSS 可窃取密码
**参照:** mobile 页面正确地 `removeItem`

**修复:**

```typescript
// 修复前（密码从未删除）
socket.on("connect", () => {
  socket.emit("join_room", { roomId: id as string, password: roomPassword });
});

// 修复后（emit 之后清除密码）
socket.on("connect", () => {
  socket.emit("join_room", { roomId: id as string, password: roomPassword });
  sessionStorage.removeItem(passwordKey);
});
```

**Commit:** `c7c9be8`

---

### ✅ P2-DEPOSIT-TOCTOU — getOrCreateDepositAddress 索引竞争

**文件:** `apps/backend/src/deposit/deposit.service.ts`

**问题:** 原实现在事务外做 aggregate 查询，并发时两个请求读到相同 `_max.index`，产生重复 HD 钱包地址

**修复:** 将 aggregate + createMany 整体移入 `$transaction` 内部：

- 事务内 re-check `findUnique` 防并发插入
- `aggregate` 在事务内执行保证一致性
- `createMany({ skipDuplicates: true })` 兜底防止极端竞态
- 最终 `findUnique` 取胜者记录

**同步修复:** `deposit.e2e.spec.ts` 两个因 aggregate mock 缺失导致的测试失败

**Commit:** `31f4bba`

---

## 验证

| 检查项          | 结果                          |
| --------------- | ----------------------------- |
| 后端测试        | ✅ 30 suites / 325 tests PASS |
| TypeScript 编译 | ✅ 通过                       |
| ESLint (web)    | ✅ 通过                       |

---

## 遗留 P2（7项）

| ID                           | 任务                                | 状态      |
| ---------------------------- | ----------------------------------- | --------- |
| P2-TOURNAMENT-SPEC           | Tournament 测试覆盖不足             | 🔍 待实施 |
| P2-WEB-SPEC                  | Web 端 0 测试文件                   | 🔍 待实施 |
| P2-MOBILE-SPEC               | Mobile 测试覆盖不足                 | 🔍 待实施 |
| P2-CHAT-FRONTEND-TEST        | ChatPanel WS 组件测试               | 🔍 待实施 |
| P2-CHAT-IDEMPOTENCY          | 聊天消息无幂等键                    | 🔍 待认领 |
| P2-WS-RATE-UNIT              | PASSWORD_ATTEMPT_WINDOW_MS 单位混淆 | 🔍 待认领 |
| P2-ROOM-RETRY                | 重试逻辑无指数退避                  | 🔍 待认领 |
| P2-JWT-LOCALSTORAGE-FRONTEND | 前端 cookie auth 迁移               | 🔍 待实施 |

---

_Coding 第257轮 — 2026-04-26 20:30 — 修复 3 P2 (P2-CHAT-DUP + P2-ROOM-PASSWORD + P2-DEPOSIT-TOCTOU)_
