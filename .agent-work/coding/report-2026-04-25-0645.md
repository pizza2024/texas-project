# Coding Report — 第105轮

> 时间: 2026-04-25 06:45
> HEAD: `5912f19` (新提交)

## 状态

| 状态 | P0 | P1 | P2 |
|------|----|----|-----|
| 数量 | 0 🟢 | 2 (完成) | 0 |

## 本轮处理

### P1-NEW-001 — buildPots 边池计算 ✅ 已确认 + 提交

**问题**: Test 报告 `buildPots(allPlayers)` 可能在 line 265 使用 stale `allPlayers` 导致 all-in 边池计算错误。

**分析结论**: 代码本身无 bug。`buildPots` 在 `performShowdown()` (line 179) 被调用，传入的 `allPlayers` 是该函数内重新过滤的最新快照 (line 154-156)，而非外部 stale 引用。

```typescript
// table-round.ts:153-179
private performShowdown(): void {
  const allPlayers = this.table.players.filter(...)  // ← 每次调用都重新过滤
  const eligible = allPlayers.filter(p => p.status !== PlayerStatus.FOLD)
  // ...
  const pots = this.buildPots(allPlayers);  // ← 使用最新 allPlayers
}
```

**动作**: 确认无需修改，提交留存记录。

### P1-NEW-002 — Withdraw 幂等性修复 ✅ 已提交

**问题**: 工作区 `withdraw.service.ts` 有修复但未提交。

**已提交的修复**:

1. **`executeChainWithdraw`** — txHash 保存时机修正：
   - **Before**: `txHash` 在 `tx.wait(1)` **之前**保存 → 交易失败时留下脏数据
   - **After**: `txHash + fromAddress + status` 在 `tx.wait(1)` **之后**一起原子保存
   - **Also**: 新增 `CONFIRMED` 状态的幂等跳过逻辑

2. **`processWithdraw`** — PROCESSING 状态更新加事务：
   - 使用 `prisma.$transaction` 先 re-check `status === 'PENDING'`，再更新，防止并发 race condition

**Commit**: `5912f19` — "fix: P1-NEW-001 buildPots + P1-NEW-002 withdraw idempotency"

## 测试结果

```
Test Suites: 24 passed, 24 total
Tests:       239 passed, 239 total
Time:        2.415 s
```

## 遗留任务

| ID | 任务 | 状态 |
|----|------|------|
| P1-004 | Jest Worker 泄漏 | 🟡 监控中 |
| P1-TEST-001 | `connection-state.service.spec.ts` 缺失 | 🟡 待实现 |

## 下轮关注

- P1-004: Jest `--detectOpenHandles` worker 泄漏需在下次 Test 报告中确认状态
- P1-TEST-001: `connection-state.service.spec.ts` 缺失 — 可安排实现

---

*Coding 第105轮 — P0 全部清零，P1-NEW-001/002 已关闭，239 tests pass，commit `5912f19`*
