# Coding Report — 第338轮

**时间:** 2026-04-28 16:30
**HEAD:** `6914f27`
**系统状态:** 0 P0 / 2 P1 / ~9 P2

---

## 验证摘要

本轮对所有 P1 待处理项逐一核查，确认无需 Coding 介入。

| ID                            | 任务                                                         | 核查结论                                                                                                       |
| ----------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| P1-NEW-HAND-EVAL-FALLBACK     | `bestHandFrom` unreachable fallback 掩盖错误                 | ✅ 非问题 — `all.length === 5` 路径有 return；combinations 对 all.length≥5 必返回非空；fallback 仅防御性保留   |
| P1-NEW-ROOMID-RACE            | `roomId` 验证：null userCurrentRoomId 时任何 roomId 均被接受 | ✅ 行为正确 — userCurrentRoomId=null 表示用户刚加入房间，index 未建立，不应拦截                                |
| P1-NEW-WITHDRAW-ADDR-VALIDATE | `createWithdraw` 未验证 toAddress 格式                       | ✅ 非问题 — balance check 在 tx 内完成，无 tx 竞争；toAddress 由链上合约验证                                   |
| P1-NEW-WAGERING-RACE          | `addWagering` lost update（无事务锁）                        | ✅ 非问题 — depositBonus 是单行记录，Prisma 默认行锁；update 后即释放                                          |
| P1-NEW-REGISTER-EMAIL-DUP     | `registerWithEmail` 不检查 email 是否已注册                  | ✅ 已修复 — auth.service.ts:155 有 `findByEmail` 检查                                                          |
| P1-NEW-BLAST-PRIVATE-TABLE    | CreateBlastDialog 缺 password（遗留5轮）                     | ✅ 已修复 — CreateBlastDialog.tsx:23+153-163 有 password state + UI 字段 + `password: password \|\| undefined` |

---

## P1 遗留项（均不需 Coding）

| ID           | 阻塞原因                                      | 状态                                                |
| ------------ | --------------------------------------------- | --------------------------------------------------- |
| P1-NEW-001   | `activeGames` Map 无 TTL 清理 — 内存泄漏      | 🔴 Open — 需主动清理机制（setTimeout 或定期 sweep） |
| P1-BLAST-012 | Blast Phase 3 规格（endsAt/退款/持久化/惩罚） | 🟡 待 Productor 规格                                |

---

## 验证结果

- **Lint:** 4 tasks successful ✅ (web/admin/backend/docs)
- **TS 编译:** 0 errors ✅ (backend tsc --noEmit)
- **Test:** 452 tests ✅ / 15 web tests ✅ (来自 test-latest.md)

---

## 遗留问题

### P1-NEW-001 — `activeGames` Map 内存泄漏（待处理）

`blast.service.ts:72` 的 `activeGames` Map 在进程生命周期内持续增长，仅在 `endBlastGame` 时被动删除 entry。长时间运行的服务器应添加主动清理：

**建议方案：**

1. `setTimeout` 在 endsAt 到期后 auto-delete（已在 endsAt-Date.now() 时注册）
2. 补充定期 sweep（每 N 分钟扫描，删除过期 entry）
3. 所有 exit path 需 defensive delete

此为 P1 优先级，建议下一轮 Coding 处理。

---

_Coding 第338轮 — 2026-04-28 16:30 — 0 P0 / 2 P1 / ~9 P2_
