# Coding Report — 第396轮

**时间:** 2026-04-30 05:45
**HEAD:** `5e73a2f`
**分支:** develop

---

## 处理的任务

### P2-DEPOSIT-UX-004 — `SavedAddress.network` 不存在（实际为 `isDefault`）

**问题:** `SavedAddress` 接口定义了 `network: string` 字段，但 Prisma schema 的 `DepositAddress` 模型没有 `network` 字段。API 返回的字段是 `isDefault: boolean`。

**修复:** `apps/web/app/deposit/page.tsx`
- `SavedAddress.network` → `SavedAddress.isDefault: boolean`

**影响:** 地址簿 UI 的 `network` 显示现在可以正确显示 `isDefault` badge。

---

### P2-DEPOSIT-UX-005 — `handleSaveAddress`/`handleDeleteAddress` 错误静默丢弃

**问题:** 两个函数在 `catch` 块中静默失败，用户完全不知道操作失败。

**修复:** `apps/web/app/deposit/page.tsx`
- `handleSaveAddress`: `console.error('[handleSaveAddress]', err)` + `setFaucetMsg({ type: 'error', text: '保存地址失败，请稍后重试' })`
- `handleDeleteAddress`: `console.error('[handleDeleteAddress]', err)` + `setFaucetMsg({ type: 'error', text: '删除地址失败，请稍后重试' })`

---

## 遗留 P2（未处理）

| ID | 任务 | 备注 |
|----|------|------|
| P2-DEPOSIT-UX-003 | 充值手续费透明化 | 待实施 |
| P2-WITHDRAW-UX-001~004 | 提现 UX | 待实施 |
| P2-ROOM-UX-001~005 | 房间列表 UX | 待实施 |
| P2-UX-004 | Emoji 飞行轨迹 | 待实施 |
| P2-NOTIFY-001 | 站内通知中心 | 规格已就绪，待实施 |

---

## 验证

- ✅ TypeScript 编译通过（backend）
- ✅ `deposit.e2e.spec.ts` — 15/15 通过
- ✅ Commit `5e73a2f` 完成

---

_Coding r396 — 2026-04-30 05:45 — P2-DEPOSIT-UX-004/005 已修复并提交_
