# Coding Report — 第232轮

**时间:** 2026-04-26 14:19
**Commit:** `ca66c4a`

## 处理的任务

### P1-WITHDRAW-003 — `processWithdraw` REJECT 路径无事务保护

**文件:** `apps/backend/src/withdraw/withdraw.service.ts:360`

**问题:** REJECT 路径中 `refundChips()` (更新 wallet + user + transaction) 和 `withdrawRequest.update()` 是分离的操作，并发请求可导致钱包余额丢失更新。

**修复:** 将整个 REJECT 逻辑包裹在 `$transaction` 内：

- 重查 PENDING 状态（防止竞态）
- `wallet.upsert` + `user.update` + `transaction.create` 原子执行
- `withdrawRequest.update(status=FAILED)` + `adminLog` 原子执行

**状态:** ✅ 已修复，commit `ca66c4a`

---

### P1-WITHDRAW-004 — `handleWithdrawFailure` 无事务保护

**文件:** `apps/backend/src/withdraw/withdraw.service.ts:594`

**问题:** `withdrawRequest.update(status=FAILED)` 和 `refundChips()` 分离，并发调用导致双重退款。

**修复:** 包裹在 `$transaction` 内，status 更新在 refund 之前（防止双重退款），使用 `upsert` 保证幂等性。

**状态:** ✅ 已修复，commit `ca66c4a`

---

### P1-CHAT-XSS — 聊天室消息内容未转义（XSS 漏洞）

**文件:** `apps/web/components/chat/ChatPanel.tsx:198`

**问题:** `msg.content` 直接作为 React children 渲染，恶意用户可注入 `<script>`、`onerror` 等 payload。

**修复:**

1. 安装 `dompurify` 依赖 (`pnpm add dompurify --filter web`)
2. `msg.content` 通过 `DOMPurify.sanitize()` 净化后用 `dangerouslySetInnerHTML` 渲染
3. 保留 dangerouslySetInnerHTML 正确处理换行等合法 HTML

**额外清理:** 删除 `apps/web/app/room/[id]/components/ChatPanel.tsx`（未使用的重复文件）

**状态:** ✅ 已修复，commit `ca66c4a`

---

## 验证

- **Backend tests:** 28 suites, 323 tests ✅
- **Pre-existing TS errors:** 0 new (仅有 node_modules/ethers 的 TS1240/TS18028 和 @types/mdx 的 JSX namespace 错误，与本次改动无关)

## 遗留问题

无 P1/P2 遗留。

---

_Coding 第232轮 — 2026-04-26 14:19_
