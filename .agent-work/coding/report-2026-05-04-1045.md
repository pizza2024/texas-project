# Coding Report — r415

**时间:** 2026-05-04 10:47
**HEAD:** `bc0f38a`
**分支:** develop

---

## 状态

- **P0/P1:** ✅ 清零（无新增）
- **单元测试:** 451/452 (1 skipped) ✅
- **ESLint:** ✅ backend 4/4 + web 0 errors
- **TS 编译:** ✅ backend + web 均 0 errors

---

## 本轮处理

### P2-SCHEDULE-001 — TournamentScheduleController AdminGuard

**问题:** `POST :id/start`、`POST :id/complete`、`DELETE :id`、`POST :id/reminder` 端点无鉴权，任何认证用户可操控赛事（启动/取消/完成/发送提醒）。

**修复:**

1. `tournament-schedule.controller.ts` — 导入 `UseGuards` + `AdminGuard`，4个端点添加 `@UseGuards(AdminGuard)`
2. **新建** `tournament-schedule.module.ts` — 创建缺失的 Module，提供 `JwtModule.register`（AdminGuard 依赖 `JwtService`）和 `AdminGuard` provider
3. `app.module.ts` — 导入并注册 `TournamentScheduleModule`

**commit:** `bc0f38a`

---

### P2-SCHEDULE-002 — mission admin reset AdminGuard

**问题:** `POST missions/admin/reset-daily` 和 `POST missions/admin/reset-weekly` 端点注释标注"生产环境应加 AdminGuard"但未实现，任何认证用户可重置全服任务进度。

**修复:**

1. `mission.controller.ts` — 导入 `AdminGuard`，两个端点添加 `@UseGuards(AdminGuard)`；移除已过时的内联注释
2. `mission.module.ts` — 添加 `JwtModule.register` + `AdminGuard` 到 providers（JwtService 依赖）

**commit:** `bc0f38a`（同 P2-SCHEDULE-001）

---

## 代码变更

| 文件 | 变更 |
|------|------|
| `tournament-schedule.controller.ts` | +`UseGuards` + `AdminGuard` import；+`@UseGuards(AdminGuard)` × 4 端点 |
| `tournament-schedule.module.ts` | **新建** — Module with JwtModule + AdminGuard |
| `app.module.ts` | +`TournamentScheduleModule` import + 数组注册 |
| `mission.controller.ts` | +`AdminGuard` import；+`@UseGuards(AdminGuard)` × 2 端点；移除过时注释 |
| `mission.module.ts` | +`JwtModule.register`；+`AdminGuard` provider |

---

## 遗留 P2

| ID | 任务 | 状态 |
|----|------|------|
| P2-TOURNAMENT-GTD | GTD 保底奖池规格 | ⏳ 待 Productor |
| P2-NOTIFY-EMAIL-WIRE | 邮件预埋字段 | ⚠️ 等 Resend |
| P2-WITHDRAW-UX-004 | 提现历史追踪 UI | ⏳ 待 Coding |
| P2-SOCIAL-001 | 观战系统 | 🆕 建议 |
| P2-SOCIAL-002 | 私人俱乐部 | 🆕 建议 |

---

_Coding r415 — 2026-05-04 10:47 — P2-SCHEDULE-001/002 均已修复并提交；所有测试通过_
