# Coding Report — 2026-04-25-0011

## 处理的任务

### P2-003: Sit-Out Option C（完成）

- **目标**: 连续3次超时未操作 → 强制坐下（sit-out）
- **改动**:
  - `player.ts`: 添加 `consecutiveTimeouts: number = 0` 字段
  - `table-player-ops.ts`: 添加 `MAX_CONSECUTIVE_TIMEOUTS = 3` 常量，更新 `createTablePlayer()` 初始化
  - `table-player-ops.ts`: 重写 `checkAndAutoFoldSittingOut()` 追踪连续超时，达到3次时强制坐下
  - `table-game-logic.ts`: `startHand()` 重置所有玩家的 `consecutiveTimeouts`
  - `table-player-ops.ts`: fold/check/call/raise/allIn 动作后重置 `consecutiveTimeouts = 0`
- **i18n**: `en.json` / `zh-CN.json` 添加 `room.sitOutWithCount: "暂离 ({{count}}/3)"`
- **Web UI**: Sit-Out 按钮显示当前超时计数 `暂离 (1/3)`
- **测试**: 27/27 sit-out 相关测试通过

### P2-004: All-in 确认弹窗（完成）

- **目标**: 玩家点击 All-in 时显示确认弹窗（显示 pot odds + equity）
- **改动**:
  - `AllInConfirmModal.tsx`: 新建组件，WSOP 风格，显示底池赔率和胜率统计
  - `ActionBar.tsx`: 添加 `onRequestAllIn` 回调，All-in 按钮点击时触发弹窗
  - `page.tsx`: 添加 `showAllInConfirm` / `allInConfirmAmount` 状态，渲染弹窗组件
  - `types.ts`: 添加 `consecutiveTimeouts?: number`
- **i18n**: `en.json` / `zh-CN.json` 添加 7 个翻译键（标题、描述、pot odds、equity、确认/取消按钮）
- **构建**: `tsc --noEmit` 通过，无错误

## 遗留问题

- 无

## 代码改动摘要

- 5 个后端文件修改（player.ts, table-player-ops.ts, table-game-logic.ts）
- 4 个前端文件修改（page.tsx, ActionBar.tsx, types.ts, AllInConfirmModal.tsx）
- 2 个 i18n 文件修改（en.json, zh-CN.json）
