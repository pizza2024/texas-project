# Coding Report — 第344轮

**时间:** 2026-04-30 01:30
**HEAD:** `c0083d5`
**分支:** develop

---

## 处理任务

### P2-TABLE-021 — `attemptStraddle` 重置顺序问题 ✅ 已修复

**文件:** `apps/backend/src/table-engine/table-round.ts`
**问题:** `calledAllIn = null` 在 PREFLOP stage 检查之前执行，导致即使 straddle 失败也会修改状态
**修复:** 将 `calledAllIn = null` 重置移至所有校验（stage、straddle、activePlayer）之后

```diff
  attemptStraddle(playerId: string): boolean {
+   if (this.table.currentStage !== GameStage.PREFLOP) return false;
+   if (this.table.straddle !== null) return false;
+
+   const activePlayer = this.table.players[this.table.activePlayerIndex];
+   if (!activePlayer || activePlayer.id !== playerId) return false;
+
    // Straddle resets calledAllIn to allow raises after straddle
    this.table.calledAllIn = null;
-   if (this.table.currentStage !== GameStage.PREFLOP) return false;
-   if (this.table.straddle !== null) return false;
-   const activePlayer = this.table.players[this.table.activePlayerIndex];
-   if (!activePlayer || activePlayer.id !== playerId) return false;
```

---

### P2-TABLE-022 — `leaveCurrentRoom` 两次 wallet 调用非原子 ✅ 已修复

**文件:** `apps/backend/src/table-engine/table-manager.service.ts`
**问题:** `setBalance` + `unfreezeBalance` 分开调用，中间 crash 导致 frozenChips 不同步
**修复:** 替换为原子 `resetBalanceAndUnfreeze`（$transaction 包装）

---

### P2-WS-015 — `hasOtherActiveSocketFn` 参数冗余 ✅ 已修复

**文件:** `apps/backend/src/websocket/app.gateway.ts`
**问题:** 闭包捕获 `userSockets` 和 `server`，却传 4 参数给只接受 2 参数的 callback
**修复:** 闭包简化为 `(uid) =>`，直接捕获 `this.connectionState.userSockets`、`this.server`、`client.id`

```diff
-      (uid, sid) =>
+      (uid) =>
         this.connectionState.hasOtherActiveSocket(
-          this.connectionState.userSockets,
-          this.server,
           uid,
-          sid,
+          client.id,
         ),
```

---

### P2-WS-016 — `handleEmojiReaction` DB 查询在房间校验前 ✅ 已修复

**文件:** `apps/backend/src/websocket/game.handler.ts`
**问题:** `getUserCurrentRoomId`（Redis + Map 操作）在 `checkEmojiRateLimit`（Redis 操作）之后，但在 DB 查询之前 —— DB 查询本应最后执行
**修复:** `getUserCurrentRoomId` → rate limit → DB 查询

---

### P2-TABLE-019 — `rakePercent` 未赋值 ✅ 已验证无需修复

**文件:** `table-round.ts:314`
**确认:** `this.table.rakePercent = tierConfig.rate` 已在 line 314 执行

### P2-TABLE-020 — `calledAllIn` 废弃代码 ✅ 已验证无需修复

**确认:** `table-player-ops.ts` lines 325-384 多次读取 `this.table.calledAllIn`，非废弃字段

---

## 代码改动

- 4 files changed, 12 insertions(+), 11 deletions(-)
- commit `c0083d5`: fix: Test r23 P2 fixes — straddle order, atomic wallet reset, WS cleanup

## 遗留问题

无新遗留。所有 TS 编译错误均为 pre-existing（node_modules/ethers/decorators/zod MDX types）

## 下轮关注

- P2-DEPOSIT-BONUS-TX / P2-DEPOSIT-FIRST-BONUS-RACE — 首次充值 bonus race condition（Test 建议升 P1）
- P2-NOTIFY-001 — 站内通知中心（规格已就绪）
- P2-VIP-001 / P2-RELOAD-001 — Productor 本轮发现新功能需求
