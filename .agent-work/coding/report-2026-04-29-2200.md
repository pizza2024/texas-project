# Coding Report — 2026-04-29 22:00

**Round:** 338  
**HEAD:** `7550859` (develop, up to date)  
**Working Directory:** `.agent-work/coding/`

---

## 本轮执行摘要

- **Test 反馈:** P0/P1 全部清零 ✅，4个 P2 低优先级遗留
- **Productor 反馈:** 无阻塞，P2 遗留项均无紧急度
- **本轮动作:** 清理 2 个低优先级 W-LOW 警告（lint ✅）

---

## 本轮处理任务

### ✅ W-LOW-003 — `ACTIVE_BETTING_STAGES` 每渲染重算

**问题:** `ACTIVE_BETTING_STAGES` 在组件函数体内定义，每次渲染重新创建数组。  
**位置:** `apps/web/app/room/[id]/page.tsx:89`  
**修复:** 移至模块层级（`const` at file scope），添加 `as const` 断言防止 mutation。  
**验证:** `pnpm lint --filter=web` ✅ 通过

### 🔍 W-LOW-001 — `getAuthorizedSocket()` 每调用重建（分析后不修）

**分析:** Test 报告称每次调用重建 socket。经查 `packages/shared/src/socket.ts:100`：

```ts
if (!socket || socketToken !== token) {
  if (socket) socket.disconnect();
  socket = io(serverUrl, { ... });
  socketToken = token;
}
```

`socket` 已在模块级按 `token` 缓存，重复调用相同 token 不会重建连接。`getAuthorizedSocket()` 每次调用虽然重新获取 token 和检查过期，但 socket.io 连接本身不复用。  
**决策:** 低优先级（无正确性风险），暂不修改。

---

## 遗留 P2 状态

| ID             | 任务                             | 状态        | 说明                             |
| -------------- | -------------------------------- | ----------- | -------------------------------- |
| P2-NEW-030     | 房间实时人数 UI 手动验证         | 🔍 待手动   | gameState 字段已实现，需人工测试 |
| P2-PROFILE-001 | 玩家资料页丰富化                 | 📋 待规格   | 头像框/成就徽章                  |
| P2-NOTIFY-001  | 站内通知中心                     | 📋 待开发   | 朋友上线/Club开赛提醒            |
| P2-WEB-PRIZE   | TournamentCard prize unused      | 🔍 待认领   | f071ae3 遗留                     |
| W-LOW-001      | Memoize getAuthorizedSocket      | 🔍 低优先级 | socket 已按 token 缓存，暂不修   |
| W-LOW-002      | Emoji seatIndex -1 fallback      | 🔍 低优先级 | 服务端未发送座位信息             |
| B-INFO-004     | Tournament prize Math.floor dust | 🔍 低优先级 | 未来考虑                         |

---

## 代码变更

| 文件                              | 变更                                                              |
| --------------------------------- | ----------------------------------------------------------------- |
| `apps/web/app/room/[id]/page.tsx` | `ACTIVE_BETTING_STAGES` 从组件内移至模块级 `const`，加 `as const` |

---

_下一轮继续监控 P0/P1，剩余 P2 均为低优先级人工验证或待 Productor 规格。_
