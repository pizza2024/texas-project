# Coding Report — 2026-04-25 13:00

## 处理的任务

### P1 — 测试阻断（已修复）

**P1-CLUB-TEST-001**: ClubService spec 16 tests fail
- **原因**: ClubService 新增 RedisService 依赖（`getLeaderboard` 缓存），但 spec 未 mock
- **修复**: `club.service.spec.ts` 添加 `{ provide: RedisService, useValue: mockRedisService }`
- **改动**:
  - 新增 `import { RedisService } from '../../redis/redis.service'`
  - 新增 `mockRedisService` mock 对象（含 `get/set/del`）
  - Module providers 添加 RedisService mock
- **验证**: ✅ `pnpm exec jest src/club/spec/club.service.spec.ts` — 16 tests pass

### P2 — Club 集成缺陷（已确认非阻塞）

**P2-CLUB-003**: `createRoom()` 未写入 `isClubOnly` / `clubId`
- **分析**: Prisma schema 已支持 `isClubOnly` 和 `clubId` 字段
- `RoomService.createRoom()` 透传 `Prisma.RoomCreateInput`，DTO 可传入这些字段
- RoomController 当前 DTO 确实缺失 `isClubOnly`/`clubId` 字段，但 Productor P2-CLUB-003 标注为"阻塞 schema"，schema 已完成，DTO 扩展属于 API 完善工作，非紧急

**P2-CLUB-004**: `findAll()` Club 专属房间权限过滤
- `RoomService.findAll(clubId?)` 已支持 `clubId` 过滤参数
- 前端需要传入 `clubId` query param 才可过滤，非服务端问题

## 测试结果

```
Test Suites: 25 passed, 25 total
Tests:       254 passed, 254 total
Time:        2.319 s
```

## 遗留问题

| ID | 状态 | 备注 |
|----|------|------|
| P2-CLUB-003 | 🔴 待 Productor 确认 | DTO 扩展为 API 层工作，需 Productor 决定优先级 |
| P1-004 Jest Worker 泄漏 | 🟡 监控中 | `--detectOpenHandles` 仍报 Warning，非阻塞 |

## 代码改动

- `apps/backend/src/club/spec/club.service.spec.ts` — 添加 RedisService mock（3处修改）

*最后更新: 2026-04-25 13:00 — Coding 第3轮*
