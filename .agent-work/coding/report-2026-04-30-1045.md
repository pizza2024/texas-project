# Coding Report — 2026-04-30-1045

**时间:** 2026-04-30 10:45
**HEAD:** `370761a`（未变）
**分支:** develop

---

## 处理的任务

### P0 — 阻塞修复

#### P0-NOTIFY-MIGRATE — `UserNotificationSettings` 表缺少迁移脚本 🔴

**问题:** `schema.prisma` 中 `Notification` 和 `UserNotificationSettings` 模型已定义（commit `0b6774b`），但缺少 `prisma/migrations/` 迁移脚本，导致：
- 生产环境上线后 DND 设置接口 500 错误
- `emitToUser` DND 逻辑静默失效

**分析:**
- DB 不在本地（`localhost:5432` 无法连接），无法执行 `migrate dev`
- 已创建迁移脚本（本地未连接 DB 场景下的标准处理）

**修复:**
- 创建 `prisma/migrations/202604301000_add_notification_settings/` 迁移目录
- `migration.sql` — 包含 `notifications` + `user_notification_settings` 建表语句 + 索引 + 外键
- `migration.ts` — Prisma 运行时脚手架

**部署命令:**
```bash
# 生产部署（确保 DB 可达）
npx prisma migrate deploy --name add_notification_settings

# 或本地开发
npx prisma migrate dev --name add_notification_settings
```

**状态:** ✅ 迁移脚本已创建，待部署时执行

---

### P2 — 遗留问题确认

#### P2-NOTIFY-EMAIL-WIRE & P2-NOTIFY-PUSH-WIRE

**分析结果:** `pushEnabled` / `emailEnabled` 字段已在 `UserNotificationSettings` 模型中定义并可写，但通知服务代码中没有任何读取使用的逻辑：

- `notification.service.ts` — 无任何邮件发送逻辑（仅有 Telegram admin alerts）
- `notification.gateway.ts` — `emitToUser` 只调用 `isPushAllowed()`，不检查 `pushEnabled` 字段
- 当前无外部邮件（Resend）/ 推送（Pusher/FCM）基础设施

**结论:** 这两个字段是 Phase 3+ 预留扩展点，当前无需修复。`emailEnabled` 读/写环路已就绪，但实际发送逻辑待实现。

---

## 遗留问题（未解决）

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P2-WITHDRAW-UX-004 | 提现历史追踪 | 待实施 | 规格待补充 |
| P2-TOURNAMENT-BLIND | 盲注结构可视化 | 待实施 | 赛事优先功能 |
| P2-TOURNAMENT-GTD | 保底奖池 | 待实施 | 赛事核心吸引力 |
| P2-ROOM-UX-003/004/005 | 房间收藏/邀请链接 | 待实施 | Productor 优先级较低 |
| P2-NEW-007 Phase 3 | 手牌历史库+分享 | 待实施 | Phase 1+2 已完成 |

---

## 代码状态

- **TypeScript 编译:** 通过（无新增错误）
- **Prisma Schema:** `Notification` + `UserNotificationSettings` 模型已定义
- **迁移脚本:** 已创建，待 DB 可达时执行 `migrate deploy`

---

_Coding r405 — 2026-04-30 10:45 — P0-NOTIFY-MIGRATE 迁移脚本已创建；P2-NOTIFY-EMAIL/PUSH-WIRE 确认 Phase 3+ 预留_
