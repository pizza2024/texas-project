# Coding Report — 第61轮

**时间**: 2026-04-24 19:35
**执行者**: Coding Agent

---

## 处理的 P0 任务

### P0-001: straddle 操作被 PlayerActionSchema 阻止 ✅ 已修复

**文件**: `packages/shared/src/validation.ts`

**改动**:
1. 在 `PlayerActionSchema.action` enum 中加入 `'straddle'`，恢复 straddle 操作功能
2. 在 `amount` 字段加入 `.max(1_000_000_000)` 上限验证（P2-001 一并修复）

```diff
  action: z.enum(['check', 'fold', 'call', 'raise', 'allin', 'straddle', 'sit-out']),
- amount: z.number().nonnegative().optional(),
+ amount: z.number().nonnegative().max(1_000_000_000).optional(),
```

**测试**: 215/215 通过 ✅

---

## 遗留任务

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P1-001 | 断线重连清理竞态 | 待修复 | connection-state.service.ts 空字符串 socketId |
| P1-002 | Jest Worker 泄漏 | 待调查 | --detectOpenHandles 定位 |
| P2-002 | Club 数据库迁移 | 待执行 | npx prisma migrate dev --name add_club_tables |
| P2-003 | Sit-Out 重构 | Pending | 等待产品确认 |
| P2-004 | All-in 确认弹窗 | Pending | 等待产品定义 |

---

## 下轮建议

- **P1-001**: `connection-state.service.ts` 的 `hasOtherActiveSocketFn(userId, '')` 在空字符串 socketId 时存在竞态，需捕获正确的 socketId 或重新查询活跃连接状态
- **P1-002**: 运行 `npm test -- --detectOpenHandles` 定位 worker 泄漏源头
- **P2-002**: Club 数据库迁移待执行，建议在 staging 环境先验证

---

*Coding 第61轮 — 2026-04-24 19:35 — P0 straddle 已修复，P1/P2 遗留任务继续监控*
