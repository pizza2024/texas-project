# Coding Report — 第88轮

> 时间戳: 2026-04-25 02:30
> Coding 代理第88轮

---

## 状态

| 状态    | 数量 |
| ------- | ---- |
| ✅ 完成 | 4    |
| 🔴 失败 | 0    |

---

## 已处理任务

### P0-NEW-001: `TooManyRequestsException` 编译失败 ✅

**问题**: `@nestjs/common` v11 未导出 `TooManyRequestsException`

**修复**:

- 文件: `apps/backend/src/auth/auth.service.ts`
- 移除 `TooManyRequestsException` 导入
- 将 `throw new TooManyRequestsException(...)` 替换为 `throw new BadRequestException(...)`

**改动**:

```diff
- import { TooManyRequestsException } from '@nestjs/common';
+ // (removed)

- throw new TooManyRequestsException('Too many failed attempts...');
+ throw new BadRequestException('Too many failed attempts...');
```

---

### P2-TS-001: `table-manager.service.spec.ts` 参数数量不匹配 ✅

**问题**: 构造函数需要5个参数，测试只传了4个（缺少 `rakebackService`）

**修复**: 添加 `{} as any` 作为 `rakebackService` 占位符

---

### P2-TS-002: `app.gateway.ts` action 类型 unknown 不兼容 ✅

**问题**: `handlePlayerAction` 的 `data.action` 类型是 `unknown`，不兼容 `'check' | 'fold' | ...`

**修复**: 将类型收紧为联合类型

```diff
- data: { action: unknown; amount?: unknown; roomId?: unknown },
+ data: { action: 'check' | 'fold' | 'call' | 'raise' | 'allin' | 'straddle' | 'sit-out'; amount?: unknown; roomId?: unknown },
```

---

### P2-TS-003: `timer-debug.spec.ts` 参数数量不匹配 ✅

**问题**: `processAction` mock 需要3个参数 `(userId, action, amount)`

**修复**: 更新 mock 签名

```diff
- processAction: jest.fn(() => {
+ processAction: jest.fn((_userId: string, _action: string, _amount: number) => {
```

---

## 测试验证

```
Test Suites: 24 passed, 24 total
Tests:       238 passed, 238 total
```

---

## 遗留问题

| ID           | 问题                              | 状态                                          |
| ------------ | --------------------------------- | --------------------------------------------- |
| P1-002       | Jest Worker 泄漏                  | 🟡 非阻塞 — 238 tests 通过，仍有 leak warning |
| P2-LINT-001  | game.handler.ts prettier 格式错误 | ✅ 已由上一轮修复                             |
| Pre-existing | 多个 `downlevelIteration` TS 错误 | 🔧 tsconfig 问题，非本轮引入                  |

> 注：剩余 TS 错误（如 `downlevelIteration`、`@types/mdx` JSX 命名空间）属于项目级 tsconfig 配置问题，不在 Test 报告的 P0/P1/P2 列表中。

---

## 任务队列更新

- P0-NEW-001: ✅ 已修复并标记完成
- P2-TS-001: ✅ 已修复
- P2-TS-002: ✅ 已修复
- P2-TS-003: ✅ 已修复

---

_Coding 第88轮完成_
