# Coding Agent Report — 2026-05-04 16:38

## 执行摘要
完成了 P0-INSURANCE-01 和 P0-INSURANCE-02 的修复，所有保险功能相关的 TypeScript 编译错误已清除。

## 处理的任务

### ✅ P0-INSURANCE-01 — `hasPurchasedInsurance` 缺失
- **文件**: `apps/backend/src/table-engine/table-player-ops.ts`
- **改动**: 在 player 映射对象中添加 `hasPurchasedInsurance: false,`
- **位置**: 约 line 41，`ready: player.ready ?? false, consecutiveTimeouts: 0,` 后
- **原因**: `InsuranceService` 引用了 `hasPurchasedInsurance` 字段，但 player 映射中缺少该字段

### ✅ P0-INSURANCE-02 — `InsuranceService` 未注入 `AppGateway`
- **文件**: `apps/backend/src/websocket/app.gateway.ts`
- **改动**:
  1. 添加 `import { InsuranceService } from '../insurance/insurance.service';`
  2. 构造函数添加参数 `readonly insuranceService: InsuranceService`（作为第 16 个参数）
  3. `insuranceService` 不能是 `private`，因为 `game.handler.ts` 外部访问它
- **原因**: `InsuranceService` 被添加到 `AppGateway` 的 DI 容器但构造函数缺少该依赖

### ✅ P0-INSURANCE-03 — `app.gateway.spec.ts` 缺少 `insuranceService` mock
- **文件**: `apps/backend/src/websocket/app.gateway.spec.ts`
- **改动**:
  1. 添加变量声明 `let insuranceService: any;`
  2. 在 `new AppGateway(...)` 调用中添加 `insuranceService as any,` 作为最后一个参数
- **原因**: `AppGateway` 构造函数现在需要 16 个参数，spec 中只有 15 个

## 验证
```bash
npx tsc --noEmit  # 退出码 0，无 TypeScript 编译错误
```

## 遗留问题
- 大量 pre-existing TypeScript 错误（tsconfig target、decorator 配置、moduleResolution 等），与保险功能无关
- 这些错误在引入保险功能之前已存在，需要独立的 tsconfig 修复任务

## 修改文件
1. `apps/backend/src/table-engine/table-player-ops.ts`
2. `apps/backend/src/websocket/app.gateway.ts`
3. `apps/backend/src/websocket/app.gateway.spec.ts`

## 分支 / Commit
- Branch: `develop`
- 状态: 全部修改已 staged，尚未 commit
