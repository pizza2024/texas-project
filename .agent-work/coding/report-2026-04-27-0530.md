# Coding Report — 第293轮

**时间:** 2026-04-27 05:30
**HEAD:** `c5da1b7`
**状态:** 1 P1 ✅

---

## 处理的任务

### ✅ P1-NEW-011 — `createWithdraw` 测试回归（余额不足测试失败）

**根因:**
- `P1-NEW-005` 修复将余额检查从 `walletService.getAvailableBalance()`（事务外）移入 `$transaction` 内部，通过 `tx.wallet.findUnique()` 读取
- 测试 `withdraw.service.spec.ts:220` 仍 mock `walletService.getAvailableBalance`，该函数已不再被 `createWithdraw` 调用
- 实际执行路径：`tx.wallet.findUnique()` → 返回默认 `mockWallet`（chips: 50000）→ 余额检查通过 → 测试失败

**修复:**
- `withdraw.service.spec.ts` — 重写 test mock 策略
- 直接 mock `prisma.$transaction` 的回调函数，注入低余额 wallet 对象
- 修复后测试通过

**代码变更:**
```typescript
// 修复前（mock walletService — 已过时）
walletService.getAvailableBalance.mockResolvedValue(100);

// 修复后（mock $transaction 内部 tx.wallet.findUnique）
const lowBalanceWallet = { chips: 100, frozenChips: 0, userId: mockUserId };
prisma.$transaction.mockImplementation(async (fn) => {
  const tx = {
    wallet: { findUnique: jest.fn().mockResolvedValue(lowBalanceWallet) },
    user: { update: jest.fn() },
    transaction: { create: jest.fn() },
    withdrawRequest: { create: jest.fn() },
    adminLog: { create: jest.fn() },
  };
  return fn(tx);
});
```

**Commit:** `c5da1b7`

---

## 遗留问题

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P1-NEW-010 | `checkStaleProcessing` 无幂等保护 | 🟡 待认领 | withdraw.service.ts:744-773 |
| P1-NEW-003 | `setBalances` Phase 2 非原子 | 🟡 待认领 | wallet.service.ts:64-136 |
| P1-NEW-004 | `exchangeChipsToBalance` TOCTOU | 🟡 待认领 | wallet.service.ts:275 |

---

*Coding 第293轮 — 2026-04-27 05:30*
