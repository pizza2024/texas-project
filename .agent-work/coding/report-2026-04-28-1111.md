# Coding 报告 — 第311轮

**时间:** 2026-04-28 11:11
**HEAD:** `f959e47` — 0 P0 / 0 P1 / ~19 P2
**测试:** 427 passed | 0 failed

---

## 本轮工作：P1-BLAST-001 Phase 1 完成

### 完成情况

P1-BLAST-001 Blast 即时赛事 Phase 1 **全部完成**（类型 + API + 服务方法 + 测试）。

### 提交记录

| Commit    | 内容                                                                                |
| --------- | ----------------------------------------------------------------------------------- |
| `2e58e2a` | feat(tournament): Blast Phase 1 — lobby API + service methods + 16 tests            |
| `9a46c46` | feat(redis): add list/hash convenience methods (lpush, lrange, lrem, hset, hgetall) |
| `f959e47` | style: lint fixes — prettier formatting                                             |

### 改动详情

#### 1. `packages/shared/src/types/tournament.ts` — BlastLobby 类型 + Redis key 常量

```typescript
export interface BlastLobby {
  id: string; // UUID matching Room.id
  buyin: number; // Buy-in amount in chips
  playerIds: string[]; // Max 3 players
  maxPlayers: 3;
  status: "waiting" | "starting" | "active";
  createdAt: number;
  creatorId: string;
  smallBlind: number;
  bigBlind: number;
}

export const BLAST_LOBBY_QUEUE_KEY = "blast:lobby:queue";
export const BLAST_LOBBY_KEY_PREFIX = "blast:lobby:";
```

#### 2. `apps/backend/src/tournament/tournament.controller.ts` — 4 个新 API 端点

```
POST /rooms/blast         — 创建 BLAST 大厅（自动加入为第一个玩家）
GET  /rooms/blast/lobbies — 列出所有等待中的大厅
POST /rooms/blast/:id/join — 加入大厅（满3人自动开始）
GET  /rooms/blast/:id     — 获取大厅详情
```

#### 3. `apps/backend/src/tournament/tournament.service.ts` — 4 个新服务方法

- `createBlastLobby(buyin, creatorId)` — 创建大厅，存入 Redis
- `getBlastLobbies()` — 列出所有 waiting 状态大厅
- `getBlastLobby(id)` — 获取单个大厅
- `joinBlastLobby(lobbyId, playerId)` — 加入大厅，满3人转为 `starting`
- `getBlastSmallBlind(buyin)` — 根据 buyin 档位推算小盲注

#### 4. `apps/backend/src/redis/redis.service.ts` — 5 个新 Redis 便捷方法

```typescript
async lpush(key: string, ...values: string[]): Promise<void>
async lrange(key: string): Promise<string[]>
async lrem(key: string, value: string): Promise<void>
async hset(key: string, field: string, value: string): Promise<void>
async hgetall(key: string): Promise<Record<string, string> | null>
```

#### 5. `tournament.service.spec.ts` — 16 个新单元测试

```
createBlastLobby (4 tests)
  ✓ creates lobby with correct structure
  ✓ applies correct small blind for 500 buyin
  ✓ applies correct small blind for 10000 buyin
  ✓ falls back to 1/100 ratio for unknown buyin

getBlastLobbies (3 tests)
  ✓ returns empty array when Redis unavailable
  ✓ returns waiting lobbies from Redis
  ✓ skips non-waiting lobbies

getBlastLobby (3 tests)
  ✓ returns null when Redis unavailable
  ✓ returns null when lobby not found
  ✓ returns parsed lobby data

joinBlastLobby (6 tests)
  ✓ returns null when Redis unavailable
  ✓ returns null when lobby not found
  ✓ adds player and returns updated lobby
  ✓ transitions to starting when 3rd player joins
  ✓ rejects join when player already in lobby
  ✓ rejects join when lobby is full
```

### 测试结果

```
Test Suites: 31 passed, 31 total
Tests:       427 passed, 427 total (was 411, +16 Blast tests)
Time:        3.129 s
```

---

## 遗留 P2 问题

| ID          | 任务                               | 严重度 | 状态   |
| ----------- | ---------------------------------- | ------ | ------ |
| P2-TEST-001 | `exchangeBalanceToChips` TOCTOU    | Medium | 待认领 |
| P2-TEST-002 | ELO 边界分离事务                   | Medium | 待认领 |
| P2-TEST-005 | TableState/Player 类型漂移         | Medium | 待认领 |
| P2-TEST-006 | 操作按钮防重放                     | Medium | 待认领 |
| P2-TEST-008 | 无 JWT refresh token               | Medium | 待认领 |
| P2-NEW-014  | `broadcastTableState` 异步广播顺序 | Medium | 待认领 |
| P2-NEW-016  | `unfreezeBalance` 事务外 TOCTOU    | Medium | 待认领 |
| P2-NEW-017  | adminLog 事务外                    | Medium | 待认领 |
| P2-NEW-018  | BullMQ 无幂等键                    | Medium | 待认领 |

---

## 下一轮建议

1. **P1-BLAST-001 Phase 2** — 轮盘 UI + 动画（前端）
2. **P2-TEST-005** — TableState/Player 类型统一（web 从 `@texas/shared` 导入）
3. **P2-TEST-006** — handleAction 按钮防重放

---

_P1-BLAST-001 Phase 1 完成（427 tests，0 P0 / 0 P1）_
