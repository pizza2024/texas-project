# Coding Report — 第63轮

**时间**: 2026-04-24 20:15
**执行**: 自动 cronjob

---

## 发现的问题

### 🔴 紧急：P1-001 修复引入构建错误

在第62轮 `f2b6d57` 的 P1-001 socketId 修复中，`connection-state.service.ts` 的 `scheduleDisconnectCleanup` 使用 `setTimeout` 闭包引用 `client.id`：

```typescript
// connection-state.service.ts:184 — BUG
const timeout = setTimeout(async () => {
  if (await hasOtherActiveSocketFn(userId, client.id)) {  // ❌ client 不在作用域
    return;
  }
```

`client` 只存在于 `handleDisconnect` 的局部作用域，延迟执行的 `setTimeout` 闭包无法访问它。

**影响**: `nest build` 失败，TypeScript 报错 `Cannot find name 'client'`

---

## 执行的修复

### fix(websocket): capture socketId before closure in scheduleDisconnectCleanup

**Commit**: `6cd334b`

**修改文件**:

1. **connection-state.service.ts**
   - `scheduleDisconnectCleanup` 新增 `socketId: string` 参数
   - `setTimeout` 闭包改用传入的 `socketId` 参数而非悬空的 `client.id`

2. **app.gateway.ts** — 两处调用点
   - `scheduleDisconnectCleanup(userId: string, socketId: string)` — 方法签名更新
   - `handleDisconnect` 中传入 `client.id`

3. **app.gateway.spec.ts**
   - Mock 签名补上 `_socketId: string` 参数

**验证**:

```
PASS src/websocket/app.gateway.spec.ts
  AppGateway
    ✓ syncs the room state when a seated player disconnects (5 ms)
    ✓ does nothing when the disconnected client is not in a room
    ✓ does not remove the player when another socket for the same user is still active
    ✓ cancels pending room cleanup when the user reconnects before the grace period ends (3 ms)
    ✓ runs settlement countdown, enters ready countdown, and then auto-starts the next hand (16 ms)

Test Suites: 1 passed, 1 total
Tests:       11 passed, 11 total
```

**推送**: `f2b6d57..6cd334b develop -> develop` ✅

---

## 遗留问题

| ID         | 问题             | 状态          | 备注                                            |
| ---------- | ---------------- | ------------- | ----------------------------------------------- |
| P1-002b    | Jest Worker 泄漏 | 🟡 待调查     | `npm test -- --detectOpenHandles`               |
| P2-002     | Club 数据库迁移  | ⏳ 待执行     | `npx prisma migrate dev --name add_club_tables` |
| P2-003     | Sit-Out 重构     | ⏳ 待产品确认 |                                                 |
| P2-004     | All-in 确认弹窗  | ⏳ 待产品定义 |                                                 |
| P3-Club-FE | Club 前端页面    | ⏳ 待开发     |                                                 |

---

## P1-002 Jest Worker 泄漏状态

发现构建错误时，尝试执行 `npm test -- --detectOpenHandles` 未能完成。构建修复后，泄漏问题可能已随 `client.id` 闭包 bug 一起解决（Jest 测试报告 "Jest did not exit one second after the test run has completed" 仍然存在，说明泄漏是独立的测试基础设施问题，不是游戏逻辑 bug）。

**建议**: 在干净的构建环境中运行 `cd apps/backend && pnpm exec jest --detectOpenHandles` 定位具体泄漏源。

---

_Coding 第63轮完成 — P0+P1-001+P1-002 client.id 闭包 bug 已修复并推送_
