# Coding Report — 第332轮

**时间:** 2026-04-29 18:45 | **HEAD:** `b827c8b`

---

## 处理的 P2-NEW-030-STUB（P2）

### 问题描述

Test 报告 P2-NEW-030-STUB：`gameState` prop 未接入，`RoomCard` 组件虽定义了 `gameState?: 'waiting' | 'playing' | null` 并有颜色编码逻辑（进行中=绿色/缺人=红色/等待中=黄色），但 `rooms/page.tsx` 未传该 prop，状态徽章始终回退到默认的"等待开始"。

### 修复内容（4 文件）

**1. Backend — `room-events.ts`**

- `RoomStatusUpdatedPayload` 新增可选字段 `gameState?: 'waiting' | 'playing'`

**2. Backend — `table-manager.service.ts`**

- `getRoomStatus()` 新增 `gameState` 计算逻辑：
  - 当 `table` 存在且 `table.currentStage !== GameStage.WAITING` → `'playing'`（手牌进行中）
  - 否则 → `'waiting'`（桌面空闲）

**3. Frontend — `rooms/page.tsx`**

- `RoomStatus` 接口新增 `gameState?: 'waiting' | 'playing'`
- `RoomCard` 调用处新增 `gameState={roomStatusMap[room.id]?.gameState}`

**4. Spec — `table-manager.service.spec.ts`**

- 两个 `broadcastRoomStatus` 测试用例的 expected payload 新增 `gameState: 'waiting'`

### 验证

- 452 tests passed ✅

### 遗留问题

无

---

## 提交记录

- `b827c8b` fix(P2): P2-NEW-030 — add gameState to room_status_updated WS event

---

_Coding 第332轮 — 2026-04-29 18:45_
