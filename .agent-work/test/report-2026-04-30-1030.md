# Test Report — 2026-04-30-1030

**时间:** 2026-04-30 10:30
**HEAD:** `370761a`
**分支:** develop
**状态:** P0/P1 均清零

---

## 本轮 CodeReview 发现

### P0 — 阻塞（必须修复后再上线）

| ID | 问题 | 位置 | 优先级 |
|---|---|---|---|
| P0-NOTIFY-MIGRATE | `UserNotificationSettings` 模型存在但无数据库迁移脚本 | `schema.prisma:428-439` | **P0** |

**详情:**
- `UserNotificationSettings` 表在 `schema.prisma` 中已定义（commit `0b6774b`），但未生成迁移脚本
- `prisma/migrations/` 目录最新迁移为 `202604300500_add_deposit_address_labels`，无 `UserNotificationSettings` 相关迁移
- 上线前必须执行 `prisma migrate deploy`，否则：
  - DND 设置无法持久化（500 错误）
  - `isPushAllowed()` 查询失败 → 所有通知静默丢弃
  - `emitToUser()` DND 逻辑实际不生效（DB 查询报错被上层的 `try/catch` 吞掉）

**建议:** 立即生成迁移脚本 `npx prisma migrate dev --name add_user_notification_settings`

---

### P2 — 规格未完成（不影响当前功能，但与 Phase 3 承诺不符）

| ID | 问题 | 位置 | 优先级 |
|---|---|---|---|
| P2-NOTIFY-EMAIL-WIRE | `emailEnabled` 字段存储后从未被读取使用 | `notification.service.ts` | P2 |
| P2-NOTIFY-PUSH-WIRE | `pushEnabled` 字段存储后从未被读取使用 | `notification.service.ts` | P2 |

**详情:**
- `UpdateSettingsDto` 包含 `pushEnabled` 和 `emailEnabled` 字段
- 但 `NotificationService` 只实现了 `isPushAllowed()` — 该方法**仅**检查 `doNotDisturb` + 时间窗口，**完全忽略** `pushEnabled` 字段
- `emailEnabled` 在任何地方都没有被消费
- Phase 3 需求文档明确说明 "免打扰设置 (竞品均有)"，但目前只实现了 DND 时间窗口，`pushEnabled`/`emailEnabled` 是空壳

**建议:** 后续 sprint 补全 `pushEnabled` 逻辑，或在 UI 上隐藏这两个开关直到实现

---

### P2 — 其他观察（轻微）

| ID | 问题 | 位置 | 备注 |
|---|---|---|---|
| P2-NOTIFY-DND-EDGE | 跨天 DND 窗口（如 22:00-06:00）边界计算正确，但当 `dndStart === dndEnd` 时行为未定义 | `notification.service.ts:212-235` | 理论上不会发生（UI 使用 time input），但缺少校验 |
| P2-NOTIFY-TEST | `NotificationService.isPushAllowed()` 无单元测试覆盖 | `notification.service.ts` | `notification.service.spec.ts` 未更新 |

---

## 本轮测试执行

### 单元测试（backend）
```
Test Suites: 32 passed, 32 total
Tests:       451 passed, 452 total (1 skipped)
```
✅ 所有测试通过（`rakeback.e2e-spec.ts` mock 已修复）

### 代码差异分析
本轮只有 1 个新 commit `370761a`（文档更新），无实际代码变更：
- `P2-NOTIFY-001 Phase 3` 实际 commit 为 `0b6774b`
- `bbf2d67`: P2-NOTIFY-WS-BRIDGE（Redis pub/sub 多实例广播）
- `3386592`: P2-NOTIFY-001 Phase 2（Bell Icon + 未读数红点）

---

## 遗留 P2 跟踪

| ID | 任务 | 状态 | 备注 |
|---|---|---|---|
| P2-WITHDRAW-UX-004 | 提现历史追踪（规格待补充） | 待实施 | Productor 待补充 |
| P2-TOURNAMENT-BLIND | 盲注结构可视化 | 待实施 | 赛事 UX |
| P2-TOURNAMENT-GTD | 保底奖池 | 待实施 | 赛事核心 |
| P2-ROOM-UX-003 | /rooms 空状态 CTA | 待实施 | GGPoker "No games — Create one" |
| P2-ROOM-UX-004 | 收藏常玩房间 | 待实施 | 888poker ⭐ 收藏 |
| P2-ROOM-UX-005 | 私人房间邀请链接/二维码 | 待实施 | WSOP 邀请码分享 |
| P2-NOTIFY-001 Phase 3 | DND 设置（迁移缺失） | 🔴 阻塞 | 需 `prisma migrate deploy` |
| P2-NOTIFY-EMAIL-WIRE | `emailEnabled` 未连线 | P2 | 空壳字段 |
| P2-NOTIFY-PUSH-WIRE | `pushEnabled` 未连线 | P2 | 空壳字段 |

---

## 优先级任务队列

```
🔴 立即（上线前必须）：
P0-NOTIFY-MIGRATE    — 生成并执行 UserNotificationSettings 迁移脚本

🟡 本周：
P2-NOTIFY-EMAIL-WIRE — 补全 emailEnabled 逻辑或隐藏 UI 开关
P2-NOTIFY-PUSH-WIRE  — 补全 pushEnabled 逻辑或隐藏 UI 开关

📋 待规格：
P2-WITHDRAW-UX-004   — 提现历史追踪（Productor 待补充规格）
P2-TOURNAMENT-BLIND  — 盲注结构可视化
P2-TOURNAMENT-GTD    — 保底奖池
P2-ROOM-UX-003/004/005 — 房间 UX 改进
```

---

## 结论

**P0 阻塞 1 个**: `UserNotificationSettings` 表无迁移脚本。上线会导致 DND 设置完全失效。

**测试状态:** 451/452 测试通过，1 skipped，无 regression。

**跨代理状态**（来自 Productor）:
- P0/P1 均清零 ✅
- 赛事留存机制调研完成
- Phase 3 前优先任务：盲注可视化、保底奖池、房间 UX

---
_Test r56 — 2026-04-30 10:30 — P0/P1 清零；P0-NOTIFY-MIGRATE 阻塞_
