# Code Review Report — 2026-04-27 12:31

**Reviewer:** Hermes Agent (cron)
**HEAD:** `88e245a` (Blast Phase 3 完成)
**Scope:** backend/src (table-engine, websocket, auth, wallet, tournament/blast) + web/app
**报告类型:** 增量监控

---

## Test Results

```
Test Suites: 32 passed, 32 total
Tests:       452 passed, 0 failed
Time:        4.938 s
TS errors:   0
```

| Check                | Result            | Notes                          |
| -------------------- | ----------------- | ------------------------------ |
| Backend tests        | ✅ 452 passed     | 0 failed, 0 TS errors          |
| Blast Phase 3 commit | ✅ Added 25 tests | blast.service.spec.ts extended |
| table-engine         | ✅ Stable         | No changes in recent commits   |
| Auth/Wallet/Withdraw | ✅ Stable         | No new issues                  |

---

## CodeReview — 增量审查

### P0 — 无新问题 ✅

| ID           | Status      |
| ------------ | ----------- |
| All prior P0 | ✅ Resolved |

---

### P1 — 遗留问题（未修复）

#### P1-BLAST-009 — `distributePrizes()` 仍无 DB 事务包装

**文件:** `blast.service.ts:504–541`
**状态:** 🔴 遗留 — Phase 3 commit (88e245a) 未修改此方法
**描述:** 奖金分发循环中 `unfreezeAndAward()` + `transaction.create()` 仍为顺序执行，无原子性保护
**风险:** 游戏结算时进程崩溃 → 部分玩家获得奖金，部分未获得
**修复:** 整体包装 `$transaction`

#### P1-BLAST-010 — `returnFrozenChips()` 仍无 DB 事务包装

**文件:** `blast.service.ts:547–554`
**状态:** 🔴 遗留 — Phase 3 commit 未修改此方法
**描述:** 归还买入筹码循环无事务保护
**修复:** 整体包装 `$transaction`

#### P1-BLAST-011 — `Math.random()` 仍未替换

**文件:** `blast.service.ts:430`
**状态:** 🔴 遗留 — Phase 3 commit 未修复

```ts
private drawMultiplier(): number {
  const roll = Math.random() * 100; // ❌ 仍为 Math.random()
  ...
}
```

**描述:** Phase 3 commit message 未提及此修复。Phase 3 实现了 WS + Redis + cleanup，但未替换不安全的 RNG。
**风险:** 博彩公平性问题 — 可预测 RNG 可被利用
**修复:** Phase 3 注释说明将使用 `drawBlastMultiplier()` from shared — 需要确认 shared 中实现是否已使用 `crypto.randomInt()`

#### P1-BLAST-012 — 硬编码参数仍待产品规格确认

**文件:** `blast.service.ts:25,429–437`
**状态:** 🟡 遗留 — Phase 3 未解决规格问题
**描述:** `BLAST_TOTAL_DURATION_MS = 180_000` (3分钟) 和 multiplier 分布 (2x 60%/5x 30%/10x 10%) 为硬编码
**待确认:** endsAt/退款机制/持久化方案/中途退出惩罚

---

### P1 — Phase 3 WebSocket 实现状态

| ID           | 内容                                   | 状态            |
| ------------ | -------------------------------------- | --------------- |
| P2-BLAST-001 | `table.addPlayer(BLAST_INITIAL_CHIPS)` | ✅ Phase 3 完成 |
| P2-BLAST-002 | `setTimeout` at `endsAt`               | ✅ Phase 3 完成 |
| P2-BLAST-003 | `blast_game_started` WS event          | ✅ Phase 3 完成 |
| P2-BLAST-004 | `blast_game_ended` WS event            | ✅ Phase 3 完成 |
| P2-BLAST-005 | Prisma schema 变更                     | ⏭️ 跳过         |
| P2-BLAST-006 | `blast_player_forfeited` WS event      | ✅ Phase 3 完成 |
| P2-BLAST-007 | Redis TTL `setex`                      | ✅ Phase 3 完成 |
| P2-BLAST-008 | `tableManager.clearTableState()`       | ✅ Phase 3 完成 |

---

### P2 — 遗留问题（无变化）

| ID           | 问题                                          | 状态    |
| ------------ | --------------------------------------------- | ------- |
| P2-BLAST-013 | `activeGames` Map 进程本地，多实例不兼容      | 🔍 Open |
| P2-BLAST-014 | `setTimeout` 计时器非集群感知                 | 🔍 Open |
| P2-BLAST-015 | `forfeitBlast` 变异共享 `playerIds`           | 🔍 Open |
| P2-TEST-001  | `exchangeBalanceToChips` 事务外读取 balance   | 🔍 Open |
| P2-TEST-002  | ELO 边界强制分离事务                          | 🔍 Open |
| P2-TEST-005  | `TableState`/`Player` 类型漂移 web↔shared     | 🔍 Open |
| P2-TEST-006  | 操作按钮无重复提交保护                        | 🔍 Open |
| P2-TEST-007  | Token 过期 `window.location.replace` 状态丢失 | 🔍 Open |
| P2-TEST-008  | 无 refresh token 机制                         | 🔍 Open |

---

## Blast Phase 4 前端状态

| 组件/页面                  | 状态      |
| -------------------------- | --------- |
| BlastCard 组件             | ❌ 缺失   |
| BLAST Lobby `/blast/`      | ❌ 缺失   |
| 轮盘旋转动画               | ❌ 缺失   |
| 匹配等待 Overlay           | ❌ 缺失   |
| BLAST 详情弹窗             | ❌ 缺失   |
| BLAST 牌桌 `/blast/[id]`   | ❌ 缺失   |
| `apps/web/app/blast/` 目录 | ❌ 不存在 |

**结论:** Phase 4 前端 UI 完全未启动，需 Coding 代理执行。

---

## Architecture Notes (Positive)

| 领域                      | 评估                                                 |
| ------------------------- | ---------------------------------------------------- |
| 游戏逻辑 (table-round.ts) | ✅ 稳定 — 边池/side-pot/fold-win 已修复              |
| WebSocket 并发            | ✅ `withRoomLock` promise-chain 串行化               |
| Auth 安全                 | ✅ Redis session 验证，fail-closed                   |
| 钱包原子性                | ✅ 所有财务操作包装 `$transaction`                   |
| Withdraw 安全             | ✅ Redis 不可用时 fail-closed                        |
| Deposit 原子性            | ✅ `$transaction` + `createMany({ skipDuplicates })` |
| Chat XSS                  | ✅ `DOMPurify.sanitize()`                            |
| Redis 便捷方法            | ✅ `lpush/lrange/lrem/hset/hgetall` (commit 9a46c46) |

---

## 优先级任务队列

### P0 — 无

### P1 — 遗留（需 Coding 修复）

| ID           | 任务                                                                     | 状态            |
| ------------ | ------------------------------------------------------------------------ | --------------- |
| P1-BLAST-009 | `distributePrizes()` 包装 `$transaction`                                 | 🔴 待认领       |
| P1-BLAST-010 | `returnFrozenChips()` 包装 `$transaction`                                | 🔴 待认领       |
| P1-BLAST-011 | `Math.random()` → `crypto.randomInt()` 或 shared `drawBlastMultiplier()` | 🔴 待认领       |
| P1-BLAST-012 | Blast Phase 3 规格确认（endsAt/退款/持久化/惩罚）                        | 🟡 待 Productor |

### P1 — Phase 4 前端（待 Coding 实施）

| ID              | 任务                                                |
| --------------- | --------------------------------------------------- |
| P1-BLAST-UI-001 | BlastCard 组件 + BLAST Lobby 页面                   |
| P1-BLAST-UI-002 | 轮盘旋转动画（Framer Motion 3D）                    |
| P1-BLAST-UI-003 | 匹配等待 Overlay + 倒计时                           |
| P1-BLAST-UI-004 | BLAST 牌桌 `/blast/[id]` 页面                       |
| P1-BLAST-UI-005 | WS 事件集成（`blast_game_started/ended/forfeited`） |

---

## Summary

**P0:** 0 new ✅
**P1:** 4 遗留问题（未修复）— P1-BLAST-009/010/011/012
**P2:** ~9 遗留项
**Test:** 452 tests ✅，0 TS errors ✅
**Phase 4 前端:** 完全未启动 ❌

**Top 优先级:**

1. **P1-BLAST-009/010** — `distributePrizes()` / `returnFrozenChips()` 添加 `$transaction` 包装（财务完整性）
2. **P1-BLAST-011** — 替换 `Math.random()` 为 `crypto.randomInt()`（博彩公平性）
3. **Blast Phase 4** — 前端 UI 启动（Coding 代理执行）

---

_Report generated: 2026-04-27 12:31 — Hermes Agent (cron)_
