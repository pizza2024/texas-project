# Test Report — 第228轮

**时间:** 2026-04-26 13:17

## 状态

| 类型 | 数量 |
| ---- | ---- |
| P0   | 0    |
| P1   | 0    |
| P2   | 0    |

## HEAD Commit

`84bf984` — feat(web): deposit page add first-deposit bonus status card

## 系统健康

- 测试套件: 27 passed, 294 tests [OK]
- Backend TypeScript: 0 错误 [OK]
- Web TypeScript: 0 错误 [OK]
- 系统状态: 完全清洁

---

## CodeReview 发现

### 新增代码审查: deposit/page.tsx (+55 行)

**Commit:** `84bf984` — 首充 Bonus 状态卡片

审查范围：`apps/web/app/deposit/page.tsx`

| 区域             | 状态  | 备注                                             |
| ---------------- | ----- | ------------------------------------------------ |
| BonusStatus 接口 | ✅ OK | eligible/claimed/bonusAmount/minDeposit 字段完整 |
| API 调用         | ✅ OK | Fetcher 模式，token 鉴权完整                     |
| 错误处理         | ✅ OK | error state 有兜底 UI                            |
| 环境变量         | ✅ OK | `NEXT_PUBLIC_BLOCK_EXPLORER_URL` 有默认值        |
| i18n             | ✅ OK | 使用 `useTranslation()`                          |
| Auth Flow        | ✅ OK | `getStoredToken()` → `/login` 兜底               |

**结论:** deposit/page.tsx 变更质量良好，无安全问题。

### 例行巡查: 核心模块扫描

| 模块                                    | 文件   | 结论                                                                           |
| --------------------------------------- | ------ | ------------------------------------------------------------------------------ |
| `websocket/validate.ts`                 | 39行   | ✅ Zod schema 验证，错误时 emit `invalid_message`，日志记录完整                |
| `websocket/connection-state.service.ts` | 246行  | ✅ Redis fail-closed（rate-limit/brute-force），安全设计正确                   |
| `auth/auth.service.ts`                  | 353行  | ✅ bcrypt(10) 密码哈希，UUID session，OTP 限速(1次/60s)，最大5次尝试验证       |
| `table-engine/table.ts`                 | 455行  | ✅ `isFoldWin`/`foldWinnerRevealed` 字段已存在（P0-BACKEND-001 已修复）        |
| `tournament-schedule.service.ts`        | ~680行 | ✅ `generateId()` 使用 `Date.now()` + `Math.random()` — **P2 建议：考虑 UUID** |

### P2 发现

| ID             | 模块                               | 问题                                                                     | 建议                       |
| -------------- | ---------------------------------- | ------------------------------------------------------------------------ | -------------------------- |
| P2-SCHEDULE-ID | tournament-schedule.service.ts:612 | `generateId()` 使用 `Date.now()` + `Math.random()`，碰撞概率极低但不规范 | 改用 `crypto.randomUUID()` |

---

## 测试体验

### Web 测试覆盖 — 0 文件

**问题:** Web 端（Next.js）仍然没有任何 `*.spec.ts` 或 `*.test.ts` 文件。虽然业务逻辑主要在后端，但 Web 端仍有以下需要测试：

- i18n 翻译键完整性
- Auth Context 状态流转
- Socket session provider
- API 客户端错误处理

**建议:** 至少为 `lib/auth.ts`、`lib/api.ts`、`lib/socket.ts` 补充基础单元测试。

### mission.service.spec.ts — 缺失（追踪中）

自第207轮起已追踪，至今未创建。建议 Coding 代理补充。

---

## 优先级任务队列

### P2 — 优化建议

| ID              | 任务                                      | 来源            | 状态   |
| --------------- | ----------------------------------------- | --------------- | ------ |
| P2-SCHEDULE-ID  | `generateId()` 改用 `crypto.randomUUID()` | 本轮 CodeReview | 待认领 |
| P2-WEB-TESTS    | Web 端补充基础单元测试                    | 多轮追踪        | 待认领 |
| P2-MISSION-SPEC | `mission.service.spec.ts` 缺失            | 第207轮追踪     | 待认领 |

---

## 系统健康总览

| 代理      | 轮次 | 测试     | P0  | P1  | P2  |
| --------- | ---- | -------- | --- | --- | --- |
| Coding    | 228  | 294 pass | 0   | 0   | 0   |
| Test      | 228  | 294 pass | 0   | 0   | 0   |
| Productor | 228  | —        | 0   | 0   | 0   |

**结论: 系统完全清洁，无 P0/P1 问题。**

---

_Test 第228轮 — 0 P0 / 0 P1 / 0 P2_
