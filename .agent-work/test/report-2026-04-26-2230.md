# Test Report — 第168轮

> 时间: 2026-04-26 22:30

## 状态

| 类型 | 数量 |
|------|------|
| P0 | 0 🟢 |
| P1 | 0 🟢 |
| P2 | 1 🟡 |

## HEAD Commit

`7869f51` — chore: update shared docs — P2-CHAT-001 前端验证完成

## 测试状态

- **295 tests pass** — ✅ 全部通过（27 suites，+21 新测试）
- **Backend build** — ✅ 干净（0 errors）
- **Web lint** — ✅ 无错误

## 本轮 CodeReview

### 1. Tournament 模块 — scaffold 就绪，WS 集成缺失

**文件（Working Directory，未 commit）**：
- `apps/backend/src/tournament/tournament.service.ts` ✅
- `apps/backend/src/tournament/tournament.service.spec.ts` ✅（295 tests 含此模块）
- `apps/backend/src/tournament/tournament.controller.ts` ✅
- `apps/backend/src/tournament/tournament.module.ts` ✅
- `packages/shared/src/types/tournament.ts` ✅

**已就绪**：
- `TournamentService` — 盲注计时器、排名计算、奖金分发
- `TournamentController` — `GET /rooms/:id/prizes`
- `TournamentModule` — 已正确 import 到 `app.module.ts`
- 类型定义完整（`SngConfig`、`BlindLevel`、`createDefaultBlindSchedule`）
- `295 tests pass`（含 tournament.service.spec.ts 的 21 个新测试用例）

**缺失（P2-TOURNAMENT-WS-001）**：
- `game.handler.ts` 导入了 `SNG_START_COUNTDOWN_MS` 但未使用
- **无任何 `@SubscribeMessage` 绑定 tournament WS 事件**
- `TournamentService` 未被任何 WS handler 注入/调用
- 8人满员触发 SNG 开始 → 盲注计时器的调用链完全未实现

**风险评估**：P2 — Tournament scaffold 本身完整，但 WS 集成断链，Coding 需完成 Phase 1 剩余工作。

### 2. ChatPanel — fire-and-forget 已验证

**变更**：`ChatPanel.tsx` 移除 ack callback，改为 fire-and-forget `socket.emit`
- 后端 `handleChatMessage` 完整（身份校验 + 房间校验 + 200字符限制 + 1msg/5s rate limit）
- 前端 listener 已注册（`socket.on('chat-message')`）
- **风险**：低 — 发送失败前端无视觉反馈，但不影响游戏核心流程

### 3. 竞态条件检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `handleChatMessage` 房间校验 | ✅ | `getUserCurrentRoomId` vs `roomId` |
| `client.leave()` vs DB 更新顺序 | ✅ | 已修复（上一轮） |
| TournamentService `blindTimers` Map 竞态 | ✅ | 单进程 `setTimeout`，无并发问题 |
| Redis `zadd`/`zrem` 盲注计时器 | ⚠️ | Redis 不可用时静默失败（warn only） |

### 4. 安全检查

| 检查项 | 状态 |
|--------|------|
| SQL injection | ✅ 无 — Prisma parameterized |
| XSS/eval | ✅ 无 |
| Auth JWT | ✅ bcrypt + JWT + Redis fail-closed |
| Brute force | ✅ Redis IP banning |
| Chat rate limit | ✅ 1 msg/5s via Redis |
| WS rate limit | ✅ 已验证 |

## 测试体验

- **295 tests** — TournamentService 21 个新测试用例通过
- **P2-TOURNAMENT-WS-001** — WS 集成层未完成，阻断 SNG 完整流程
- **房间聊天** — P2-CHAT-001 端到端验证建议进行

## P2 问题

| ID | 问题 | 状态 | 备注 |
|----|------|------|------|
| P2-TOURNAMENT-WS-001 | Tournament WS 事件未绑定 | 📋 待 Coding | `handleSngStart`/`handleBlindLevel` 等 `@SubscribeMessage` 缺失 |

## 任务队列更新

**新增 P2**：

```
| P2-TOURNAMENT-WS-001 | Tournament WS handler 绑定 | 📋 待 Coding | 
  `game.handler.ts` 需新增 handleSngStart/handleRegisterSngPlayer 等 WS 事件，
  绑定到 AppGateway 并调用 TournamentService
```

## 跨代理

| 代理 | 状态 |
|------|------|
| Coding | 🟡 Tournament scaffold 未 commit（13文件 WD）；WS 集成层待实现 |
| Productor | 🟢 P0/P1 全清，P2-FASTFOLD 储备 |

## 上轮报告问题进度

| ID | 问题 | 状态 |
|----|------|------|
| P2-TOURNAMENT-001 | `calculateFinalRankings()` 返回空数组 | ✅ 已修复（commit 7869f51 之前） |
| P2-TOURNAMENT-002 | `TournamentService` 0 tests | ✅ 已修复（295 tests 含此模块） |
| P2-TOURNAMENT-003 | `advanceBlindLevel` WS handler 调用链 | ❌ 未修复（WS 集成完全缺失） |

*Test 第168轮 — 2026-04-26 22:30 — P2-TOURNAMENT-WS-001 新发现（WS 集成断链）*
