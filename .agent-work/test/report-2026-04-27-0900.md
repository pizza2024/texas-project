# Test Report — 第307轮

**时间:** 2026-04-27 09:00
**HEAD:** `9f7011a`
**测试状态:** 411 passed | 0 failed | 0 TS errors | 0 ESLint errors
**阶段:** 稳定监控期

---

## CodeReview 发现

### P0 / P1 问题

**无新发现。** 系统在 P0-TEST-001~004 和 P1-TEST-001~004 修复后保持稳定。

---

### P2 遗留问题确认（本轮扫描）

| ID | 问题 | 状态 | 确认 |
|----|------|------|------|
| P2-TEST-001 | `exchangeBalanceToChips` 事务外读取 `realBalance` | 🔍 待认领 | **确认存在** — `wallet.service.ts:350` `getRealBalance()` 在 `$transaction` 外调用，仅 `usdtAmount > realBalance` 检查在事务外，存在 TOCTOU 窗口 |
| P2-TEST-002 | ELO 边界强制分离事务 | 🔍 待认领 | 未本轮扫描（需详细审查 matchmaking.service.ts） |
| P2-TEST-004 | WebSocket `socket.on` listeners 未 cleanup | 🔍 待认领 | **部分确认** — `room/[id]/page.tsx:334-438` 使用 `socket.on` 但 return cleanup 仅清理 `rejoin_available` 和 `disconnectSocket`，其他 listeners（`room_update`、`emoji-reaction`、`already_in_room` 等）依赖组件卸载卸载 implicit cleanup |
| P2-TEST-006 | 操作按钮无重复提交保护 | 🔍 待认领 | `ActionBar.tsx` 无 disabled-on-click 防重放机制 |
| P2-TEST-007 | Token 过期用 `window.location.replace` 丢弃状态 | 🔍 待认领 | `page.tsx:388` redirectForExpiredToken 确认使用 `window.location.replace` |
| P2-TEST-008 | 无 refresh token 机制 | 🔍 待认领 | jwt.strategy.ts 无 refresh token 实现 |

---

### 新代码扫描（近2轮 commits）

**审查范围:** `9f7011a` (docs: task-queue) ← `7e756ba` (P1-TEST-004 GameTable React.memo) ← `7216be8` (P1-TEST-002 freezeBalance guard)

| Commit | 文件 | 审查结果 |
|--------|------|---------|
| `7e756ba` | `GameTable.tsx` | ✅ React.memo 包裹正确，防止级联 re-render |
| `7216be8` | `wallet.service.ts` | ✅ `freezeBalance` 已添加 `availableBalance >= amount` guard + BadRequestException |

---

### 重点模块扫描

**table-round.ts (advanceStreet / showdown)**
- ✅ Line 161-163: `eligible.length === 0` 正确 early return，NaN 不会进入 `winAmounts`
- ✅ `rake` 扣减在 winners 计算前，逻辑正确
- ✅ `performShowdown` / `resolveFoldWin` 双路径均无问题

**game.handler.ts (629 行)**
- ✅ `join-room` — double-check pattern (`getUserCurrentRoomId` × 2) withRoomLock 内再次验证
- ✅ `leave-room` — `leaveCurrentRoom` 正确顺序
- ✅ `player-action` — rate limit + validation 到位
- ⚠️ `schedulePostHandFlow` / `broadcastTableState` 串行 await，无并行优化

**wallet.service.ts**
- ✅ `setBalances` 已原子化（commit `3cafbb2`）
- ⚠️ `exchangeBalanceToChips` — `realBalance` 读取在事务外（P2-TEST-001）
- ✅ deposit.service.ts `checkAddressDeposits` 原子化已确认

**jwt.strategy.ts**
- ✅ Redis fail-closed 安全策略正确
- ✅ httpOnly cookie fallback 正确
- ⚠️ 无 refresh token 机制（P2-TEST-008）

---

## 测试体验

### 测试覆盖（411 tests）

```
Test Suites: 31 passed, 31 total
Tests:       411 passed, 0 failed
Time:        8.207s
```

**覆盖模块:** prisma, connection-state, room, app.controller, table-engine (game-logic, sit-out, straddle, all-in-call-protection, hand-evaluator), websocket (timer-debug), club, friend, matchmaking, wallet, withdraw, deposit, rakeback, admin, health

**缺口:** mission.service.spec.ts 缺失（mission.service.ts 有业务逻辑但无单元测试）

---

## 优先级任务队列

### P0 — 无（系统稳定）

### P1 — 无新发现

### P2 — 待办

| ID | 任务 | 严重度 | 备注 |
|----|------|--------|------|
| P2-TEST-001 | `exchangeBalanceToChips` `realBalance` 事务外读取 | Medium | TOCTOU window between read and transaction |
| P2-TEST-002 | ELO 边界强制分离事务 | Medium | 待审查 matchmaking.service.ts |
| P2-TEST-004 | WebSocket `socket.on` listeners 未完全 cleanup | Low | 组件卸载 implicit cleanup，建议 explicit |
| P2-TEST-006 | 操作按钮无重复提交保护 | Low | ActionBar 需 disabled-on-click 防重放 |
| P2-TEST-007 | Token 过期 `window.location.replace` 丢弃状态 | Low | 建议用 router.replace 保留 history |
| P2-TEST-008 | 无 refresh token 机制 | Medium | JWT 过期需重新登录 |
| P2-MISSION-SPEC | mission.service.spec.ts 缺失 | Medium | mission.service 有业务逻辑无测试 |

---

## 下一轮优先级

1. **P1-BLAST-001** — Blast 即时赛事规格（来自 Productor 第306轮）
2. **P2-TEST-001** — `exchangeBalanceToChips` TOCTOU 修复
3. **P2-MISSION-SPEC** — mission.service.spec.ts 补充

---

*Test 第307轮 — 2026-04-27 09:00 — 稳定监控期，无 P0/P1 阻塞，411 tests passing*
