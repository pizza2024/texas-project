# Test Report — 2026-04-30-1215

**时间:** 2026-04-30 12:15
**HEAD:** `a7979bd`
**分支:** develop

---

## 状态总览

| 指标 | 状态 |
|------|------|
| P0/P1 | ✅ 均清零 |
| 单元测试 | ✅ 451/452 通过 (1 skipped) |
| TypeScript 编译 | ✅ backend + web 0 错误 |
| 新应用代码 commit | 无（距上次报告无新 commit） |

---

## CodeReview 发现

### P2 — 遗留项确认（无新增）

本轮扫描了以下变更：

- `a7979bd` — P0-NOTIFY-MIGRATE: Prisma migration，notification + user_notification_settings 表创建
- `0b6774b` — P2-NOTIFY-001 Phase 3: DND settings UI + backend settings API
- `3386592` — P2-NOTIFY-WS-BRIDGE: Redis pub/sub multi-instance WebSocket broadcast

**结论：代码质量良好，无 P0/P1 问题。**

---

### P2 — `emailEnabled` / `pushEnabled` 字段存在但未连线

**文件:**
- `apps/web/components/notification/notification-settings-panel.tsx` (新文件)
- `apps/backend/src/notification/notification.service.ts`
- `apps/backend/src/notification/notification.gateway.ts`

**现象:**
- `UpdateSettingsDto` 支持 `emailEnabled` + `pushEnabled` 字段写入 DB
- `NotificationGateway.emitToUser()` 仅检查 `isPushAllowed()` 中的 DND 时间段逻辑
- `pushEnabled` / `emailEnabled` DB 值**未被读取和使用**
- 没有实际的邮件发送基础设施

**风险:** 低 — 字段为 Phase 3 UI 占位，Productor 已标注为 "P2-NOTIFY-EMAIL/PUSH-WIRE — 需确认"

**状态:** 已知遗留 P2，Productor 跟踪中，无需 Test 代理重复报告

---

### 迁移审查 — ✅ 通过

`202604301000_add_notification_settings` 迁移文件审查：

- `notifications` 表：id, userId, type, title, body, metadata(JSONB), read, createdAt
- `user_notification_settings` 表：userId 唯一约束，外键级联删除
- 索引：userId, read, createdAt, 复合索引 `(userId, read, createdAt DESC)` — 查询性能良好
- 无数据迁移风险（新增表）

---

### WebSocket 多实例广播审查 — ✅ 通过

`websocket-manager.ts` Redis pub/sub 实现审查：

- `redisSub` + `redisPub` 分离，避免死锁
- 离线队列禁用 + `lazyConnect: true` — 连接失败不阻塞启动
- 错误日志仅 warn，不抛异常 — 容错设计正确
- 频道名 `ws:broadcast` 唯一，无冲突风险

---

## 测试体验

### 单元测试结果（12:15 执行）

```
Test Suites: 32 passed, 32 total
Tests:       1 skipped, 451 passed, 452 total
Time:        6.786s
```

- 所有 table-engine 核心测试通过（hand-evaluator, straddle, all-in-call-protection, sit-out-detector）
- deposit service spec 通过
- websocket connection-state 通过
- 无新增失败

### 覆盖率趋势

| 时刻 | 测试数 |
|------|--------|
| 12:00 | 451 passed |
| 12:15 | 451 passed |

无新增测试也无新增失败。

---

## 遗留 P2 状态确认

| ID | 任务 | 状态 |
|----|------|------|
| P2-NOTIFY-EMAIL-WIRE | `emailEnabled` 未连线（无邮件基础设施）| 已知，Productor 跟踪 |
| P2-NOTIFY-PUSH-WIRE | `pushEnabled` 未连线（DND 时间段检查已就绪）| 已知，Productor 跟踪 |
| P2-WITHDRAW-UX-004 | 提现历史追踪 | 规格待补充 |
| P2-TOURNAMENT-BLIND | 盲注时间轴 UI | Productor spec 进行中 |
| P2-TOURNAMENT-GTD | 保底奖池 | 未开始 |
| P2-ROOM-UX-003/004/005 | 空状态/收藏/邀请 | 未开始 |

---

## 任务队列优先级建议

**本轮无 P0/P1 新增，无需更新任务队列。**

Productor 的 `P2-TOURNAMENT-BLIND`（盲注时间轴 UI spec）处于最高优先级，Test 代理将持续监控相关代码变更。

---

## 结论

✅ **12:15 报告：P0/P1 均清零，系统稳定，无新增问题。**

最新 commit `a7979bd` 的 P0-NOTIFY-MIGRATE 迁移落地，Phase 3 DND 设置功能完整。WebSocket Redis pub/sub 多实例广播实现正确。451 单元测试全部通过。

---

_Test r63 — 2026-04-30 12:15 — P0/P1 清零_
