# Test Report — 2026-04-28 16:30

**Agent:** Hermes Test (cron)
**HEAD:** `6914f27` — P0 三连修复 ✅
**Interval:** 15 min since last report (16:15)
**报告类型:** 里程碑确认 + 回归验证

---

## Test Results

```
Backend:  32 suites / 452 tests — ✅ 0 failed
Web:       2 suites  / 15 tests  — ✅ 0 failed
TS errors: 0 ✅
```

---

## 里程碑：P0 问题全部清零 ✅

`6914f27` 一次性修复 3 个 P0，commit message 完整描述如下：

| # | 修复内容 | 文件 | 验证 |
|---|---------|------|------|
| ✅ P0-NEW-TABLE-STUCK | `checkAndAutoFoldSittingOut` MAX(3) 超时后现在正确调用 `advanceStreet`/`nextActiveFrom` 推进 turn | `table-player-ops.ts:228-235` | 代码已确认：超时后 `player.status=FOLD`，再走 `isBettingRoundComplete` 分支推进 |
| ✅ P0-NEW-WITHDRAW-DOUBLE-REFUND | `handleWithdrawFailure` 改用 `updateMany` + `WHERE status=PENDING` 原子化防护双重退款 | `withdraw.service.ts:627-633` | 代码已确认：`updated.count === 0` 时跳过 refund |
| ✅ P0-NEW-TRC20-CHARS | TRC20 正则从 `/^T[A-HJ-NP-Za-km-z1-9]{33}$/` 改为 `/^T[A-Za-z0-9]{33}$/`，不再排除合法字符 `I`, `O` | `wallet.service.ts:259` | 代码已确认：接受全字母数字 |
| ~~P0-NEW-OTP-NORATELIMIT~~ | `verifyEmailCode` 已有 `OTP_MAX_ATTEMPTS=5` + `TTL=300s` 防护，关闭 | — | 关闭 |

---

## P0 问题状态（历史首次清零）

```
P0 历史遗留: 0 🔴 → 0（首次清零）✅
```

---

## P1 遗留问题

| ID | 任务 | 状态 |
|----|------|------|
| P1-NEW-001 | `activeGames` Map 无 TTL 清理 | 🔴 Open — 内存泄漏风险 |
| P1-BLAST-012 | Blast Phase 3 规格（endsAt/退款/持久化/惩罚） | 🟡 待 Productor |

### P1-NEW-001 详情（持续跟踪）

`TableManager.activeGames: Map<string, Table>` 在牌桌关闭后从不清理，无 TTL 机制。长期运行会导致内存持续增长。

**当前缓解：** Node.js 进程通常有重启周期，生产环境 k8s 容器定期重启可限制泄漏规模。

**建议方案：** 添加 `activeGames.delete(tableId)` 在 `TableManager.closeTable()` 路径中。

---

## P2 遗留问题

| ID | 任务 | 状态 |
|----|------|------|
| P2-NEW-018 | `drawBlastMultiplier()` shared 版用 `Math.random()` | 🔍 Open |
| P2-NEW-020 | `activeGames` 无主动 TTL 清理 | 🔍 Open（与 P1-NEW-001 重叠） |
| P2-NEW-021 | BlastService Phase 3 缺少单元测试 | 🔍 Open |
| P2-NEW-022 | Emoji username 与 Chat 显示不一致 | 🔍 Open（已修复 P1-NEW-003） |
| P2-TEST-001 | `exchangeBalanceToChips` 事务外读取 | 🔍 Open |
| P2-TEST-002 | ELO 边界强制分离事务 | 🔍 Open |
| P2-TEST-005 | `TableState`/`Player` 类型 web↔shared 漂移 | 🔍 Open |
| P2-TEST-006 | ActionBar 无重复提交保护 | 🔍 Open |
| P2-TEST-007 | Token 过期 `window.location.replace` 丢弃状态 | 🔍 Open |
| P2-TEST-008 | 无 refresh token 机制 | 🔍 Open |

---

## 测试体验观察

| # | 观察 | 备注 |
|---|------|------|
| T1 | P0 三连修复代码质量高 | 每个修复都有 commit message 说明，测试覆盖同步更新（`withdraw.service.spec.ts` 30行改动） |
| T2 | 452 backend tests 持续绿灯 | 无 regression |
| T3 | Blast Phase 4 前端组件已落地 | `SpinWheel` + `MatchingOverlay` + `CreateBlastDialog` 已实现 |

---

## 优先级任务队列

### P0 — 清零 ✅

### P1 — 重要

| ID | 任务 | 备注 |
|----|------|------|
| P1-NEW-001 | `activeGames` Map TTL 清理 | 建议在 `closeTable` 路径加 `delete` |
| P1-BLAST-012 | Blast Phase 3 规格确认 | 待 Productor 输出 |

### P2 — 建议

| ID | 任务 |
|----|------|
| P2-NEW-021 | BlastService Phase 3 单元测试补充 |
| P2-TEST-001 | `exchangeBalanceToChips` 事务边界审查 |
| P2-TEST-005 | `TableState` 类型漂移检查 |

---

## Summary

| 维度 | 状态 |
|------|------|
| P0 新增 | 0 |
| P0 遗留 | **0 ✅（历史首次清零）** |
| P1 遗留 | 2（1 内存泄漏 + 1 待 Productor） |
| P2 遗留 | ~10 |
| Backend 测试 | ✅ 452 passed |
| Web 测试 | ✅ 15 passed |
| TS 编译 | ✅ 0 errors |

**重大里程碑：** 4 个 P0 全部关闭（其中 P0-NEW-OTP-NORATELIMIT 验证为误报，P0-TABLE-STUCK/P0-WITHDRAW-DOUBLE-REFUND/P0-TRC20-CHARS 于 `6914f27` 修复）。

---

*Test Report — 2026-04-28 16:30 — Hermes Agent (cron)*
