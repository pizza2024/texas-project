# Test Report — 第328轮

**时间:** 2026-04-28 14:15
**HEAD:** `de4156b`
**系统:** npm test (backend) → 32 suites / 452 tests ✅ | Backend TS: 0 errors ✅ | **Web TS: 3 errors** ⚠️

---

## 状态

| 优先级 | 数量 | 变化 |
|--------|------|------|
| P0 | 0 | unchanged |
| P1 | 1 | ⚠️ NEW |
| P2 | ~9 | unchanged |

---

## 一、CodeReview 发现

### ⚠️ P1 — Web TS 编译错误（Blast Phase 4 新增）

commit `de4156b` 引入的 Phase 4 前端代码有 **3 个 TypeScript 编译错误**：

```
components/blast/MatchingOverlay.tsx(3,41): error TS2307: Cannot find module 'framer-motion' or its corresponding type declarations.
components/blast/SpinWheel.tsx(4,38): error TS2307: Cannot find module 'framer-motion' or its corresponding type declarations.
hooks/useBlastSocket.ts(2,10): error TS2459: Module '"@texas/shared/socket"' declares 'socket' locally, but it is not exported.
```

| 文件 | 问题 | 原因 |
|------|------|------|
| `MatchingOverlay.tsx` | 缺少 `framer-motion` | Web package.json 未声明此依赖 |
| `SpinWheel.tsx` | 缺少 `framer-motion` | Web package.json 未声明此依赖 |
| `useBlastSocket.ts` | `socket` 未导出 | `packages/shared/src/socket.ts` 只导出 `getSocket()`，不导出 `socket` 单例 |

**分析：**
- `framer-motion` 依赖缺失 — `SpinWheel.tsx` 使用 `motion, useAnimation` from framer-motion，但 web 的 `package.json` 中不存在此依赖
- `socket` 导出问题 — `useBlastSocket.ts` 引用 `socket` from `@texas/shared/socket`，但 shared 只导出 `getSocket()` 函数，`socket` 是模块内部变量（line 60: `let socket: Socket...`）

### ✅ 无其他新问题

- Backend 无 `Math.random()` — 0 个
- Backend 无 `as any` production 滥用 — 受控
- Web 无 `Math.random()` — 0 个
- Backend tests: 32 suites / 452 tests ✅

---

## 二、测试覆盖

### Backend 测试 ✅

```
32 suites / 452 tests — 无变化
```

### Web TS 编译 ⚠️

```
apps/web: 3 errors (framer-motion × 2, socket export × 1)
```

---

## 三、P1 修复建议

| ID | 优先级 | 问题 | 建议 |
|----|--------|------|------|
| **P1-BLAST-4-FIX** | P1 | `framer-motion` 依赖缺失 | 方案A: `pnpm add framer-motion`（推荐）<br>方案B: 用 CSS animation 替换 framer-motion（无新依赖） |
| **P1-BLAST-4-SOCKET** | P1 | `socket` 未导出 | `useBlastSocket.ts` 改用 `getSocket()` 替代直接引用 `socket` 单例 |

---

## 四、P2 遗留清单（~9项）

| ID | 描述 | 建议动作 | 状态 |
|----|------|---------|------|
| P2-WEB-SPEC | Web 测试覆盖不足 | 补充 room/[id]、blast/page、rooms page 测试 | 🔍 待实施 |
| P2-TOURNAMENT-SPEC | Tournament spec 不足 | 补充 Matchmaking/wallet/friend spec | 🔍 待实施 |
| P2-ROOM-RETRY | 重试无指数退避 | `deposit/page.tsx:108` 加指数退避 | 🔍 待认领 |
| P2-CODE-PATTERN | `Promise.all` 无错误隔离 | `mission.service.ts:170` 改用 allSettled | 🔍 待认领 |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键 | `game.handler.ts` 加 messageId | 🔍 待认领 |
| P2-CHAT-STUB | `handleClaim` 无实际 API 调用 | 架构决策，建议关闭 | 🔍 待认领 |
| P2-WS-RATE-UNIT | `PASSWORD_ATTEMPT_WINDOW_MS` 单位 | 确认 `connection-state.service.ts:107` | 🔍 待确认 |
| P2-NEW-020 | `activeGames` Map 无主动 TTL 清理 | P3 — 多实例部署需 Redis TTL | 🔍 Open |
| P2-NEW-021 | BlastService Phase 3 缺少单元测试 | `startBlastGame/endBlastGame/forfeitBlast` 无 spec | 🔍 Open |

---

## 五、任务队列更新

### P0 — 0 项 ✅

### P1 — 1 项 NEW

| 优先级 | ID | 任务 | 建议 |
|--------|----|------|------|
| P1 | P1-BLAST-4-FIX | framer-motion 依赖缺失 | 在 web 目录安装 framer-motion 或替换为 CSS animation |
| P1 | P1-BLAST-4-SOCKET | socket 未从 @texas/shared/socket 导出 | useBlastSocket.ts 改用 getSocket() 函数 |

### P2 — 9 项

沿用上轮清单（见第三节）

---

## 六、下轮关注

1. **P1-BLAST-4-FIX + P1-BLAST-4-SOCKET** — 2个 TS 错误需修复才能使 web 编译通过
2. **Blast Phase 4B/C** — SpinWheel 和 WS 事件集成依赖上述 TS 修复
3. **develop 分支 push** — Productor 提到 develop 落后 1 commit

---

*Test Agent 第328轮 — 2026-04-28 14:15 — 0 P0 / 2 P1 / 9 P2 待处理*
