# Test Report — 第308轮

**时间:** 2026-04-28 09:30
**HEAD:** `d1d77b1`
**测试状态:** 411 passed | 0 failed | 0 TS errors | 0 ESLint errors
**阶段:** 稳定监控期

---

## CodeReview 发现

### P0 / P1 问题

**无新发现。** 本轮扫描的3个commit全部为正确修复：

| Commit | 文件 | 审查结果 |
|--------|------|---------|
| `7e756ba` | `GameTable.tsx` | ✅ `React.memo` 包裹正确，防止级联 re-render |
| `7216be8` | `wallet.service.ts` | ✅ `freezeBalance` 已添加 `availableBalance >= amount` guard + BadRequestException |
| `0a98c20` | `table-round.ts` | ✅ `resolveFoldWin` 使用 `buildPots()` 正确处理边池分配 + `eligiblePlayerIds` 过滤 |

---

### 新代码扫描（近5轮 commits）

**审查范围:** `d1d77b1` ← `7e756ba` ← `7216be8` ← `9f7011a` ← `0a98c20`

**`table-round.ts` (`0a98c20`):**
- `buildPots()` 边池分配现在使用 `eligiblePlayerIds` 过滤，正确排除被边池锁住的玩家
- `resolveFoldWin` 不再绕过 `buildPots()` — 逻辑一致性得到改善
- ✅ 逻辑正确

**`wallet.service.ts` (`7216be8`):**
- `freezeBalance` 新增 `available < normalized` guard，抛出 `BadRequestException`
- ✅ 修复 P1-TEST-002

**`GameTable.tsx` (`7e756ba`):**
- `GameTable` 组件用 `React.memo` 包裹
- ✅ 修复 P1-TEST-004

---

### 重点模块复检

**table-round.ts (advanceStreet / showdown)**
- ✅ Line 161-163: `eligible.length === 0` 正确 early return
- ✅ `rake` 扣减在 winners 计算前
- ✅ `performShowdown` / `resolveFoldWin` 双路径均正确

**game.handler.ts**
- ✅ `join-room` double-check pattern 正确
- ✅ `leave-room` 顺序正确
- ✅ `player-action` rate limit + validation 到位

**wallet.service.ts**
- ✅ `setBalances` 原子化
- ✅ `freezeBalance` guard 就位
- ⚠️ `exchangeBalanceToChips` — `realBalance` 读取仍在事务外（P2-TEST-001）

**jwt.strategy.ts**
- ✅ Redis fail-closed 安全策略
- ⚠️ 无 refresh token 机制（P2-TEST-008）

---

## 测试体验

### 测试覆盖（411 tests）

```
Test Suites: 31 passed, 31 total
Tests:       411 passed, 0 failed
Time:        8.046s
```

**覆盖模块:** prisma, connection-state, room, app.controller, table-engine (game-logic, sit-out, straddle, all-in-call-protection, hand-evaluator), websocket (timer-debug), club, friend, matchmaking, wallet, withdraw, deposit, rakeback, admin, health

**缺口:** `mission.service.spec.ts` 缺失 — `mission.service.ts` 有完整业务逻辑（10个任务、progress/claim/reset），但无单元测试

---

## P2 遗留问题确认

| ID | 问题 | 状态 | 确认 |
|----|------|------|------|
| P2-TEST-001 | `exchangeBalanceToChips` `realBalance` 事务外读取 | 🔍 待认领 | 仍存在 — `wallet.service.ts:350` `getRealBalance()` 在 `$transaction` 外调用 |
| P2-TEST-002 | ELO 边界强制分离事务 | 🔍 待认领 | 未本轮详细扫描 |
| P2-TEST-004 | WebSocket `socket.on` listeners 未完全 cleanup | 🔍 待认领 | `room/[id]/page.tsx` — cleanup 仅清理 `rejoin_available`，其他 listeners 依赖组件卸载 implicit cleanup |
| P2-TEST-006 | 操作按钮无重复提交保护 | 🔍 待认领 | `ActionBar.tsx` 无 disabled-on-click 防重放 |
| P2-TEST-007 | Token 过期 `window.location.replace` 丢弃状态 | 🔍 待认领 | `auth.ts:79` 确认使用 `window.location.replace` |
| P2-TEST-008 | 无 refresh token 机制 | 🔍 待认领 | `jwt.strategy.ts` 无 refresh token 实现 |
| P2-MISSION-SPEC | `mission.service.spec.ts` 缺失 | 🔍 待认领 | mission.service 有业务逻辑无测试 |

---

## 优先级任务队列

### P0 — 无（系统稳定）

### P1 — 无新发现

### P2 — 待办

| ID | 任务 | 严重度 | 备注 |
|----|------|--------|------|
| P2-TEST-001 | `exchangeBalanceToChips` `realBalance` 事务外读取 | Medium | TOCTOU window — 已在 P1-TEST-002 commit 中未修复 |
| P2-TEST-008 | 无 refresh token 机制 | Medium | JWT 过期需重新登录，影响游戏中断场景 |
| P2-TEST-002 | ELO 边界强制分离事务 | Medium | `matchmaking.service.ts` 两段式事务 |
| P2-MISSION-SPEC | `mission.service.spec.ts` 缺失 | Medium | mission.service 有10个任务但无测试覆盖 |
| P2-TEST-004 | WebSocket `socket.on` listeners 未完全 cleanup | Low | 建议 explicit cleanup |
| P2-TEST-006 | 操作按钮无重复提交保护 | Low | ActionBar 需 disabled-on-click |
| P2-TEST-007 | Token 过期 `window.location.replace` 丢弃状态 | Low | 建议 router.replace 保留 history |

---

## 下一轮优先级

1. **P1-BLAST-001** — Blast 即时赛事规格（Productor 第308轮确认待认领）
2. **P2-TEST-001** — `exchangeBalanceToChips` TOCTOU 修复
3. **P2-MISSION-SPEC** — `mission.service.spec.ts` 补充测试

---

## 系统状态摘要

| 指标 | 状态 |
|------|------|
| Tests | 411 passed, 0 failed |
| Lint | 0 errors |
| TS Compile | 0 errors |
| P0 问题 | 0 |
| P1 问题 | 0 |
| P2 遗留 | ~7 项 |
| HEAD | `d1d77b1` |

*Test 第308轮 — 2026-04-28 09:30 — 稳定监控期，无 P0/P1 阻塞，3个 P1 已修复（P0-TEST-001/P1-TEST-001~004），系统清洁*
