# Test Report — 第79轮

> 更新时间: 2026-04-25 00:15
> **项目**: Texas Hold'em Monorepo

---

## 状态

| 状态 | P0 | P1 | P2 | P3 |
|------|----|----|----|-----|
| 数量 | 3 🔴 | 4 🟠 | 3（Sit-Out/All-in新功能） | 12（规划中） |

---

## HEAD Commit & 测试

- **HEAD**: `d6e18df refactor(web): clean up unused props and add useCallback hooks`
- **Backend Jest**: ✅ 22 suites / 215 tests 全部通过（1.762s）
- **Worker leak 警告**: 仍存在（非阻塞）

---

## 本轮 CodeReview：新代码审查

本轮审查了本轮新提交和上一轮遗留的 P0 问题。

### 本轮新增变更（commit `d6e18df` 及之前一批）

#### ✅ P2-Sit-Out Option C — 已实现

**文件**: `table-engine/table-player-ops.ts`（+37行），`player.ts`（+2行），`table-game-logic.ts`（+2行）

实现了 Sit-Out 重构的 Option C（折中方案）：
- 新增 `consecutiveTimeouts` 字段，每局新 hand 重置为 0
- 玩家超时未操作时 +1，连胜 3 次后强制 SITOUT
- 任何玩家操作（fold/check/call/raise/allin/straddle）均重置计数器
- 强制 SITOUT 后玩家保持离座状态直到手动重新参与

**评估**: ✅ 逻辑正确，实现了 P2-003 Option B/C 混合方案

#### ✅ P2-All-in 确认弹窗 — 前端已实现

**文件**: `AllInConfirmModal.tsx`（新建，100行），`ActionBar.tsx`，`page.tsx`

WSOP 风格 All-in 确认弹窗：
- Pot Odds + 胜率双显示
- "Good value" / "Bluff spot" 标签
- 确认/取消按钮
- ⚠️ **硬编码问题**: `equity={50}` 是硬编码的 50%，未调用后端胜率计算 API

**P2-004-POTENTIAL-001 🟡 — All-in 弹窗 equity 硬编码**
- 文件: `apps/web/app/room/[id]/page.tsx` line ~820
- `equity={50}` 固定 50%，未调用后端手牌胜率 API
- 影响: 玩家看到的"胜率"是假的，影响决策体验
- 修复: 需要后端计算实际胜率或前端使用 hand-evaluator 计算

#### ✅ 前端 Props 清理

**文件**: `ActionBar.tsx`, `GameTable.tsx`, `PlayerSeat.tsx`, `GameHeader.tsx`

- 移除未使用的 props（`myUserId`, `isActionStage`, `activeCountdown`, `countdownLabel`, `table` 等）
- 添加 `useCallback` 包装 `redirectToLogin` 和 `redirectForExpiredToken`
- 评估: ✅ 正确的 React 性能优化

---

## P0 遗留问题（上一轮发现，仍未修复）

> ⚠️ 以下 P0 问题在上轮发现，本轮 commit 无变更，请 Coding 优先处理

### P0-001 🔴 — Auth: Redis宕机时JWT验证被绕过

- 文件: `auth/jwt.strategy.ts` lines 22-42
- Redis 不可用时记录 `[SECURITY-AUTH-BYPASS]` 但仍返回有效用户
- 影响: 任何失窃 JWT 在 Redis 宕机期间均可无限使用
- **状态**: 未修复

### P0-002 🔴 — Withdraw: Redis不可用时cooldown被静默跳过

- 文件: `withdraw/withdraw.service.ts` lines 97-130
- `checkAndConsumeCooldown` 在 Redis 返回 null 时直接 return
- 影响: 分布式部署中 Redis 不可达时所有用户可无冷却提取资金
- **状态**: 未修复

### P0-003 🔴 — Withdraw: processWithdraw服务层无Admin角色验证

- 文件: `withdraw/withdraw.service.ts` lines 344-428
- 服务层本身缺少角色检查
- **状态**: 未修复

---

## P1 遗留问题

### P1-001 🟠 — Wallet: freeze/unfreeze不同步User.coinBalance

- 文件: `wallet/wallet.service.ts` lines 133-175
- **状态**: 未修复

### P1-002 🟠 — Auth: verifyEmailCode无速率限制

- 文件: `auth/auth.controller.ts` lines 68-77
- 6位OTP 可被暴力枚举（100万种可能）
- **状态**: 未修复

---

## 本轮发现新问题

### P2-004-POTENTIAL-001 🟡 — All-in 弹窗 equity 硬编码

- 文件: `apps/web/app/room/[id]/page.tsx` line ~820
- `equity={50}` 固定 50%，不是真实胜率
- 需后端提供胜率 API 或前端集成 hand-evaluator

---

## 测试状态

| 测试套件 | 状态 | 备注 |
|---------|------|------|
| Backend Jest | ✅ 22 suites / 215 tests | 全部通过，1.762s |
| Worker leak | ⚠️ | 仍存在，非阻塞 |
| socket-io.integration | ⚠️ | 连接断开日志（预期行为） |

### 测试覆盖缺失

- `TableManagerService` 缺少单元测试（当前依赖集成测试覆盖）
- `verifyEmailCode` 无自动化测试
- 前端（apps/web）无自动化测试
- All-in 弹窗组件无测试

---

## P2 跟踪（待人工决策）

| ID | 任务 | 状态 |
|----|------|------|
| P2-002 | Club 数据库迁移 | ⏳ 待执行（需要 DATABASE_URL） |
| P2-003 | Sit-Out 重构 | ✅ 已实现 Option C |
| P2-004 | All-in 确认弹窗 | ✅ 前端已实现（equity 硬编码需修复） |

---

## 协同确认

| 代理 | 轮次 | 状态 |
|------|------|------|
| Coding | 78 | ✅ Sit-Out Option C + All-in 弹窗实现完成 |
| Test | 79 | ✅ 本轮完成，发现 equity 硬编码新问题 |
| Productor | 78 | ✅ P1 正式立项，首充红利 + Rakeback |

---

## 本轮建议

1. **P0-001/P0-002/P0-003**: 资金安全相关，上一轮发现后仍未修复，**建议 Coding 本轮优先处理**
2. **P2-004-POTENTIAL-001**: All-in 弹窗 equity 硬编码，需在 P2-004 完成前修复或说明是否需要真实胜率
3. **P2-002**: Club 迁移需 DevOps 协调 DATABASE_URL

---

*Last updated: 2026-04-25 00:15 — Test 第79轮 — 新增 P2 equity 硬编码问题，P0 遗留未修复*
