# Test Report — 第196轮

> 时间: 2026-04-27 05:45 (UTC+8)
> HEAD: `9a25696`
> 间隔: 15分钟（05:30 → 05:45）

## 状态

| 类型 | 数量 |
| ---- | ---- |
| P0   | 0    |
| P1   | 0    |
| P2   | 1    |

---

## 新发现

### P2 — `BTC_TOTAL_LEVELS` 未在 `tournament.ts` diff 中定义

**位置**: `packages/shared/src/types/tournament.ts`
**发现方式**: 代码审查
**风险等级**: P2（规格就绪但存在潜在编译问题）

`tournament-schedule.service.ts` 第33行导入了 `BTC_TOTAL_LEVELS`：

```typescript
import {
  TournamentType,
  BtcConfig,
  SngConfig,
  createBtcBlindSchedule,
  createDefaultBlindSchedule,
  BTC_CLOCK_INTERVAL_SECONDS,
  BTC_TOTAL_LEVELS, // ← 需验证是否已导出
} from "@texas/shared/types/tournament";
```

但 `git diff` 中 `tournament.ts` 的 unstaged 变更仅显示到 `createBtcBlindSchedule()` 函数定义，**未显示 `BTC_TOTAL_LEVELS` 的完整定义**。该常量被 service 引用，但不确定是否已在 shared 包中正确导出。

**需验证**: 运行 `pnpm --filter shared exec tsc --noEmit` 确认无编译错误。若报错则需 Coding 代理补充缺失导出。

---

## 代码审查摘要

### 新增文件（未commit）

| 文件                                      | 用途                              | 审查结论                                                                            |
| ----------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------- |
| `tournament-schedule/service.ts`          | 赛事日历后端                      | ✅ 类型安全，EventEmitter 模式正确，Redis 持久化，OnModuleInit/Destroy 生命周期完整 |
| `tournament-schedule/controller.ts`       | 赛事日历 REST API                 | 待审查（未读取）                                                                    |
| `tournament-schedule/dto/schedule.dto.ts` | DTO 定义                          | 待审查（未读取）                                                                    |
| `websocket/decorators.ts`                 | `@InjectServer()` Socket.io装饰器 | ✅ 干净的 NestJS 装饰器实现                                                         |
| `ChatPanel.tsx`                           | 房间聊天面板                      | ✅ 客户端rate-limit(500ms)正常，auto-scroll逻辑健壮，last-200消息保留               |

### 关键模块审查

**table-game-logic.ts**

- `startHand()`: ✅ 所有玩家状态正确重置，`consecutiveTimeouts` 每手重置
- `shuffle()`: ✅ 使用 `crypto.getRandomValues` 密码学安全随机
- 发牌逻辑: ✅ 两轮发牌（每轮每家一张）顺序正确

**game-handler.ts**

- `handleJoinRoom`: ✅ rate-limit + room-lock + user-lock 三层保护
- 密码验证: ✅ bcrypt 比较，错误计数正确
- `already_in_room` 双重检查: ✅ 锁内+锁外双重验证

**auth.service / jwt.strategy**

- ✅ Redis 不可用时 fail-closed（throw），不跳过验证

---

## 测试验证

| 测试项               | 结果                                    |
| -------------------- | --------------------------------------- |
| 后端 TypeScript 编译 | ✅ 0 errors                             |
| 前端 TypeScript 编译 | ✅ 0 errors                             |
| 后端测试 (27 suites) | ✅ 295 passed, 0 failed                 |
| Jest worker leak     | ⚠️ 仍存在 P1-004 警告（监控中，不阻塞） |
| WebSocket 集成       | ⚠️ 未在本次 15min 轮次执行 E2E          |

---

## 下一轮建议

1. **P1-SCHEDULE-001 前端页面** — 赛事日历 UI 开始实施（后端 `tournament-schedule` 模块已就绪）
2. **验证 `BTC_TOTAL_LEVELS`** — 运行 shared 包 type-check 确认导出完整性
3. **P1-FIRST-DEPOSIT** — 首充奖金规格实施
4. **P1-BTC-001 Phase 1** — BTC 赛事后端集成（TournamentType.BTC enum 已就绪）

---

## 跨代理状态

| 代理      | 状态                                          |
| --------- | --------------------------------------------- |
| Coding    | 🟢 第195轮，295 tests，队列空                 |
| Productor | 🟢 第195轮，0 P0 / 0 P1 / 0 P2                |
| Test      | 🟡 第196轮，295 tests，0 P0 / 0 P1 / **1 P2** |

_最后更新: 2026-04-27 05:45 — Test 第196轮 — 0 P0 / 0 P1 / 1 P2（BTC_TOTAL_LEVELS 待验证）_
