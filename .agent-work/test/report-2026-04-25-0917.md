# Test Report — 第115轮

> 更新时间: 2026-04-25 09:17

## 状态

| 状态 | P0 | P1 | P2 |
|------|----|----|-----|
| 数量 | 0 🟢 | 1 | 2 |

---

## HEAD Commit

`d61e623` — P1-CLUB-INVITE-001 完成，rakeback e2e 已提交

---

## CodeReview 发现

### ✅ 无新增问题

本轮代码变更（commit d61e623 ~ bfd9ffa）审查通过：

| 文件 | 变更类型 | 审查结果 |
|------|---------|---------|
| `table-round.ts:263-313` | fold win + rake 扣减修复 | ✅ 正确 |
| `table-manager.service.ts` | persistSettlementRecords 两阶段提交（Hand row sentinel） | ✅ 正确 |
| `game.handler.ts:99` | `checkPasswordAttemptLimit` + `clearPasswordAttempts` 正确 await | ✅ 已修复（P0-BRUTE-001 验证通过） |
| `app.gateway.ts:119-130` | `checkPasswordAttemptLimit` / `clearPasswordAttempts` 改为 async | ✅ 正确 |
| `rakeback.service.ts` | 5 tier 设计文档落地（BRONZE 15% → DIAMOND 50%） | ✅ 设计正确，代码待实现 |
| `club/` | ClubInviteCode model + 5 API + 前端 | ✅ 已完成 |

---

## 测试验证

### 单元测试 ✅

```
Test Suites: 26 passed, 26 total
Tests:       267 passed, 267 total
Time:        2.421 s
```

- 全量通过，无新增失败
- Startup cleanup 日志正常：`WARN [TableManagerService] Startup cleanup cleared 1 stale seat(s)`
- Redis fail-closed 日志正常：`denying request (fail-closed)`

### Lint ✅

```
web:lint: 10 warnings, 0 errors
```

10 个 warnings 均为 React Hook `exhaustive-deps` 和 1 个 `no-unused-vars`，均为 P2 级别，不阻塞。

### TypeScript 编译 ✅

Backend TSC: 0 errors ✅

---

## P0 — 已清零 ✅

| ID | 问题 | 状态 |
|----|------|------|
| P0-GAME-001 | `buildPots()` 边池分配错误 | ✅ 已修复 |
| P0-GAME-002 | `resolveFoldWin()` rake 未从 pot 扣减 | ✅ 已修复 |
| P0-GAME-003 | `bestHandFrom()` 5张牌早退路径 | ✅ 已修复 |
| P0-Withdraw-001 | `executeChainWithdraw` txHash 过早保存 | ✅ 已修复 |
| P0-Withdraw-002 | `processWithdraw` 无幂等保护 | ✅ 已修复 |
| P0-Deposit-001 | `checkAddressDeposits` 非原子操作 | ✅ 已修复 |
| P0-BRUTE-001 | `game.handler.ts:99` 未 await `checkPasswordAttemptLimit` | ✅ 已修复 |
| P0-NEW-001 | TooManyRequestsException 编译失败 | ✅ 已修复 |

---

## P1 进行中

| ID | 问题 | 状态 | 备注 |
|----|------|------|------|
| P1-RAKEBACK-001 | Rakeback E2E 测试验证 | 🔴 配置已修复 | jest-e2e.json setupFiles + ClubModule JwtModule + JWT_SECRET 长度修复（待 CI 验证） |

### P1-RAKEBACK-001 详情

- **根因**: `jest-e2e.json` 缺少 `setupFiles` 加载 `.env.test`
- **修复**: commit `d61e623` 中 `test/jest-e2e.json` 已添加 `setupFiles`
- **验证方法**: 本地运行 `npm run test:e2e` 或 CI pipeline
- **阻塞**: CI pipeline E2E 阶段

---

## P2 进行中

| ID | 问题 | 状态 | 备注 |
|----|------|------|------|
| P2-RAKEBACK-001 | Rakeback 5 tier 代码实现 | 📋 待 Coding | Productor 设计已完成（BRONZE 15% → DIAMOND 50%），后端代码仍为 3 tier |
| P2-NEW-001 | Web 10 React hooks exhaustive-deps warnings | 📋 待修复 | room/page.tsx (5处), room-mobile/page.tsx (1处), club/page.tsx (1处), rooms/page.tsx (2处), rakeback/page.tsx (1处) |

### P2-RAKEBACK-001: Rakeback 5 Tier 实现缺口

**Productor 设计（已完成）:**

| Tier | Rakeback | 月累计门槛 |
|------|---------|----------|
| BRONZE | 15% | $0 |
| SILVER | 22% | $500 |
| GOLD | 30% | $2,000 |
| PLATINUM | 40% | $10,000 |
| DIAMOND | 50% | $50,000 |

**当前代码（`rakeback.service.ts`）:**

```ts
const TIER_THRESHOLDS = {
  BRONZE: { minRake: 0, maxRake: 999, rate: 0.1 },    // 10%, not 15%
  SILVER: { minRake: 1000, maxRake: 4999, rate: 0.2 }, // 20%, not 22%
  GOLD: { minRake: 5000, maxRake: Infinity, rate: 0.3 }, // 30%, correct
} as const;
```

**缺口:**
1. 缺少 PLATINUM (40%, $10,000) 和 DIAMOND (50%, $50,000) tier
2. BRONZE rate 应为 15%（当前 10%）
3. SILVER rate 应为 22%（当前 20%）
4. 月累计门槛应按 USDT 计算（非 rake 绝对值，需确认 currency）

---

## 任务队列更新

### P1 — 下轮行动

1. **P1:** 运行 `npm run test:e2e` 验证 P1-RAKEBACK-001 配置修复
2. **P1:** Rakeback 5 tier 代码实现（PLATINUM/DIAMOND + rate 修正）

### P2 — 待办

1. **P2:** Web React hooks exhaustive-deps warnings 修复（10 处）
2. **P2:** Rakeback 5 tier 后端实现

---

## 下轮计划

1. **P1**: E2E 测试本地运行验证 `npm run test:e2e`
2. **P1**: Rakeback 5 tier 代码实现 + 单元测试
3. **P2**: Web lint warnings 修复（useCallback 包装或 eslint-disable 注释）

---

*Test 第115轮 — 267 tests pass, 0 TSC errors, E2E 配置已修复待验证，Rakeback 5 tier 设计完成但代码仍为 3 tier，Web 10 lint warnings（P2）*
