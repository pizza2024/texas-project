# Test 报告 — 2026-04-24 21:16（第67轮）

## 状态

| 状态 | P0   | P1   | P2        |
| ---- | ---- | ---- | --------- |
| 数量 | 0 ✅ | 0 ✅ | 2（已知） |

---

## HEAD 信息

- **Backend**: `2fa99db` — docs: update task queue — Rake P3 完成
- **Web**: `2fa99db` — 同步
- **分支**: develop

---

## CodeReview 发现

### 持续性问题（未修复）

| ID             | 问题                                                                                                          | 严重度 | 位置                             | 状态      |
| -------------- | ------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------- | --------- |
| P2-Club-FE-001 | `overlayRef` 作用域错误 — `ref={overlayRef}` 引用了嵌套组件内声明的局部变量，而非 ClubDetail Modal 的顶层 ref | P2     | `apps/web/app/club/page.tsx:522` | 🔴 未修复 |
| P2-Club-FE-003 | `overlayRef` 死代码 — CreateClubForm 组件内声明但未使用                                                       | P2     | `apps/web/app/club/page.tsx`     | 🔴 未修复 |
| P2-Club-FE-004 | `chatCursor/hasMoreMessages` 死代码 — 状态声明但从未读取                                                      | P2     | `apps/web/app/club/page.tsx`     | 🔴 未修复 |

### 已确认修复

| ID             | 问题                            | 状态                                  |
| -------------- | ------------------------------- | ------------------------------------- |
| P2-Club-FE-002 | `crypto.randomInt` 浏览器不兼容 | ✅ 已自行消失（当前 TS 编译无此错误） |

### Club Modal overlayRef 根因分析

```tsx
// 第 522 行：在 ClubDetail Modal JSX 顶层 div 上
<div ref={overlayRef} ...>

// 但 overlayRef 在第 372 行声明于 ClubDetail 组件内层：
const ClubDetail = ({ club, onClose }) => {
  // ...
  const overlayRef = useRef<HTMLDivElement>(null); // 第 372 行
  // ...
  return (
    <div ref={overlayRef}>  // ← 引用自身作用域的 overlayRef（正确）
```

**问题**：第 522 行的 `<div ref={overlayRef}>` 位于 ClubDetail 的 return 内，但 overlayRef 在第 372 行声明。这意味着该 ref 在第 522 行应该可见。

**真正问题**：TypeScript 报错 `Cannot find name 'overlayRef'` 暗示第 522 行可能位于某个内层作用域（如条件渲染分支、`<Modal>` 子组件等），该作用域内没有 `overlayRef` 的访问权限。需要检查第 505-522 之间的 JSX 结构，确认是否存在函数式子组件或条件分支阻断了作用域链。

---

## 测试结果

### Backend Jest

```
Test Suites: 22 passed, 22 total
Tests:       215 passed, 215 total
```

- ⚠️ **Jest Worker 泄漏**：仍有 `A worker process has failed to exit gracefully` 警告，但测试全部通过
- `--detectOpenHandles` 模式已通过（无声退出）

### Web TypeScript

```
app/club/page.tsx(525,12): error TS2304: Cannot find name 'overlayRef'.
app/club/page.tsx(528,42): error TS2304: Cannot find name 'overlayRef'.
```

### Backend Lint

✅ 无错误

---

## 遗留任务

| 优先级 | 任务                              | 状态              | 备注            |
| ------ | --------------------------------- | ----------------- | --------------- |
| P2     | Club 前端 overlayRef 修复         | 🔴 待 Coding 修复 | 作用域/闭包问题 |
| P2     | CreateClubForm overlayRef 死代码  | 🔴 待删除         | 未使用变量      |
| P2     | chatCursor/hasMoreMessages 死代码 | 🔴 待处理         | 声明未使用      |
| P2     | Club 数据库迁移                   | ⏳ 待执行         | 需 DATABASE_URL |
| P1     | Rake 抽水系统                     | ✅ P3 完成        | commit 2d807b1  |
| P1     | 首充红利                          | 待开发            | 商业化 P1       |
| P3     | Tournament 赛制                   | 规划中            |                 |
| P3     | Rakeback 体系                     | 规划中            |                 |

---

## 协同注意

- Club 前端 P2-001 虽然 commit a004993 已合并，但 TS 编译错误持续存在
- Rake P3 完成：commit 2d807b1（tier-based TIER_RAKE_CONFIG）
- Club 后端 P2-001 完成：commit acbac2c

---

_P3 优先级: Rake(商业化 P1) > 首充红利 > 好友邀请/Club-Invite > Tournament > 表情互动_

_Test 第67轮 — 2026-04-24 21:16 — Club 前端 overlayRef 持续编译错误，Jest Worker 泄漏未根除_
