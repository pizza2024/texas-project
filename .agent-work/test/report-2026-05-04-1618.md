# Test Report — r99

**时间:** 2026-05-04 16:18
**HEAD:** `ea88a76` ✅ 已同步（develop 分支）
**分支:** develop

---

## 状态

- **P0/P1:** ⚠️ **2 个新增 TS 错误阻塞编译（Insurance Phase 1 未完成）**
- **新发现问题:** 2 P0（编译错误，阻塞部署）
- **ESLint/TS:** ❌ backend 2 errors（见下方）; web 0 errors
- **本轮新增:** 2 TS 错误（Insurance Phase 1 集成问题）

---

## CodeReview — Insurance Phase 1 集成问题

### P0 — 新增（编译错误，阻塞）

| ID | 文件 | 问题 | 状态 |
|----|------|------|------|
| P0-INS-001 | `table-player-ops.ts:36` | `newPlayer` 对象缺少 `hasPurchasedInsurance: boolean` 属性 → TS2741 | ❌ 未修复 |
| P0-INS-002 | `app.gateway.ts` + `game.handler.ts:742` | `InsuranceService` 未注入 `AppGateway`，但 `handleBuyInsurance` 调用了 `gateway.insuranceService` → TS2339 | ❌ 未修复 |

### 详细分析

#### P0-INS-001 — `hasPurchasedInsurance` 缺失

**文件:** `apps/backend/src/table-engine/table-player-ops.ts:36`

```typescript
// 新增 Player 需要包含保险字段
const newPlayer: Player = {
  // ...existing fields...
  hasPurchasedInsurance: false,   // ❌ 缺失
  // insuranceRate, insuranceFee 可选
};
```

**修复:** 在 `newPlayer` 初始化对象中添加 `hasPurchasedInsurance: false`

---

#### P0-INS-002 — `InsuranceService` 未注入 `AppGateway`

**文件:** `apps/backend/src/websocket/app.gateway.ts`

`InsuranceModule` 已在 `websocket.module.ts` 中导入，但 `InsuranceService` 未注入 `AppGateway` 的 constructor：

```typescript
constructor(
  readonly tableManager: TableManagerService,
  // ...existing services...
  // ❌ 缺少: readonly insuranceService: InsuranceService,
)
```

`handleBuyInsurance` 调用 `gateway.insuranceService.recordInsurancePurchase()` 但该服务不存在。

**修复:** 在 `AppGateway` constructor 中添加 `InsuranceService` 注入

---

## 已验证 — P0/P1 遗留项（来自 r98）

所有 WebSocket P0/P1 问题已确认在 `ea88a76` 中修复：

| ID | 状态 |
|----|------|
| P0-WS-001 | ✅ 已修复（`6364ff3`） |
| P0-WS-002 | ✅ 已修复（`a27d0e8`） |
| P0-WS-003 | ✅ 已修复（`a27d0e8`） |
| P1-WS-001 | ✅ 已修复（`7452a41`） |
| P1-MATCH-001 | ✅ 已修复（`7452a41`） |
| P1-WS-002 | ✅ 已修复（`7452a41`） |
| P1-REDIS-001 | ✅ 已修复（`7452a41`） |
| P1-ROOM-001 | ✅ 已修复（`7452a41`） |

---

## 本轮新增代码审查（Insurance Phase 1）

### `insurance-calculator.ts` ✅

公式实现正确，单元测试覆盖全面：
- `calculateInsuranceFee`: `fee = floor(netLoss × (1 - equity) × rate/100)` ✅
- `calculateInsurancePayout`: `payout = floor(netLoss × rate/100)` ✅
- `checkInsuranceEligibility`: pot × minPotMultiplier 门槛检查 ✅
- 151 行测试文件，边界条件覆盖充分 ✅

### `insurance.service.ts` ✅（逻辑层面）

- `recordInsurancePurchase`: 正确创建 `InsuranceTransaction` 记录 ✅
- `settleInsurance`: WIN 扣 fee / LOSE 赔付，正确使用 `walletService.addChips` ✅
- `getInsuranceStats`: 统计逻辑正确（fee 求和，payout 仅 LOSE 计入）✅
- `hasPurchasedInsurance`: 防止重复购买 ✅

### `insurance-calculator.spec.ts` ✅

测试用例质量高，边界条件覆盖：
- equity=0（冷牌）/ equity=1（锁定）/ 负 netLoss / floor 行为 ✅

---

## Web 前端 — `InsuranceOfferModal.tsx`

- 组件存在但 diff 无变更（仅 untracked 文件）
- 无 Web diff 输出，TypeScript 编译 0 errors ✅

---

## 优先级任务队列

### P0 — 立即修复

| ID | 任务 | 状态 |
|----|------|------|
| P0-INS-001 | `table-player-ops.ts` 添加 `hasPurchasedInsurance: false` | ❌ 未修复 |
| P0-INS-002 | `AppGateway` 注入 `InsuranceService` | ❌ 未修复 |

### P2 — 后续迭代（规格已就绪）

| ID | 任务 | 状态 |
|----|------|------|
| P2-INSURANCE-001 | All-In Insurance 实施 | 📋 Phase 1 代码存在，集成未完成 |
| P2-BADBEAT-001 | Bad Beat Jackpot 实施 | 📋 待认领 |
| P2-FAST-FOLD | Fast-Fold 实施 | 📋 待认领 |
| P2-SOCIAL-001 | 观战系统 | 📋 待认领 |

---

## 跨代理状态

- **Productor r458:** P0/P1 清零；规格就绪；观战系统调研完成
- **Coding:** Insurance Phase 1 集成中（`ea88a76`），2 个 TS 错误待修复
- **Git 状态:** 工作目录有未提交更改（Insurance + schema + gateway 修改）

---

## 备注

- Insurance Phase 1 核心逻辑（calculator / service / spec）质量高，仅集成缺失
- `prisma generate` 需在部署前运行（已验证生成成功）
- 2 个 TS 错误均为简单缺失字段/参数，修复成本低（<10 分钟）

---

_Test r99 — 2026-05-04 16:18 — Insurance Phase 1 集成未完成；2 P0 TS 错误阻塞编译；P0/P1 WebSocket 问题已全部清零_
