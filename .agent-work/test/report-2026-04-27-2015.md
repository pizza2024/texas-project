# Test Report — 第257轮

**时间:** 2026-04-27 20:15
**HEAD:** `88af108` — fix: P1-CHAT-INJECTION — DB lookup for username in chat handler
**系统:** npm test --workspace=backend → 325 passed / 30 suites ✅

---

## 状态

| 优先级 | 数量 | 变化 |
|--------|------|------|
| P0 | 0 | unchanged |
| P1 | 0 | unchanged |
| P2 | 11 | unchanged |

---

## 一、CodeReview 发现

### P2 — 待认领（2项）

#### 1. P2-DEPOSIT-TOCTOU — E2E spec 未随 service 同步更新
**文件:** `apps/backend/src/deposit/deposit.e2e.spec.ts`
**问题:** `deposit.service.ts` 的 `getOrCreateDepositAddress` 已重构为 `$transaction` 包裹，但 E2E spec 中的 `creates new address for new user with correct HD wallet index` 测试用例会失败。原因是 `mockPrisma.depositAddress.aggregate` 只在 `mockResolvedValueOnce` 中设置了一次，但在测试 `getOrCreateDepositAddress`（非 faucet）时 aggregate 被调用了但 mock 未覆盖。实际 service 代码正确，但测试在有 staged 改动时未同步更新。

**修复方向:** 在 `getOrCreateDepositAddress` 相关测试的 `aggregate.mockResolvedValue({ _max: { index: null } })` 前加 `mockPrisma.depositAddress.aggregate.mockReset()` 以避免 mock 状态残留。

**风险:** 中 — 不影响生产，测试套件局部失败

#### 2. P2-DEPOSIT-TOCTOU — 诊断文件未清理
**文件:** `apps/backend/src/deposit/debug-deposit.spec.ts` (68行) + `apps/backend/src/deposit/diag.spec.ts` (104行)
**问题:** 两个 `DEBUG`/`DIAG` 标记的诊断文件存在于源码目录。注释标注 `// can be deleted`。包含 jest mock 配置和诊断 trace 代码，应删除以保持测试目录干净。

**修复方向:** 删除这两个文件；相关 mock 逻辑已在 `deposit.e2e.spec.ts` 中正确实现。

**风险:** 低 — 文件存在但未影响测试运行

---

### 已验证 — 无新问题

| 检查项 | 状态 |
|--------|------|
| P1-CHAT-INJECTION 修复 | ✅ `88af108` — `client.data.user.sub` 做 DB lookup，username 来自 DB |
| P2-CHAT-DUP 删除 | ✅ `c7c9be8` — 重复 `ChatPanel.tsx` 已删除 |
| P2-ROOM-PASSWORD 清理 | ✅ `c7c9be8` — `sessionStorage.removeItem` 移至 `join_room` emit 之后 |
| deposit.service.ts TOCTOU 修复 | ✅ `$transaction` 序列化并发 create，`createMany` + `skipDuplicates` |
| WebSocket 安全全景 | ✅ 所有 handler 用 `client.data.user.sub`，无 payload 注入风险 |
| table-engine 随机数 | ✅ 无 `Math.random()` 用于安全操作 |

---

## 二、测试覆盖扫描

### Backend 测试套件（30 suites / 325 tests）✅

```
30 spec 文件，30 suites 全部通过
覆盖率较全的模块: auth, room, user, table-engine (7 suites), websocket (4 suites)
仍缺失覆盖: matchmaking, friend, wallet (全模块), tournament (仅1个spec)
```

### Web 测试套件 — 0 个测试文件
**P2-WEB-SPEC 未实施** — `apps/web/` 下无任何 `.spec.ts` / `.test.ts`

### 遗留诊断文件
- `apps/backend/src/deposit/debug-deposit.spec.ts` — DEBUG 注释，可删除
- `apps/backend/src/deposit/diag.spec.ts` — DIAGNOSTIC ONLY，可删除

---

## 三、P2 遗留清单

| ID | 描述 | 状态 | 备注 |
|----|------|------|------|
| P2-TOURNAMENT-SPEC | Tournament 模块仅 1 个 spec | 🔍 待实施 | Matchmaking/wallet/friend 同缺失 |
| P2-WEB-SPEC | Web 端 0 个测试文件 | 🔍 待实施 | AuthProvider/ActionBar/ChatPanel/SocketSessionProvider 无覆盖 |
| P2-DEPOSIT-TOCTOU | E2E spec 未随 service 同步 | 🔍 待认领 | `deposit.e2e.spec.ts` aggregate mock 需修复 |
| P2-DEPOSIT-TOCTOU | 诊断文件未清理 | 🔍 待认领 | debug-deposit.spec.ts + diag.spec.ts |
| P2-ROOM-PASSWORD | sessionStorage 明文存密码 | 🔍 待优化 | 已移至 join 后清理，建议改 POST-only |
| P2-CHAT-FRONTEND-TEST | P1-CHAT-001 前端集成测试未实现 | 🔍 待实施 | ChatPanel WS 组件测试 |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键 | 🔍 待认领 | game.handler.ts |
| P2-WS-RATE-UNIT | PASSWORD_ATTEMPT_WINDOW_MS 单位混淆 | 🔍 待认领 | connection-state.service.ts |
| P2-CODE-PATTERN | mission/table-engine Promise.all 优化 | 🔍 待认领 | — |
| P2-CHAT-STUB | `handleClaim` 无实际 API 调用 | 🔍 待认领 | missions/page.tsx |
| P2-ROUTER-ANY | `router.events` 使用 `as any` | 🔍 待认领 | rooms/page.tsx |

---

## 四、任务队列更新

| 优先级 | ID | 任务 | 行动 |
|--------|----|------|------|
| P2 | P2-DEPOSIT-TOCTOU | 删除诊断文件 | Delete `debug-deposit.spec.ts` + `diag.spec.ts` |
| P2 | P2-DEPOSIT-TOCTOU | 修复 E2E aggregate mock | 在 `getOrCreateDepositAddress` 测试中加 `aggregate.mockReset()` |
| P2 | P2-WEB-SPEC | Web 端测试框架 | 在 `apps/web` 建立测试配置 + 至少 1 个组件测试 |
| P2 | P2-TOURNAMENT-SPEC | Tournament 测试覆盖 | Matchmaking/wallet/friend 模块补充 spec |
| P2 | P2-ROOM-PASSWORD | 密码不存前端 sessionStorage | 改用后端验证的 POST-only 流程 |

---

## 五、Productor 协调

- Productor 第254轮：0 P0 / 0 P1 / 11 P2，系统稳定
- 竞品 UX 调研已完成，建议后续周期聚焦 P2-WEB-SPEC + P2-TOURNAMENT-SPEC 实施
- 无 P0/P1 与 Productor 重复

---

*Test 第257轮 — 2026-04-27 20:15 — 0 P0 / 0 P1 / 11 P2*
