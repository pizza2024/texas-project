# Test Report — 第96轮

> 时间戳: 2026-04-25 04:30

## 状态

| 状态 | P0   | P1  | P2  | P3  |
| ---- | ---- | --- | --- | --- |
| 数量 | 0 ✅ | 1   | 2   | 4+  |

## HEAD Commit

```
b24eec7 docs: update task-queue — P0-NEW-001 + P2-TS all fixed
```

（距上一轮无新 commit）

---

## CodeReview

### 构建 & 测试

| 检查项               | 状态                             |
| -------------------- | -------------------------------- |
| turbo build          | ✅ 4/4 tasks 成功（全部 cached） |
| Jest 测试            | ✅ 238 tests，24 suites 全通过   |
| 生产代码 console.log | ✅ 未发现                        |
| TODO/FIXME/HACK/BUG  | ✅ 未发现                        |

### 核心模块审查

#### 1. Rakeback Service（第95轮新增）

| 方法               | 审查结果                                                            |
| ------------------ | ------------------------------------------------------------------- |
| `getRakeback()`    | ✅ 返回结构完整，Tier 进度计算正确                                  |
| `claimRakeback()`  | ✅ 原子事务（`$transaction` callback 模式），双重写入 wallet + user |
| `creditRakeback()` | ⚠️ 数组语法 `[$transaction([...])]`（P2-NEW-002 延续）              |

**P2-NEW-002 详情**：`rakeback.service.ts:160` 使用 `prisma.$transaction([...])` 数组语法，与 `claimRakeback` 中的 callback 语法 `prisma.$transaction(async (tx) => {...})` 不一致。建议统一为 callback 语法（更推荐），因为 callback 支持更好的类型推断。非阻塞。

#### 2. WebSocket player-action 流程

| 层级       | 机制                                             | 状态 |
| ---------- | ------------------------------------------------ | ---- |
| Gateway    | `PlayerActionSchema` 校验                        | ✅   |
| Rate Limit | `checkRateLimit()`                               | ✅   |
| Room Lock  | `withRoomLock()`                                 | ✅   |
| DB Persist | `persistTableState` + `persistTableBalances`     | ✅   |
| Timer      | `scheduleActionTimeout` / `schedulePostHandFlow` | ✅   |

#### 3. TableManagerService

- `cleanupOfflineResidueOnStartup()`: ✅ 启动清理幽灵座位逻辑正确
- `pendingGetTable` deduplication: ✅ 防止并发创建重复 Table 实例
- `userRooms` index: ✅ O(1) 用户→房间查询

#### 4. game.handler.ts

| 安全检查                            | 状态                                                     |
| ----------------------------------- | -------------------------------------------------------- |
| `roomId` 校验（用户是否在指定房间） | ✅ `validated.roomId !== userCurrentRoomId` 时 reject    |
| 密码暴力破解保护                    | ✅ `checkPasswordAttemptLimit` + `clearPasswordAttempts` |
| `freezePlayerBalance` on join       | ✅                                                       |

---

## P2 问题

### P2-NEW-002: creditRakeback 事务语法不一致

- **文件**: `rakeback.service.ts:160`
- **问题**: `prisma.$transaction([...])` 数组语法 vs `claimRakeback` 中的 `async (tx) => {...}` callback 语法
- **影响**: 低（运行时正确，只是代码风格不一致）
- **建议**: 统一为 callback 语法

### P2-004: Club `<img>` → `<Image />` 未优化

- **文件**: `apps/web/app/club/page.tsx`
- **状态**: 🟡 待处理（从 P1-004 轮次延续）
- **影响**: Next.js Image 优化失效，CLS 问题

---

## P3 体验优化（来自 Productor）

| ID     | 问题                     | 行动                                             |
| ------ | ------------------------ | ------------------------------------------------ |
| P3-001 | Rakeback i18n 翻译键缺失 | 需添加 `rakeback.title`, `rakeback.loadError` 等 |
| P3-002 | 段位阈值硬编码在 UI      | 应移至 `packages/shared/` 常量                   |
| P3-003 | 无 Rakeback 导航入口     | 建议在 `/stats` 或导航栏添加                     |
| P3-004 | Club `<img>` 未优化      | 同 P2-004                                        |

---

## 优先级任务队列

### P1 — 进行中

| ID     | 任务             | 状态      | 备注                                 |
| ------ | ---------------- | --------- | ------------------------------------ |
| P1-002 | Jest Worker 泄漏 | 🟡 监控中 | 非阻塞，`--detectOpenHandles` 已启用 |

### P2 — 待处理

| ID         | 任务                        | 状态      | 备注     |
| ---------- | --------------------------- | --------- | -------- |
| P2-NEW-002 | creditRakeback 事务语法统一 | 📋 待处理 | 低优先级 |
| P2-004     | Club `<img>` → `<Image />`  | 📋 待处理 | CLS 优化 |

---

## 结论

- **无 P0/P1 新问题** — 项目稳定性良好
- **Rakeback MVP** 已完成交付（P1-004 ✅）
- Rakeback 代码质量高：原子事务、参数校验、Tier 计算逻辑清晰
- WebSocket 游戏流程安全：rate limit、room lock、roomId 交叉验证均到位
- 下轮建议优先处理 P2-004 Club 图片优化（P3 体验优化可并行）

---

_Test 第96轮 — 无新阻塞问题，Rakeback MVP 完全交付。_
