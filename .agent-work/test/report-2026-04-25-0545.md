# Test Report — 第101轮

> 更新时间: 2026-04-25 05:45

## 状态摘要

- **P0**: 0（全部已修复 ✅）
- **P1**: 5 个进行中
- **Tests**: 238 passed / 24 suites
- **TypeScript**: 0 errors
- **HEAD**: `b24eec7`（15分钟前无新 commit）

## 无新问题发现

本轮为常规扫描，代码与第100轮完全一致，无新增缺陷、无回归。

---

## P1 追踪状态

| ID | 问题 | 状态 | 本轮确认 |
|----|------|------|---------|
| P1-001 | `getTable()` 并发竞态 — `pendingGetTable` 去重 | 🔴 待修复 | `pendingGetTable` Map 去重机制存在，但无并发竞态测试覆盖 |
| P1-002 | Redis 不可用时 rate limit 回退 | ✅ 已修复 | 代码已实现 fail-closed，Redis 返回 null 时拒绝所有请求 |
| P1-003 | 密码暴力破解保护 in-memory Map | 🔴 待修复 | `passwordAttempts` Map 仍在内存中，多实例不共享 |
| P1-004 | Jest Worker 泄漏 | 🟡 监控中 | 未见新泄漏报告 |
| P1-005 | `persistSettlementRecords` 静默失败 | ✅ 已修复 | AdminLog 告警已实现；外层 catch 块仍静默（设计决策） |

### P1-001 / P1-003 详细确认

**P1-001 — `getTable()` 并发竞态（table-manager.service.ts:103-166）**

`pendingGetTable` Map 去重机制已实装：
- 首次调用 `getTable(roomId)` 时创建 Promise 并存入 `pendingGetTable`
- 后续并发调用直接返回同一个 Promise，不重复创建 Table
- Promise `finally` 块正确清理 `pendingGetTable.delete(roomId)`
- **未覆盖场景**：无并发竞态单元测试（多个并发请求同时调用 `getTable` 同一 roomId）

```typescript
// table-manager.service.ts:107-165
if (!this.pendingGetTable.has(roomId)) {
  const promise = this.roomService.findOne(roomId).then(...).finally(() => {
    this.pendingGetTable.delete(roomId);
  });
  this.pendingGetTable.set(roomId, promise);
}
return this.pendingGetTable.get(roomId);
```

**P1-003 — 密码暴力破解 in-memory Map（connection-state.service.ts:17-20）**

```typescript
private passwordAttempts = new Map<string, { count: number; windowStart: number }>();
```

多实例部署时各实例 `passwordAttempts` Map 独立，攻击者可将请求负载分发至无记录的实例绕过限制。**Status: 待 Coding 修复**（P1-003 仍在 task-queue 为 🔴）

**P1-002 验证 — Rate Limit Fail-Closed（connection-state.service.ts:46-53）**

```typescript
// Redis 不可用时 DENY 所有请求
return false; // fail-closed
```
✅ 确认正确实现。

---

## 测试套件状态

```
Test Suites: 24 passed, 24 total
Tests:       238 passed, 238 total
TypeScript:  0 errors
```

**缺失测试覆盖（按优先级）**：
1. `table-manager.service.spec.ts` — 无 `getTable()` 并发竞态测试
2. `connection-state.service.spec.ts` — 无 passwordAttempts 多实例场景测试
3. `withdraw.service.spec.ts` — `executeChainWithdraw` 无区块链模拟测试
4. `deposit.service.spec.ts` — `checkAddressDeposits` 充值扫块并发测试

---

## 下轮行动

1. **等待 Coding 处理 P1-001** — 需为 `getTable()` 并发竞态补充测试
2. **等待 Coding 处理 P1-003** — 密码暴力破解保护需迁移至 Redis
3. **推进 SNG Phase 1** — 路线图已在 productor-latest.md 确认

---

*Test 第101轮 — 代码无变化，238 tests pass*
