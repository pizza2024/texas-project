# Code Review Report — 2026-04-28 12:45

**Reviewer:** Hermes Agent (cron)
**HEAD:** `82ebcf6` (Blast P1 Financial Fixes)
**Scope:** backend/src (blast, tournament, websocket) + web/app + shared/types
**报告类型:** 增量监控

---

## Test Results

```
Test Suites: 32 passed, 32 total
Tests:       452 passed, 0 failed
Time:        2.853 s
TS errors:   0
```

|| Check | Result | Notes |
|-------|--------|-------|
| Backend tests | ✅ 452 passed | 0 failed, 0 TS errors |
| Blast P1 fixes | ✅ Verified | commit 82ebcf6 applied |
| BLAST Phase 4 frontend | ❌ Not started | 0 files |
| `tournament.module.ts` | ✅ Updated | BlastService exported (untracked change) |

---

## HEAD 变更分析 — commit 82ebcf6

**Message:** `fix: Blast P1 financial integrity issues`

**修复的问题:**

| ID | 问题 | 修复内容 |
|----|------|----------|
| P1-BLAST-009 | `distributePrizes()` 无事务 | 整体包装 `$transaction` |
| P1-BLAST-010 | `returnFrozenChips()` 无事务 | 整体包装 `$transaction` |
| P1-BLAST-011 | `Math.random()` 不安全 | 替换为 `crypto.randomBytes(4)` |
| P2-BLAST-015 | `forfeitBlast` 迭代时变异 `playerIds` | 先过滤到 `remainingPlayerIds`，迭代完成后再赋值 |

**验证:**
- ✅ `distributePrizes()` — `await this.prisma.$transaction(async (tx) => { ... })` 包装所有 DB 写操作
- ✅ `returnFrozenChips()` — 同上
- ✅ `drawMultiplier()` — `crypto.randomBytes(4)` 替换 `Math.random()`
- ✅ `forfeitBlast` — `remainingPlayerIds` 先过滤，迭代完成后再 `game.playerIds = remainingPlayerIds`

---

## CodeReview — P1 遗留问题状态

### P1 — 财务完整性（已修复 ✅）

|| ID | 问题 | 状态 |
|----|------|------|------|
| P1-BLAST-009 | `distributePrizes()` 无事务 | ✅ 已修复 — commit 82ebcf6 |
| P1-BLAST-010 | `returnFrozenChips()` 无事务 | ✅ 已修复 — commit 82ebcf6 |
| P1-BLAST-011 | `Math.random()` 不安全 RNG | ✅ 已修复 — commit 82ebcf6 |

### P1 — 规格待确认

|| ID | 问题 | 状态 |
|----|------|------|
| P1-BLAST-012 | 硬编码 duration/multiplier 参数 | 🟡 遗留 — 无产品规格确认 |

**问题:** `BLAST_TOTAL_DURATION_MS = 180_000` (3分钟) 和 multiplier 分布为硬编码，待 Productor 确认：
- endsAt 时长
- 中途退出惩罚机制
- 持久化方案

---

## P2 — 遗留问题（无变化）

|| ID | 问题 | 状态 |
|----|------|------|
| P2-BLAST-013 | `activeGames` Map 进程本地，多实例不兼容 | 🔍 Open |
| P2-BLAST-014 | `setTimeout` 计时器非集群感知 | 🔍 Open |
| P2-BLAST-015 | `forfeitBlast` 变异共享 `playerIds` | ✅ 已修复 — commit 82ebcf6 |
| P2-TEST-001 | `exchangeBalanceToChips` 事务外读取 balance | 🔍 Open |
| P2-TEST-002 | ELO 边界强制分离事务 | 🔍 Open |
| P2-TEST-005 | `TableState`/`Player` 类型漂移 web↔shared | 🔍 Open |
| P2-TEST-006 | 操作按钮无重复提交保护 | 🔍 Open |
| P2-TEST-007 | Token 过期 `window.location.replace` 状态丢失 | 🔍 Open |
| P2-TEST-008 | 无 refresh token 机制 | 🔍 Open |

---

## 新发现 — P2

### P2-NEW-018 — `drawBlastMultiplier()` 在 shared 仍使用 `Math.random()`

**文件:** `packages/shared/src/types/tournament.ts:226–245`
**状态:** 🔍 Open（低风险 — 当前未被调用）

```ts
export function drawBlastMultiplier(): number {
  const roll = Math.random() * 100; // ❌ 非安全 RNG
  if (roll < 60) {
    return Math.floor(Math.random() * 9) + 2; // ❌
  } ...
}
```

**分析:**
- `blast.service.ts` 中的 `drawMultiplier()` 已使用 `crypto.randomBytes(4)` ✅
- shared 中的 `drawBlastMultiplier()` 目前**未被任何地方调用**（搜索结果为空）
- **风险:** 如果将来前端或其他服务调用此函数，存在博彩公平性问题

**建议:** 将 shared 版本也替换为 `crypto.randomInt()`，保持两端一致

---

## Blast Phase 4 前端状态

|| 组件/页面 | 状态 |
|----------|------|
| BlastCard 组件 | ❌ 缺失 |
| BLAST Lobby `/blast/` | ❌ 缺失 |
| 轮盘旋转动画 | ❌ 缺失 |
| 匹配等待 Overlay | ❌ 缺失 |
| BLAST 详情弹窗 | ❌ 缺失 |
| BLAST 牌桌 `/blast/[id]` | ❌ 缺失 |
| `apps/web/app/blast/` 目录 | ❌ 不存在 |

**结论:** Phase 4 前端 UI 完全未启动（距上次报告无变化）

---

## 未跟踪的本地修改

```
M apps/backend/src/tournament/tournament.module.ts  # 导出 BlastService
M apps/web/jest.config.ts                            # 添加 shared 模块映射
```

`tournament.module.ts` 的导出变更尚未 commit，建议确认是否为预期变更。

---

## 优先级任务队列

### P1 — 待认领

|| ID | 任务 | 状态 |
|----|------|------|
| P1-BLAST-012 | Blast 规格确认（endsAt/退款/持久化/惩罚） | 🟡 待 Productor |

### P1 — Phase 4 前端（待 Coding 实施）

|| ID | 任务 |
|----|------|------|
| P1-BLAST-UI-001 | BlastCard 组件 + BLAST Lobby 页面 |
| P1-BLAST-UI-002 | 轮盘旋转动画（Framer Motion 3D） |
| P1-BLAST-UI-003 | 匹配等待 Overlay + 倒计时 |
| P1-BLAST-UI-004 | BLAST 牌桌 `/blast/[id]` 页面 |
| P1-BLAST-UI-005 | WS 事件集成（`blast_game_started/ended/forfeited`） |

### P2 — 遗留

|| ID | 任务 |
|----|------|------|
| P2-BLAST-013 | `activeGames` Map 多实例部署不兼容 |
| P2-BLAST-014 | `setTimeout` 非集群感知 |
| P2-NEW-018 | `drawBlastMultiplier()` shared 版本使用 `Math.random()` |
| P2-TEST-001~008 | 遗留测试问题 |

---

## Summary

**P0:** 0 new ✅
**P1:** P1-BLAST-009/010/011 已修复 ✅；P1-BLAST-012 待 Productor
**P2:** ~8 遗留项 + 1 新增（P2-NEW-018）
**Test:** 452 tests ✅，0 TS errors ✅
**Phase 4 前端:** 完全未启动 ❌
**未跟踪变更:** `tournament.module.ts` 导出 BlastService（需确认）

**Top 优先级:**
1. **P1-BLAST-012** — Productor 确认 Blast 规格
2. **Phase 4 前端** — BlastCard + Lobby 启动（Coding 代理）
3. **P2-NEW-018** — 统一 `drawBlastMultiplier()` 使用 `crypto.randomInt()`

---

*Report generated: 2026-04-28 12:45 — Hermes Agent (cron)*
