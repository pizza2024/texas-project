# Test Report — 2026-04-25-1231（初始报告）

## 基本状态

| 项目             | 状态                                  |
| ---------------- | ------------------------------------- |
| 后端 TypeScript  | ✅ 0 错误                             |
| Web TypeScript   | ✅ 0 错误                             |
| ESLint (backend) | ✅ 通过                               |
| ESLint (web)     | ✅ 通过（ESLintIgnoreWarning 可忽略） |
| Jest 测试        | ✅ 26 suites / 270 tests 全部通过     |
| HEAD Commit      | `ac33323` — Button loading prop       |

---

## 本轮 CodeReview 发现

### 🔍 审查范围

- `table-engine/` — 手牌评估、Pot Odds、Straddle、All-in、结算
- `websocket/` — game.handler.ts、app.gateway.ts、connection-state.service
- `auth/` — JWT 策略、Redis 降级策略
- `withdraw/` — 提现服务（幂等性、txHash 保存时机）
- `table-manager.service.ts` — 并发锁、用户房间索引

### ✅ P0 已修复项持续有效

| ID              | 问题                                 | 验证结果                                      |
| --------------- | ------------------------------------ | --------------------------------------------- |
| P0-GAME-001     | `buildPots()` 边池分配错误           | ✅ 已修复（table-round.ts:265-271）           |
| P0-GAME-002     | `resolveFoldWin()` rake 未扣减       | ✅ 已修复（table-round.ts:304）               |
| P0-GAME-003     | `bestHandFrom()` 5张牌早退           | ✅ 已修复（hand-evaluator.ts:210-213）        |
| P0-Withdraw-001 | txHash 过早保存                      | ✅ 已修复                                     |
| P0-Withdraw-002 | `processWithdraw` 幂等性             | ✅ 已修复（$transaction + txHash 确认后保存） |
| P0-BRUTE-001    | 未 await `checkPasswordAttemptLimit` | ✅ 已修复（game.handler.ts:99）               |
| P0-SEC-001      | Redis 宕机时 JWT 验证绕过            | ✅ 已修复（fail-closed，jwt.strategy.ts）     |

### ✅ 游戏逻辑审查

**table-round.ts（showdown / fold-win）**

- `buildPots()` 逻辑正确：按 `totalBet` 分层，ACTIVE + ALLIN 玩家为 eligible
- fold-win 时 rake 扣减逻辑（line 304）与 showdown 一致
- 弃牌胜时 pot 不产生边池（eligible.length === 0 时直接 return）

**hand-evaluator.ts**

- `bestHandFrom()`：当 `all.length === 5` 时直接 evaluate5，不再走 combinations，修复了之前的早退路径
- `determineWinners()` 对空手牌（弃牌）也能正确处理（空数组传入时 combinations 返回空，best 为 null）

**table-manager.service.ts**

- `pendingGetTable` Map 防并发重复创建 Table 实例 ✅
- `userRooms` 索引在 `getTable()` 恢复时正确填充（line 152-154）✅
- `cleanupOfflineResidueOnStartup()` 仅清理 WAITING 阶段桌子，不影响进行中牌局 ✅

### ✅ WebSocket / Auth 审查

**game.handler.ts**

- 房间密码暴力破解保护：`checkPasswordAttemptLimit` 正确 await（line 99）
- `handlePlayerAction`：roomId 验证（line 262-268），防跨房间操作 ✅
- Rate limit 在所有写操作前检查 ✅

**jwt.strategy.ts**

- Redis 不可用时：`throw UnauthorizedException('AUTH_SERVICE_UNAVAILABLE')`（fail-closed）✅

---

## 测试体验

### ✅ 测试覆盖

| 模块                        | 测试数                                         | 状态 |
| --------------------------- | ---------------------------------------------- | ---- |
| hand-evaluator              | 100+ cases                                     | ✅   |
| table-game-logic            | 覆盖 startHand / processAction / advanceStreet | ✅   |
| table-manager.service       | 并发 + 余额冻结                                | ✅   |
| connection-state.service    | 22 tests                                       | ✅   |
| rakeback.service            | 20 tests（5-tier）                             | ✅   |
| sit-out / straddle / all-in | 各模块 spec                                    | ✅   |

### ⚠️ 观察到的潜在改进空间（P2）

| ID         | 发现                                                                                                                                   | 位置                             | 优先级 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------ |
| P2-OBS-001 | `handleLeaveRoom` 中 `leaveCurrentRoom` 余额重置路径（line 506-510）：先 setBalance 再 unfreezeBalance，如果进程在两步之间崩溃会如何？ | table-manager.service.ts:506-510 | P2     |
| P2-OBS-002 | `persistSettlementRecords` 的 Phase 2 为非原子（settlement + transaction + rakeback credit 分开执行），但有 AdminLog 兜底告警          | table-manager.service.ts:351-368 | P2     |
| P2-OBS-003 | Club 排行榜 / 专属房间 / 战绩统计 API 规格已设计（productor-latest.md），前端待实现                                                    | P2-CLUB-002/003/004              | P2     |

---

## P0 / P1 状态

- **P0**: ✅ 全部清零
- **P1**: ✅ 全部清零（P1-004 Jest Worker 泄漏监控中，但 `--detectOpenHandles` 已启用）

---

## 下轮建议任务

| 优先级 | 任务                       | 备注                              |
| ------ | -------------------------- | --------------------------------- |
| P2     | Club 排行榜 API + 前端实现 | P2-CLUB-002，productor 已完成规格 |
| P2     | Club 专属房间前端联调      | P2-CLUB-003                       |
| P2     | Club 战绩统计聚合 API      | P2-CLUB-004                       |
| P2     | 战后手牌复盘 UI 开发       | P2-NEW-007                        |

---

_Report generated: 2026-04-25 12:31 — Test Agent 第1轮（初始报告）_
