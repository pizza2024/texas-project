# Test Report — 第245轮

**时间:** 2026-04-26 17:45
**HEAD:** `9a8e7a4` — chore: update task-queue（仅文档更新）
**代码提交:** `a8442a0` — P1-WALLET-001/002 P1-WITHDRAW-005/007 transaction safety

---

## 系统健康

| 指标 | 状态 |
|------|------|
| 后端测试 | ✅ 28 suites / 323 tests PASS |
| TypeScript | ✅ 编译通过 |
| P0 问题 | ✅ 全部清零 |
| P1 问题 | 🔍 3 → 待核实（见下方） |
| P2 问题 | 🔍 12 项待处理 |

---

## CodeReview 发现

### ✅ P1-WALLET-001 `exchangeBalanceToChips` 原子性 — 已修复

**文件:** `wallet.service.ts:364-391`

```ts
await this.prisma.$transaction(async (tx) => {
  await tx.wallet.upsert({ ... });   // wallet 更新
  await tx.user.update({ ... });      // coinBalance 同步
  await tx.transaction.create({ ... }); // 审计日志
});
```

三步 DB 操作已原子化，commit `a8442a0`。✅

---

### ✅ P1-WITHDRAW-005 `createWithdraw` transaction log — 已修复

**文件:** `withdraw.service.ts:212-249`

- 外部 `transaction.create`（日志）已在 `a8442a0` 中移除
- 原子块内：wallet 更新 → user 更新 → withdrawRequest 创建
- REJECT 路径（line 353）：`refundChips` + transaction log + adminLog 全部在 `$transaction` 内 ✅

---

### ✅ P1-WITHDRAW-007 `executeChainWithdraw` DB 更新重试 — 已修复

**文件:** `withdraw.service.ts:575-615`

```ts
const MAX_RETRIES = 3;
for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
  try {
    await this.prisma.withdrawRequest.update({ ... });
    break;
  } catch (err) {
    if (attempt < MAX_RETRIES) {
      await new Promise((r) => setTimeout(r, 500 * attempt)); // 指数退避
    }
  }
}
```

链上确认后 DB 状态更新已加 3× 重试 + 指数退避。✅

---

### P1 在续问题 — 核实状态

| ID | 问题 | 代码现状 | 结论 |
|----|------|----------|------|
| P1-WALLET-001 | `exchangeBalanceToChips` 无 `$transaction` | ✅ 已原子化 | **已修复** |
| P1-WITHDRAW-005 | transaction log 在原子块外 | ✅ 已移除外部日志 | **已修复** |
| P1-WITHDRAW-007 | 链确认后 DB 更新无重试 | ✅ 3× retry + backoff | **已修复** |

**建议:** `test-latest.md` 中的 P1 状态标记应更新为已修复，与 `task-queue.md` 保持一致。

---

### P2 代码抽查（随机扫描）

#### ✅ deposit.service.ts:366 — 已原子化
`checkAddressDeposits` 中 wallet 更新 + depositRecord + transaction 已在 `$transaction` 内（commit `8ce5e54`）。

#### ✅ withdraw.service.ts:429 — adminLog 在链操作前
```ts
await this.prisma.adminLog.create({ ... }); // APPROVE 日志
await this.withdrawQueue.enqueueWithdraw(id); // 然后才入队
```
adminLog 在原子块外是合理的（财务核心操作已原子化，此处仅为审计）。

#### ⚠️ P2-DEPOSIT-TOCTOU — 仍需关注
`getOrCreateDepositAddress`（deposit.service.ts:62-77）索引竞争窗口未完全消除，建议后续 review。

---

## 测试体验

### 测试覆盖现状

| 模块 | spec 文件数 | 覆盖状态 |
|------|------------|----------|
| table-engine | 8 个 spec | ✅ 全面 |
| websocket | 3 个 spec | ✅ 基础覆盖 |
| withdraw | 1 个 spec（含 $transaction mock）| ✅ |
| wallet | spec 存在 | ✅ |
| deposit | spec 存在 | ✅ |
| 认证/用户 | spec 存在 | ✅ |

**缺失覆盖（建议 P2 跟进）:**
- `matchmaking.service.spec.ts` — 缺失
- `friend.service.spec.ts` — 缺失
- Web 前端组件 — 0 测试文件

---

## 任务队列更新建议

### P1 建议更新

test-latest.md 中以下三项应标记为 ✅ 已修复：

```
P1-WALLET-001   → ✅ 已修复  commit a8442a0
P1-WITHDRAW-005 → ✅ 已修复  commit a8442a0  
P1-WITHDRAW-007 → ✅ 已修复  commit a8442a0
```

### P2 待处理项（维持）

| ID | 描述 | 优先级 |
|----|------|--------|
| P2-DEPOSIT-TOCTOU | getOrCreateDepositAddress 索引竞争 | P2 |
| P2-JWT-LOCALSTORAGE | JWT → httpOnly Cookie（XSS 风险） | P2 |
| P2-WEB-SPEC | Web 端 0 测试文件 | P2 |
| P2-TOURNAMENT-SPEC | Tournament 模块测试覆盖不足 | P2 |
| P2-CHAT-FRONTEND-TEST | ChatPanel WS 集成测试缺失 | P2 |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键 | P2 |

---

## 结论

**系统状态: 稳定** — 所有 P0/P1 资金安全问题已在 `a8442a0` 中修复。323 个测试全部通过，TypeScript 编译干净。

**待跟进:** 
1. test-latest.md 中的 P1 状态标记需更新（避免与 task-queue.md 不一致）
2. P2 中的 XSS 风险（JWT in localStorage）建议下次迭代优先处理

---

*Test 第245轮 — 2026-04-26 17:45 — 0 P0 / 0 P1(已修复) / 12 P2*
