# Test Report — 第307轮

**时间:** 2026-04-27 10:15
**HEAD:** `d1d77b1`
**测试:** 411 tests | 0 TS errors | 0 ESLint errors
**阶段:** 稳定监控期

---

## 系统状态

- **测试通过:** 31 suites / 411 tests — 全通过
- **TS 编译:** 0 errors
- **ESLint (web):** 0 errors（config 文件缺失告警可忽略）
- **本轮 diff (HEAD~3..HEAD):**
  - `wallet.service.ts` — `freezeBalance` 增加 `availableBalance` 校验（P1-TEST-002 fix）
  - `wallet.service.spec.ts` — 新增 balance 不足测试用例
  - `GameTable.tsx` — 包裹 `React.memo`（P1-TEST-004 fix）
  - `task-queue.md` — P0/P1/P2 队列更新

---

## CodeReview 发现

### P2 — 待认领

| ID | 模块 | 问题 | 严重度 | 备注 |
|----|------|------|--------|------|
| P2-TEST-001 | wallet | `exchangeBalanceToChips` 事务外读取 balance | Medium | wallet.service.ts — 事务内应使用 Prisma `select` 读取 |
| P2-TEST-002 | matchmaking | ELO 边界强制分离事务 | Medium | matchmaking.service.ts — 边界检查与余额更新非原子 |
| P2-TEST-003 | wallet | `getBalance` 返回总额（依赖调用方 discipline） | Low | wallet.service.ts — 应明确命名或加 guard |
| P2-TEST-004 | web | WebSocket `socket.on` listeners 未 cleanup | Low | 已在 `use-game-socket.ts:66-70` 确认有正确 on/off cleanup |
| P2-TEST-005 | shared/types | `TableState`/`Player` 类型在 web/shared 间漂移 | Medium | types 定义重复/不一致 |
| P2-TEST-006 | web | 操作按钮无重复提交保护 | Medium | ActionBar — 快速连击可触发多次 WS 消息 |
| P2-TEST-007 | web | Token 过期用 `window.location.replace` 丢弃状态 | Low | auth — 应先提示再跳转 |
| P2-TEST-008 | auth | 无 refresh token 机制 | Medium | JWT 过期需重新登录 |

### 遗留 P2（来自 task-queue）

| ID | 问题 | 备注 |
|----|------|------|
| P2-NEW-014 | `broadcastTableState` 异步广播 | Medium — 广播顺序无保证 |
| P2-NEW-016 | `unfreezeBalance` 事务外 TOCTOU | Medium |
| P2-NEW-017 | adminLog 事务外 | Medium |
| P2-NEW-018 | BullMQ 无幂等键 | Medium |
| P2-NEW-019 | JWT 无 jti/refreshToken | Low |
| P2-NEW-020 | `logout` 错误静默吞掉 | Low |
| P2-NEW-021 | `resetToWaiting` 不清理 rake | Low |

---

## 测试体验

### 已验证（本轮）

| ID | 验证结果 |
|----|---------|
| P0-TEST-001 | ✅ `advanceStreet` line 161-163 — `eligible.length === 0` 已有 `return`，NaN 不会进入 `winAmounts` |
| P1-TEST-001 | ✅ `client.leave()` 在 room lock 之前 — intentional design，避免与 DB 状态竞态 |
| P1-TEST-002 | ✅ `freezeBalance` 已增加 `availableBalance >= amount` 校验（commit 7216be8）|
| P1-TEST-003 | ✅ Bot prefix 碰撞已验证无问题，bot UUID 唯一 |
| P1-TEST-004 | ✅ GameTable 已包裹 `React.memo`（commit 7e756ba）|

### 游戏核心引擎验证（table-round.ts）

- `performShowdown()` line 161-163: `eligible.length === 0` 正确早退
- `resolveFoldWin()`: 已重构为使用 `buildPots()` 做边池分配（commit 0a98c20）
- Rake 扣减顺序: 先 `pot -= rake`，再分配 — 正确
- `advanceStreet()`: 行数 368，结构完整

---

## 优先级任务队列

### P1（建议优先处理）

| ID | 任务 | 来源 |
|----|------|------|
| P1-BLAST-001 | Blast 即时赛事 Phase 1 — 数据库模型 + 基础 API | Productor |
| P1-DAILY-LOGIN | 每日签到奖励规格（Bovada 模式） | Productor |
| P1-FIRST-DEPOSIT | 首充奖励后端逻辑实施 | Productor |

### P2（优化项）

| ID | 任务 | 备注 |
|----|------|------|
| P2-TEST-006 | ActionBar 重复提交保护 | 建议加 debounce/loading state |
| P2-TEST-005 | TableState/Player 类型漂移 | 建议统一到 @texas/shared |
| P2-TEST-008 | JWT refresh token 机制 | 改善长期在线体验 |
| P2-NEW-014 | broadcastTableState 异步广播顺序 | Medium |

---

## 本轮结论

系统状态：**清洁**  
- 0 P0 / 0 P1 / ~27 P2
- 测试套件 100% 通过
- 最新 diff 包含 2 个已验证修复（P1-TEST-002, P1-TEST-004）
- `resolveFoldWin` 边池分配重构已落地

Productor 第311轮关注的 **首充体验 + 留存漏斗** 专题，建议 Test 后续关注：
1. 充值地址过期无刷新提示（UX P1）
2. 充值状态轮询无乐观更新（UX P1）
3. 每日签到奖励 — **最高优先级留存抓手**

---

*Test 第307轮 — 2026-04-27 10:15 — 系统清洁，0 P0 / 0 P1 / ~27 P2*
