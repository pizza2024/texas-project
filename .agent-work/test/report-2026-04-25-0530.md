# Test Report — 第100轮

> 更新时间: 2026-04-25 05:30

## 状态

| 状态 | P0   | P1  | P2  |
| ---- | ---- | --- | --- |
| 数量 | 0 🟢 | 5   | 0   |

## HEAD Commit

`b24eec7` — 2026-04-25 03:17（无新 commit，13分钟前）

---

## 一、CodeReview 发现

### P0 全部已修复 ✅

第97轮发现的 6个 P0 问题持续保持已修复状态，本轮 diff 验证：

| ID              | 问题                                   | 验证                                                                       | 文件                                 |
| --------------- | -------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------ |
| P0-GAME-001     | `buildPots()` 边池分配排除 all-in 玩家 | ✅ `p.status === PlayerStatus.ACTIVE \|\| p.status === PlayerStatus.ALLIN` | table-round.ts:265-271               |
| P0-GAME-002     | `resolveFoldWin()` rake 未从 pot 扣减  | ✅ `this.table.pot -= rake` 先扣再分配                                     | table-round.ts:300-306               |
| P0-GAME-003     | `bestHandFrom()` 5张牌早退返回非最优   | ✅ `all.length === 5` 早退路径 + 防御性注释                                | hand-evaluator.ts:209-214            |
| P0-Withdraw-001 | txHash 在链上确认前保存（双花）        | ✅ txHash 保存移至 `tx.wait(1)` 之后                                       | withdraw.service.ts:552-565          |
| P0-Withdraw-002 | `processWithdraw` 无幂等保护           | ✅ `$transaction` + `CONFIRMED` 幂等检查                                   | withdraw.service.ts:390-410, 505-515 |
| P0-Deposit-001  | `checkAddressDeposits` 非原子操作      | ✅ 每事件 `$transaction` 原子化                                            | deposit.service.ts                   |

### 新发现

**无新的 P0/P1/P2 发现。**

### 本轮 diff 验证

`b24eec7` HEAD~3 → HEAD 关键变更（已验证正确）：

**table-engine（game fixes）**

- `hand-evaluator.ts`: `all.length === 5` 早退路径，防止 5张公牌时 combo 循环错误
- `table-round.ts`: `this.table.pot -= rake` 先扣 rake 再算 winAmount；`eligiblePlayerIds` 显式包含 `ACTIVE|ALLIN`

**withdraw（double-spend + idempotency）**

- `processWithdraw`: `$transaction` 包裹 + PENDING 状态重检
- `executeChainWithdraw`: txHash 保存延后至 `tx.wait(1)` 之后 + CONFIRMED 幂等跳过

**rakeback（new feature）**

- `rakeback.service.ts`: 新增 `getRakeback()` API
- `rakeback.module.ts`: 新增 `RakebackController`
- `lobby-header.tsx`: 新增 Rakeback 按钮（sm+ 屏幕可见）

**websocket (type fix)**

- `app.gateway.ts`: `handlePlayerAction` 参数类型从 `unknown` 收紧为联合类型（check/fold/call/raise/allin/straddle/sit-out）

### P1 进行中问题 — 状态确认

| ID     | 问题                                         | 状态              | 本轮确认                                                                          |
| ------ | -------------------------------------------- | ----------------- | --------------------------------------------------------------------------------- |
| P1-001 | `TableManager.getTable()` 并发竞态           | 🔴 待 Coding 修复 | `pendingGetTable` Map 已存在（lines 107-165），防止重复创建。测试未覆盖并发场景。 |
| P1-002 | Redis 不可用时 rate limit 回退 in-memory Map | 🔴 待 Coding 修复 | `connection-state.service.ts:46` 确认存在fallback逻辑                             |
| P1-003 | 密码暴力破解保护 in-memory Map               | 🔴 待 Coding 修复 | `connection-state.service.ts:17` 确认存在 `passwordAttempts` Map                  |
| P1-004 | Jest Worker 泄漏                             | 🟡 监控中         | `--detectOpenHandles` 已启用                                                      |
| P1-005 | `persistSettlementRecords` 错误被静默吞掉    | 🟡 需加监控       | `table-manager.service.ts:354-356` — catch 后仅 logger.error，无告警              |

**P1-001 并发分析补充：**
`pendingGetTable` 使用 `Map<string, Promise<Table>>` deduplicate 并发请求，但：

- `finally()` 清理正确处理了 reject 路径
- 测试套件未覆盖两请求同时 `getTable(roomId)` 的并发场景
- 建议 Coding 补充并发单元测试（模拟两个 `getTable()` 并发调用同一 roomId）

**P1-002/003 风险说明：**
两者都使用单实例 in-memory Map，在多实例部署（K8s 多 pod）时无法跨实例共享。但作为 Redis 回退fallback，其设计符合"Redis 不可用时不完全停机"的降级策略。真正风险在于多 pod 部署时的 bypass 可能性。

---

## 二、测试执行结果

```
Test Suites: 24 passed, 24 total
Tests:       238 passed, 238 total
Time:        1.943 s
```

**测试质量**：238 tests passing，24 suites，覆盖：

- ✅ table-engine (9 suites): hand-evaluator, table-round, sit-out, straddle, all-in, table-game-logic, table-manager, sit-out-auto-action
- ✅ websocket: timer-debug
- ✅ auth, user, room, deposit, withdraw, rakeback, admin, club, prisma

---

## 三、项目体验

### 新功能体验

**Rakeback MVP 已上线：**

- 后端：`GET /user/rakeback` + `POST /user/rakeback/claim`
- 前端：`/rakeback` 页面已实现，`LobbyHeader` 新增 Rakeback 按钮
- 等级体系：BRONZE/SILVER/GOLD 三档，rakeback 率 5%/10%/15%
- 新增 `RakebackController` 注册至 `RakebackModule`

### 文档与测试覆盖

- ✅ AGENTS.md 完整
- ✅ docker-compose 本地开发环境完整
- ✅ Turborepo monorepo 结构清晰
- ✅ 盲注结构 (BLIND_TIERS)、tier-based rake 配置完整

### 遗留体验问题

| 问题                            | 优先级 | 说明                              |
| ------------------------------- | ------ | --------------------------------- |
| Club 数据库迁移未执行           | P2     | task-queue 中 P2-002 标注"待执行" |
| Phase 1 Tournament (SNG) 规划中 | P3     | Productor roadmap                 |

---

## 四、任务队列更新建议

### 无变化

P0 全部清零，P1/P2 队列维持不变。

### 建议观察

| 建议                            | 理由                                                         |
| ------------------------------- | ------------------------------------------------------------ |
| P1-001 补充并发 getTable() 测试 | 当前仅测恢复场景，未覆盖并发创建 table 场景                  |
| P1-005 增加监控告警             | `persistSettlementRecords` 静默失败 → 财务记录丢失但无人知晓 |

---

## 五、下轮行动

1. 等待 Coding 第100轮处理 P1-001/002/003
2. 推进 Phase 1 Tournament PRD（Productor 主导）
3. Club 数据库迁移（P2-002）待手动执行

---

_Test 第100轮 — 238 tests pass，P0 全清，遗留 5个 P1 均为已知问题_
