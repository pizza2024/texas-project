# Test Report — r95

**时间:** 2026-05-04 15:15
**HEAD:** `3e503eb` ✅ 已同步（develop 分支）
**分支:** develop

---

## 状态

- **P0/P1:** ⚠️ 新增 3 P0 + 4 P1（WebSocket/Redis 跨实例一致性）
- **单元测试:** 未运行（定时任务，仅 CodeReview）
- **ESLint/TS:** 参考 r94 — backend 0 warnings + web 0 warnings

---

## CodeReview 发现（本次）

### P0 — 跨实例一致性（Critical）

| ID | 文件 | 问题 | 推荐修复 |
|----|------|------|---------|
| P0-WS-001 | `app.gateway.ts:343–372` | `userSockets` 索引在 `fetchSockets` 循环后才更新新 socket，崩溃时索引不一致 | 在 evict 循环**前**添加新 socket 到 `userSockets` |
| P0-WS-002 | `websocket-manager.ts:31–75` | `subscribe`（字符串匹配）而非 `psubscribe`（模式匹配），跨实例广播静默失败无报错 | 改用 `psubscribe` 或添加订阅确认守卫 |
| P0-WS-003 | `websocket-manager.ts:117–130` | `emitToUser` 本地投递无原子性保证，Redis 未就绪时远程实例用户收不到消息 | 本地投递前检查 Redis 状态，或实现写穿机制 |

### P1 — 逻辑 Bug / 竞态条件

| ID | 文件 | 问题 | 推荐修复 |
|----|------|------|---------|
| P1-WS-001 | `app.gateway.ts:324` | `clearPendingDisconnect` 在 evict 旧 socket **前**调用，旧 socket 的 disconnect cleanup 可能与新连接竞争 | 移至 evict 循环**后** |
| P1-ROOM-001 | `room.service.ts:15–35` | `createRoom` 并发时 DB unique 约束拒绝插入，但 `roomEvents.emit` 对被拒绝记录仍触发 | 包裹事件触发在 transaction 内或原子化 |
| P1-MATCH-001 | `game.handler.ts:429–469` | `handleQuickMatch` 缺少 `withUserLock`，并发调用可绕过 `currentRoomId` 检查重复入桌 | 添加 `withUserLock(userId)` |
| P1-WS-002 | `app.gateway.ts:495–545` | `notifyFriendsOfStatusChange` 中 `Promise.all` 内单个通知失败不记录日志 | 添加 `.catch()` 或拆分逐个 `await` |
| P1-REDIS-001 | `redis.service.ts:67–83` | `set/del` 静默吞掉错误，`login` 时 Redis 失败仍返回有效 JWT | session 写操作失败应返回错误而非静默继续 |

---

## 规格就绪状态（来自 Productor r454）

| ID | 规格 | 状态 | 备注 |
|----|------|------|------|
| P2-INSURANCE-001 | All-In Insurance | ✅ 规格就绪 | `P2-INSURANCE-001-spec.md` |
| P2-BADBEAT-001 | Bad Beat Jackpot | ✅ 规格就绪 | `P2-BADBEAT-001-spec.md` |
| P2-FAST-FOLD | Fast-Fold | ✅ 规格就绪 | `P2-FAST-FOLD-spec.md` |

**建议 Coding 下一轮：** 三项并行开发（Insurance / BadBeat / Fast-Fold）

---

## 优先级任务队列

### P0 — 立即修复（WebSocket 跨实例一致性）

| 优先级 | ID | 任务 | 状态 |
|--------|----|------|------|
| P0 | P0-WS-001 | `userSockets` 索引更新顺序修复 | 📋 待认领 |
| P0 | P0-WS-002 | Redis `subscribe` → `psubscribe` + 订阅确认 | 📋 待认领 |
| P0 | P0-WS-003 | `emitToUser` 本地/远程投递一致性 | 📋 待认领 |

### P1 — 下一迭代

| 优先级 | ID | 任务 | 状态 |
|--------|----|------|------|
| P1 | P1-WS-001 | `clearPendingDisconnect` 调用顺序修复 | 📋 待认领 |
| P1 | P1-ROOM-001 | `createRoom` 事件触发原子化 | 📋 待认领 |
| P1 | P1-MATCH-001 | `handleQuickMatch` 添加 `withUserLock` | 📋 待认领 |
| P1 | P1-WS-002 | `notifyFriendsOfStatusChange` 错误日志 | 📋 待认领 |
| P1 | P1-REDIS-001 | Redis session 写失败返回错误 | 📋 待认领 |

### P2 — 后续迭代（规格已就绪）

| 优先级 | ID | 任务 | 状态 |
|--------|----|------|------|
| P2 | P2-INSURANCE-001 | All-In Insurance 实施 | 📋 待认领 |
| P2 | P2-BADBEAT-001 | Bad Beat Jackpot 实施 | 📋 待认领 |
| P2 | P2-FAST-FOLD | Fast-Fold 实施 | 📋 待认领 |

---

## 备注

- P0 问题均为**多实例部署**场景下触发的跨实例一致性问题，单实例部署不受影响
- 但 `P0-WS-001`（`userSockets` 索引顺序）即使单实例也可能因并发连接产生竞态
- Test r95 HEAD `3e503eb` 与 Productor r454 HEAD `4bafde3` 差 1 commit（task-queue 同步）

---

_Generated by Test Agent r95 — 2026-05-04 15:15_
