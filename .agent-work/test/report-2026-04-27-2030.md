# Test Report — 第259轮

**时间:** 2026-04-27 21:30
**HEAD:** `efae962` — fix: delete diagnostic files + reset aggregate mock in deposit E2E spec
**系统:** npm test --workspace=backend → 28 suites / 323 tests ✅

---

## 状态

| 优先级 | 数量 | 变化                                       |
| ------ | ---- | ------------------------------------------ |
| P0     | 0    | unchanged                                  |
| P1     | 0    | unchanged                                  |
| P2     | ~9   | -2 (P2-DEPOSIT-TOCTOU x2 resolved by HEAD) |

---

## 一、CodeReview 发现

### 本轮变更扫描（HEAD~5 → HEAD）

**Backend 变更（4 files）:**

- `deposit.e2e.spec.ts` — `mockReset()` + `mockResolvedValue` 修复
- `deposit.service.ts` — `$transaction` + `skipDuplicates` + `aggregate` in tx
- `game.handler.ts` — P1-CHAT-INJECTION 修复：DB lookup 替代 JWT payload username
- `withdraw.service.spec.ts` — lint fix

**Web 变更（2 files）:**

- `ChatPanel.tsx` — 删除重复文件（dedup）
- `room/[id]/page.tsx` — sessionStorage password cleanup

### 已验证 — 无新问题

| 检查项                 | 状态                                                                               |
| ---------------------- | ---------------------------------------------------------------------------------- |
| P1-CHAT-INJECTION 修复 | ✅ `88af108` — DB `userService.user({ id: userId })` lookup                        |
| P2-DEPOSIT-TOCTOU 修复 | ✅ `31f4bba` — `$transaction` + `skipDuplicates` + `aggregate` in tx               |
| P2-DEPOSIT-DIAG-FILES  | ✅ `efae962` — `debug-deposit.spec.ts` + `diag.spec.ts` 已删除                     |
| P2-DEPOSIT-E2E-MOCK    | ✅ `efae962` — `aggregate.mockReset()` 添加                                        |
| P2-CHAT-DUP            | ✅ `c7c9be8` — `ChatPanel.tsx` 已删除，统一用 `@/components/chat/ChatPanel`        |
| P2-ROOM-PASSWORD       | ✅ `c7c9be8` — `sessionStorage.removeItem` 在 join 后执行                          |
| WebSocket 鉴权         | ✅ `handleConnection` 正确验证 JWT + sessionId + single-device                     |
| 游戏逻辑完整性         | ✅ table-engine `pendingGetTable` deduplication + `cleanupOfflineResidueOnStartup` |
| 钱包原子性             | ✅ `freezeBalance`/`setBalance`/`resetBalanceAndUnfreeze` 均用 `$transaction`      |

### P2 — 观察项（无需立即行动）

#### 1. WithdrawService 内存回退 — In-Memory Cooldown Map

**文件:** `apps/backend/src/withdraw/withdraw.service.ts:51`

```typescript
private readonly cooldowns = new Map<string, number>();
```

**说明:** 当 Redis 不可用时，用 `Map` 跟踪 cooldown。这是已文档化的设计决策，仅适用于单实例 dev/test。生产环境依赖 Redis 分布式锁。
**风险:** 低 — 有明确注释和 Redis 主路径

#### 2. Web 端测试覆盖 — 0 个 spec 文件

**文件:** `apps/web/` 无任何 `.spec.ts` / `.test.ts`
**状态:** P2-WEB-SPEC — 仍在任务队列中

---

## 二、测试覆盖扫描

### Backend 测试套件 ✅

```
28 suites / 323 tests — 全部通过
测试文件: auth (3), room (2), user (2), table-engine (7), websocket (4),
         deposit (2), withdraw (2), rakeback (2), mission (2), club (1),
         tournament (1), prisma (1), app (1)
```

**缺失覆盖:** matchmaking, friend, wallet（全模块）

### Web 测试套件

```
0 个测试文件 — P2-WEB-SPEC 待实施
```

---

## 三、P2 遗留清单（本轮更新后）

| ID                  | 描述                                  | 状态      | 备注                                    |
| ------------------- | ------------------------------------- | --------- | --------------------------------------- |
| P2-WEB-SPEC         | Web 端 0 个测试文件                   | 🔍 待实施 | AuthProvider/ActionBar/ChatPanel 无覆盖 |
| P2-TOURNAMENT-SPEC  | Tournament 模块仅 1 个 spec           | 🔍 待实施 | Matchmaking/wallet/friend 同缺失        |
| P2-CHAT-IDEMPOTENCY | 聊天消息无幂等键                      | 🔍 待认领 | `game.handler.ts`                       |
| P2-WS-RATE-UNIT     | PASSWORD_ATTEMPT_WINDOW_MS 单位混淆   | 🔍 待认领 | `connection-state.service.ts`           |
| P2-CODE-PATTERN     | mission/table-engine Promise.all 优化 | 🔍 待认领 | —                                       |
| P2-CHAT-STUB        | `handleClaim` 无实际 API 调用         | 🔍 待认领 | `missions/page.tsx`                     |
| P2-ROUTER-ANY       | `router.events` 使用 `as any`         | 🔍 待认领 | `rooms/page.tsx`                        |
| P2-DEPOSIT-TOCTOU   | 诊断文件 + E2E mock                   | ✅ 已修复 | `efae962`                               |
| P2-CHAT-DUP         | ChatPanel 重复文件                    | ✅ 已修复 | `c7c9be8`                               |
| P2-ROOM-PASSWORD    | sessionStorage 明文密码               | ✅ 已优化 | `c7c9be8` — join 后删除                 |

---

## 四、Productor 协调

- Productor 第258轮：0 P0 / 0 P1 / 11 P2（含本轮已解决的2个 P2-DEPOSIT-TOCTOU）
- 竞品 GGPoker SPINS 调研已记录（P1-BLAST-001 规格待定义）
- 本轮 Test 无与 Productor 输出重复

---

## 五、任务队列更新

| 优先级 | ID                  | 任务                   | 行动                                                                   |
| ------ | ------------------- | ---------------------- | ---------------------------------------------------------------------- |
| P2     | P2-WEB-SPEC         | Web 测试框架           | 在 `apps/web` 建立 jest 配置 + 至少 1 个组件测试                       |
| P2     | P2-TOURNAMENT-SPEC  | Tournament 测试覆盖    | Matchmaking/wallet/friend 模块补充 spec                                |
| P2     | P2-CHAT-IDEMPOTENCY | 聊天消息加幂等键       | `game.handler.ts` — `crypto.randomUUID()` 已在用，可考虑加 Redis dedup |
| P2     | P2-WS-RATE-UNIT     | 确认时间单位           | `connection-state.service.ts` 常量注释澄清                             |
| P2     | P2-CHAT-STUB        | `handleClaim` API 调用 | missions/page.tsx — 确认是否需要后端接口                               |

---

## 六、系统健康总结

| 维度            | 状态      | 备注                                                  |
| --------------- | --------- | ----------------------------------------------------- |
| 编译            | ✅        | 28 suites 全部通过                                    |
| P0/P1           | ✅ 清零   | 无阻塞或重要缺陷                                      |
| 诊断文件        | ✅ 已清理 | debug-deposit.spec.ts + diag.spec.ts 已删除           |
| Web 测试        | ⚠️ 0 文件 | P2-WEB-SPEC 持续延期                                  |
| Tournament 覆盖 | ⚠️ 1 spec | P2-TOURNAMENT-SPEC 待实施                             |
| 安全修复        | ✅        | P1-CHAT-INJECTION (DB lookup), P2-DEPOSIT-TOCTOU (tx) |

---

_Test 第259轮 — 2026-04-27 21:30 — 0 P0 / 0 P1 / ~9 P2 — 系统稳定_
