# Test Report — 第151轮

> 生成时间: 2026-04-25 18:00

## 状态

| 类型 | 数量  |
| ---- | ----- |
| P0   | 0 🔴  |
| P1   | 8 🟡  |
| P2   | 16 🟢 |

## HEAD Commit

`983b85f` — fix: 3 P0 issues — resolveFoldWin bestCards, straddle calledAllIn reset, handId undefined

距上一轮 **13 分钟**，P0 修复已合并。270 tests pass，TS 编译干净。

---

## CodeReview 验证结果

### P0 修复验证 ✅

**P0-BACKEND-001** `resolveFoldWin()` bestCards — ✅ 已验证

```ts
// table-round.ts:317
bestCards: [],
```

**P0-BACKEND-002** Straddle `calledAllIn` 重置 — ✅ 已验证

```ts
// table-round.ts:60
attemptStraddle(playerId: string): boolean {
  // Straddle resets calledAllIn to allow raises after straddle (like any new street)
  this.table.calledAllIn = null;
```

**P0-BACKEND-003** `persistSettlementRecords` catch 中 `handId` — ✅ 已验证

```ts
// table-manager.service.ts:293
let handId: string | undefined = undefined;
```

---

### P1 问题状态确认

| ID             | 问题                                       | 文件                             | 状态      |
| -------------- | ------------------------------------------ | -------------------------------- | --------- |
| P1-WEB-001     | ReplayModal fetch 缺少 Authorization Token | ReplayModal.tsx:74               | 🟡 待修复 |
| P1-WEB-004     | AutoPlayPanel useEffect 闭包不稳定         | AutoPlayPanel.tsx:45–57          | 🟡 待修复 |
| P1-BACKEND-001 | netProfit 计算用 `pa.bet` 而非 `totalBet`  | hand-history.service.ts:135      | 🟡 待修复 |
| P1-BACKEND-003 | WS player-action `roomId=null` 跳过校验    | game.handler.ts:278–284          | 🟡 待修复 |
| P1-BACKEND-004 | getUserAvailableBalance 返回 null 静默用 0 | game.handler.ts:145–150          | 🟡 待修复 |
| P1-WEB-002     | AllInConfirmModal 重复组件                 | components/AllInConfirmModal.tsx | 🟡 死代码 |
| P1-WEB-003     | SocketSessionProvider 重复注册 handler     | socket-session-provider.tsx:28   | 🟡 待修复 |
| P1-BACKEND-002 | 3次超时后无视觉反馈                        | table-player-ops.ts:189–218      | 🟡 待修复 |

**P1-WEB-001 深入确认**（本轮重点）:

```ts
// ReplayModal.tsx:74 — fetch 缺少 Authorization header
const res = await fetch(`/api/table-engine/hands/${handId}/replay`);
// 未携带 cookie/JWT，认证用户也无法加载私有手牌
```

**P1-BACKEND-003 深入确认**（本轮重点）:

```ts
// game.handler.ts:278-284
if (
  validated.roomId &&          // 只有客户端显式传 roomId 才校验
  userCurrentRoomId != null &&
  validated.roomId !== userCurrentRoomId
) { return { event: 'error', ... }; }
```

当 `validated.roomId === null` 时（schema 允许），安全校验被完全跳过。如果 `getUserCurrentRoomId()` 返回错误值，攻击者可向任意 roomId 发送操作。

**P1-BACKEND-004** 确认：`balance ?? 0` 在 line 147 是有意设计（用于显示），但问题在于 `getUserAvailableBalance` 返回 `null` 时静默用 0 而不是拒绝入场。

---

### 新代码扫描

本轮新变更（commit 983b85f）：

- `table-manager.service.ts` +1 line（handId undefined 初始化）
- `table-round.ts` +3 lines（calledAllIn reset + bestCards）

无新增问题。

---

## 测试状态

- ✅ **270 tests pass** — 全量通过
- ✅ **TS 编译干净** — backend + web 均为 0 errors
- ✅ **P0 修复已验证** — 3个 P0 全部正确修复

---

## 跨代理状态

| 代理      | 状态                    | 备注                               |
| --------- | ----------------------- | ---------------------------------- |
| Coding    | 🟡 修复 P0 后等待新任务 | P0 已清零，可推进 P1 实施          |
| Test      | ✅ 本轮完成             | P0 清零，8 P1 待处理               |
| Productor | 🟢 规格已交付           | CHAT-001 + TOURNAMENT-001 规格就绪 |

---

## 优先级建议

### P0：已清零 ✅

### P1 建议处理顺序

1. **P1-BACKEND-003** — WebSocket roomId 校验绕过（安全性）
2. **P1-WEB-001** — ReplayModal 缺少认证 Token（功能可用性）
3. **P1-BACKEND-001** — netProfit 计算错误（数据准确性）
4. **P1-BACKEND-004** — getUserAvailableBalance null 处理（边界情况）
5. **P1-WEB-003** — SocketSessionProvider 重复注册（内存泄漏风险）
6. **P1-WEB-004** — AutoPlayPanel 闭包问题（功能正确性）
7. **P1-WEB-002** — AllInConfirmModal 死代码（清理）
8. **P1-BACKEND-002** — 超时无视觉反馈（用户体验）

### P2：保持现状

_Test 第151轮 — 2026-04-25 18:00 — P0 已清零，8 P1 待修复，270 tests pass_
