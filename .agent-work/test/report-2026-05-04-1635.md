# Test Report — r100

**时间:** 2026-05-04 16:35
**HEAD:** `ea88a76` (develop, ahead of origin/develop by 1 commit)
**分支:** develop

---

## 状态

- **P0/P1:** ⚠️ **1 个 TS 错误阻塞测试文件编译（app.gateway.spec.ts 未更新）**
- **新发现问题:** 1 TS 错误（测试文件 constructor 参数遗漏）
- **Backend TS:** ❌ 1 error (spec file only)
- **Web TS:** ✅ 0 errors
- **ESLint:** ⚠️ ESLint 9 config missing (无配置冲突)
- **本轮新增:** 1 TS 错误

---

## CodeReview — P0 阻塞问题

| ID | 文件 | 问题 | 修复方案 |
|----|------|------|---------|
| P0-TEST-001 | `app.gateway.spec.ts:84` | `new AppGateway(...)` 传入 15 个参数，但 `ea88a76` 后的 constructor 需要 16 个（新增 `InsuranceService`） | 添加 `insuranceService: {} as any` mock 参数 |

---

## 验证 — 上一轮报告的 P0 已全部修复

| ID | 状态 | 备注 |
|----|------|------|
| P0-INS-001 | ✅ 已修复 | `hasPurchasedInsurance: false` 已存在于 `table-player-ops.ts:53` |
| P0-INS-002 | ✅ 已修复 | `InsuranceService` 已注入 `AppGateway` constructor (`app.gateway.ts:122`) |
| P0-WS-001/002/003 | ✅ 已修复 | WebSocket P0 全清零 |
| P1-WS-001/WS-002/MATCH-001/REDIS-001/ROOM-001 | ✅ 已修复 | P1 全部清零 |

**注意：** 上一轮报告 r99 中的 P0-INS-001 和 P0-INS-002 已被 Coding 在 `ea88a76` 提交中修复（`+1 line` in `table-player-ops.ts` 确认 `hasPurchasedInsurance: false` 已存在）。当前唯一的 P0 是测试文件的 mock 参数遗漏。

---

## 已验证 — Insurance Phase 1 代码审查

| 模块 | 文件 | 状态 |
|------|------|------|
| InsuranceService | `insurance.service.ts` | ✅ 存在，导出正确 |
| InsuranceModule | `insurance.module.ts` | ✅ 正确导入 PrismaModule + WalletModule |
| Insurance Calculator | `insurance-calculator.ts` | ✅ 存在于 table-engine/ |
| handleBuyInsurance | `game.handler.ts:665` | ✅ 存在 |
| handleSkipInsurance | `game.handler.ts:779` | ✅ 存在 |
| WS 注册 | `app.gateway.ts:50` | ✅ handleSkipInsurance 已导入 |
| AppModule 集成 | `app.module.ts` | ✅ InsuranceModule 已添加 |
| WebsocketModule 集成 | `websocket.module.ts` | ✅ InsuranceModule 已添加 |

**Phase 1 后端代码完整性验证通过。**

---

## P0 — 修复方案

在 `app.gateway.spec.ts` 的 `new AppGateway(...)` 调用中，添加 `insuranceService` mock：

```typescript
// 在 connectionState mock 之后、broadcastService mock 之前添加：
{
  // ... existing connectionState mock ...
} as any,
{
  // 新增：insuranceService mock
  calculateInsuranceFee: jest.fn(),
  calculateInsurancePayout: jest.fn(),
  recordInsurancePurchase: jest.fn().mockResolvedValue(undefined),
  settleInsurance: jest.fn().mockResolvedValue(undefined),
  offerInsurance: jest.fn(),
} as any,
broadcastService as any,
```

---

## 已就绪规格状态

| 规格 | 状态 | 备注 |
|------|------|------|
| P2-INSURANCE-001 (All-In Insurance) | 🔄 Phase 1 完成，待 Phase 2 前端集成 | 后端完成，spec 修复后测试可跑 |
| P2-BADBEAT-001 (Bad Beat Jackpot) | ✅ 规格就绪 | 373行规格文档存在 |
| P2-FAST-FOLD (Fast-Fold) | ✅ 规格就绪 | 261行规格文档存在 |
| P2-SOCIAL-001 (观战系统) | ⏳ 待启动 | Productor 待输出规格 |
| P2-UX-004 (表情飞行轨迹) | ⏳ 待启动 | Productor 待输出规格 |

---

## P2 — 待办任务队列

| 优先级 | ID | 任务 | 状态 |
|--------|----|------|------|
| P0 | P0-TEST-001 | 修复 `app.gateway.spec.ts` constructor 参数（添加 InsuranceService mock） | 🔴 待修复 |
| P1 | P1-INSURANCE-001 | Insurance Phase 2：前端集成 + table-engine 集成 | 🔴 待实施 |
| P1 | P1-PR-001 | 将 `ea88a76` (`feat/insurance`) 合并至 `origin/develop` | 🔴 待合并 |
| P2 | P2-BADBEAT-001 | 实施 Bad Beat Jackpot | 📋 待认领 |
| P2 | P2-FAST-FOLD | 实施 Fast-Fold | 📋 待认领 |
| P2 | P2-SOCIAL-001 | 实施观战系统（待 Productor 输出规格） | 📋 待规格 |

---

## 备注

- Coding r437 (`ea88a76`) 完成 Insurance Phase 1，后端代码已验证完整
- 当前阻塞：测试文件 `app.gateway.spec.ts` 的 constructor mock 遗漏了新增的 `InsuranceService` 参数
- Web 端 TS 编译无错误，backend 业务代码（除 spec 外）编译通过
- `feat/insurance` 分支（`ea88a76`）领先 `origin/develop` 1 commit，建议合并

---

_Test r100 — 2026-05-04 16:35 — Insurance Phase 1 后端验证通过；1 P0（spec.ts mock 遗漏）；WebSocket P0/P1 全清；P2 规格就绪待实施_
