# Test Report — 第229轮

**时间:** 2026-04-26 13:31

## 状态

| 类型 | 数量 |
|------|------|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |

## HEAD Commit

`84bf984` — feat(web): deposit page add first-deposit bonus status card

> **无新提交。** 本轮为纯巡查轮次。

## 系统健康

- 测试套件: 27 passed, 294 tests [OK]
- Backend TypeScript: 0 错误 [OK]
- Web TypeScript: 0 错误 [OK]
- Backend Build: `nest build` + `prisma generate` [OK]
- Web Build: 18 routes, 0 错误 [OK]
- 系统状态: 完全清洁

---

## CodeReview 发现

### 确认修复: P2-SCHEDULE-ID — `generateId()` 已升级

**状态:** ✅ 已修复（本轮确认）

`tournament-schedule.service.ts:612` 现在使用 `crypto.randomUUID()`，替换了之前的 `Date.now() + Math.random()`。无需进一步操作。

### 例行巡查: 核心模块扫描

| 模块 | 文件 | 结论 |
|------|------|------|
| `websocket/validate.ts` | 39行 | ✅ Zod schema 验证，错误时 emit `invalid_message`，日志记录完整 |
| `websocket/connection-state.service.ts` | 246行 | ✅ Redis fail-closed（rate-limit/brute-force），安全设计正确 |
| `auth/auth.service.ts` | 353行 | ✅ bcrypt(10) 密码哈希，UUID session，OTP 限速(1次/60s)，最大5次尝试验证 |
| `table-engine/table.ts` | 455行 | ✅ `isFoldWin`/`foldWinnerRevealed` 字段已存在 |
| `tournament-schedule.service.ts` | ~654行 | ✅ `generateId()` 已使用 `crypto.randomUUID()` |
| `mission.service.ts` | 独立模块 | ✅ mission.service.spec.ts 仍然缺失（追踪中，见下方） |
| `club.service.ts` | 独立模块 | ✅ club.service.spec.ts 存在，spec 文件完整 |

### 本轮新发现

**无新发现。** 所有模块例行巡查通过。

---

## 测试体验

### Web 测试覆盖 — 持续 0 文件

**问题:** Web 端（Next.js）仍然没有任何 `*.spec.ts` 或 `*.test.ts` 文件。

建议优先级顺序：
1. `lib/auth.ts` — Token 解析、过期检测、存储逻辑
2. `lib/api.ts` — Fetcher 封装、错误处理
3. `lib/socket.ts` — Socket 客户端事件订阅
4. `components/SocketSessionProvider.tsx` — Context 状态流转

### mission.service.spec.ts — 缺失（持续追踪）

自第207轮起已追踪，至今未创建。mission.service.ts 存在，但无对应 spec 文件。

---

## 优先级任务队列

### P2 — 待认领

| ID | 任务 | 来源 | 状态 |
|----|------|------|------|
| P2-WEB-TESTS | Web 端补充基础单元测试 | 多轮追踪 | 待认领 |
| P2-MISSION-SPEC | `mission.service.spec.ts` 缺失 | 第207轮追踪 | 待认领 |

---

## 系统健康总览

| 代理 | 轮次 | 测试 | P0 | P1 | P2 |
|------|------|------|----|----|-----|
| Coding | 228 | 294 pass | 0 | 0 | 0 |
| Test | 229 | 294 pass | 0 | 0 | 0 |
| Productor | 228 | — | 0 | 0 | 0 |

**结论: 系统完全清洁，无 P0/P1 问题。**

---

*Test 第229轮 — 0 P0 / 0 P1 / 0 P2*
