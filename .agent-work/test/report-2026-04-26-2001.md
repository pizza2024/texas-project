# Test Report — 第256轮

**时间:** 2026-04-26 20:01
**HEAD:** `88af108` — fix: P1-CHAT-INJECTION — DB lookup for username in chat handler

## 状态

| 优先级 | 数量 | 变化      |
| ------ | ---- | --------- |
| P0     | 0    | unchanged |
| P1     | 0    | unchanged |
| P2     | 11   | unchanged |

## 系统健康

| 指标            | 状态                          |
| --------------- | ----------------------------- |
| TypeScript 编译 | ✅ 通过                       |
| 后端测试        | ✅ 28 suites / 323 tests PASS |
| HEAD 变更       | 无新提交（与上一轮相同）      |

---

## CodeReview 扫描

### 本轮重点：Chat Handler 修复验证 + 新增深度扫描

**1. P1-CHAT-INJECTION 修复验证 ✅**

commit `88af108` 已正确实施：

```ts
// game.handler.ts:556-558
// Look up authoritative username from DB — never trust JWT payload alone
const dbUser = await gateway.userService.user({ id: userId });
const username = dbUser?.username ?? "未知玩家";
gateway.server.to(roomId).emit("chat-message", {
  id: crypto.randomUUID(), // ✅ 幂等 ID
  userId,
  username, // ✅ DB 权威来源
  content: trimmed, // ✅ 已 trim + 长度校验
  timestamp: Date.now(),
});
```

**验证点全部通过：**

- ✅ `client.data.user.sub` 仅用于 userId 查询，不参与 username 广播
- ✅ DB lookup 防止 JWT payload 欺骗（`username` 字段可被客户端伪造）
- ✅ `crypto.randomUUID()` 解决聊天消息幂等性（隐式 P2-CHAT-IDEMPOTENCY 部分修复）
- ✅ 200 字符长度限制（sanitization）
- ✅ trim() 防止空白消息
- ✅ 房间成员验证（`currentRoomId !== roomId` 拒绝）
- ✅ Chat rate limit（5 秒冷却）

**2. WebSocket 安全全景 — 无新发现问题**

扫描 `apps/backend/src/websocket/` 所有 handler：

| Handler               | userId 来源                  | 风险评估                    |
| --------------------- | ---------------------------- | --------------------------- |
| `handleJoinRoom`      | `client.data.user.sub` (JWT) | ✅ 房间逻辑基于 DB 余额     |
| `handlePlayerReady`   | `client.data.user.sub` (JWT) | ✅ DB 房间成员验证          |
| `handleChatMessage`   | `client.data.user.sub` (JWT) | ✅ **已修复**               |
| `handleLeaveRoom`     | `client.data.user.sub` (JWT) | ✅ 房间成员验证             |
| `handleShowCards`     | `client.data.user.sub` (JWT) | ✅ 仅 DB 中存在的玩家可亮牌 |
| `handleEmojiReaction` | `client.data.user.sub` (JWT) | ✅ 独立 rate limit          |

**3. table-engine 深度扫描**

扫描 `Math.random()`（伪随机）、`eval`/`new Function`/`JSON.parse`（动态代码）：

| 文件                       | 发现                                    | 风险                        |
| -------------------------- | --------------------------------------- | --------------------------- |
| `hand-evaluator.ts`        | `evaluate5`/`evaluateHand` — 纯算法     | ✅ 无问题                   |
| `table-manager.service.ts` | `JSON.parse(snapshot)` 反序列化牌桌状态 | ✅ 可信来源（自身存储）     |
| `hand-history.service.ts`  | `JSON.parse(hand.table.stateSnapshot)`  | ✅ 可信来源（DB）           |
| `club.service.ts`          | `JSON.parse(cached)`                    | ✅ 需注意缓存注入风险（低） |
| `auth.service.ts`          | `JSON.parse(stored)`                    | ✅ session 存储，可信       |
| `admin.service.ts`         | `JSON.parse(cached)`                    | ✅ admin 数据，低风险       |

**无 Math.random() 用于关键安全操作** ✅
**无 eval/new Function** ✅

---

## P2 遗留问题跟踪

| ID                           | 问题                                 | 状态        | 备注                                                              |
| ---------------------------- | ------------------------------------ | ----------- | ----------------------------------------------------------------- |
| P2-DEPOSIT-TOCTOU            | `getOrCreateDepositAddress` 索引竞争 | 🔍 待认领   | HD wallet index 并发                                              |
| P2-CHAT-IDEMPOTENCY          | 聊天消息无幂等键                     | ✅ 部分修复 | `crypto.randomUUID()` 已加，但 message deduplication 客户端未处理 |
| P2-ROOM-PASSWORD             | 房间密码明文存 sessionStorage        | 🔍 待认领   | 安全建议：仅存 hash                                               |
| P2-WS-RATE-UNIT              | WebSocket 限速单位混淆               | 🔍 待认领   | 连接级 vs 用户级                                                  |
| P2-ROOM-RETRY                | 重试逻辑无指数退避                   | 🔍 待认领   | 房间操作重试                                                      |
| P2-TOURNAMENT-SPEC           | Tournament 测试覆盖严重不足          | 🔍 待实施   | 高优先级功能                                                      |
| P2-WEB-SPEC                  | Web 端 0 测试文件                    | 🔍 待实施   |                                                                   |
| P2-MOBILE-SPEC               | Mobile 端 0 测试文件                 | 🔍 待实施   |                                                                   |
| P2-CHAT-FRONTEND-TEST        | ChatPanel WS 组件测试                | 🔍 待实施   |                                                                   |
| P2-JWT-LOCALSTORAGE-FRONTEND | Web socket.io 迁移 httpOnly cookie   | 🔍 待实施   | P1-JWT-LOCALSTORAGE 已修复后端                                    |

---

## 任务队列

| 优先级 | ID                 | 任务                         | 建议                     |
| ------ | ------------------ | ---------------------------- | ------------------------ |
| P2     | P2-TOURNAMENT-SPEC | Tournament 测试覆盖          | 高优先级功能，应优先实施 |
| P2     | P2-WEB-SPEC        | Web 端测试框架搭建           | Playwright 配置          |
| P2     | P2-DEPOSIT-TOCTOU  | HD wallet index 索引竞争     | 高风险并发问题           |
| P2     | P2-ROOM-PASSWORD   | 房间密码 sessionStorage 明文 | 安全建议                 |

---

## 下轮扫描建议

1. **Deposit/Withdraw 财务路径** — 提现流程并发测试（TOCTOU 竞争条件）
2. **Tournament 流程** — SNG 8人成桌 → 开始计时 → 盲注升级 → 冠军结算
3. **Web 前端 Socket.io 重连** — `P1-MOBILE-RECONNECT` 修复后，前端回归验证

---

_Test 第256轮 — 2026-04-26 20:01 — 0 P0 / 0 P1 / 11 P2_
