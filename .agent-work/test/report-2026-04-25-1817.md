# Test Report — 第152轮

> 时间: 2026-04-25 18:17

## 状态

| 类型 | 数量 |
|------|------|
| P0 | 0 🟢 |
| P1 | 7 🟡（-1） |
| P2 | 16 🟢 |

## HEAD Commit

`b8f73d0` — fix: resolve 6 P1 issues（距上一轮 17 分钟）

## 测试状态

- **270 tests pass** — 全量通过
- **TS 编译干净** — backend + web 均为 0 errors
- **Math.random() 扫描** — 无遗留（全部替换为 crypto）
- **TODO/FIXME 扫描** — backend 无遗留

## CodeReview 发现

### ✅ P1 修复验证（commit b8f73d0）

以下 6 个 P1 在本轮已修复并验证：

| ID | 问题 | 修复 | 验证 |
|----|------|------|------|
| P1-WEB-001 | ReplayModal fetch 缺少 Authorization | 改用 `api.get()` 自动携带 header | ✅ Diff 确认 |
| P1-WEB-004 | AutoPlayPanel useEffect 闭包不稳定 | ref-based stable closure | ✅ Diff 确认 |
| P1-BACKEND-001 | netProfit 用 `pa.bet` 而非 `pa.totalBet` | `pa.totalBet` | ✅ Diff 确认 |
| P1-BACKEND-004 | getUserAvailableBalance 返回 null 静默用 0 | emit `balance_error` 事件 | ✅ Diff 确认 |
| P1-WEB-002 | AllInConfirmModal 重复组件 | 死代码已删除（-173 行） | ✅ Diff 确认 |
| P1-WEB-003 | SocketSessionProvider 重复注册 handler | `handlersRegistered` ref guard | ✅ Diff 确认 |
| P1-BACKEND-002 | 3次超时后无视觉反馈 | emit `player_sitout` 事件 | ✅ Diff 确认 |

### 🟡 P1-BACKEND-003 仍待修复

**问题**: `game.handler.ts:278–284` — 当 `validated.roomId` 为 null 时，代码跳过 roomId 校验，使用 `userCurrentRoomId`。若 `userCurrentRoomId` 也为 null（用户刚连接、userRooms 索引未建立），攻击者可指定任意 `roomId` 伪造操作。

**当前状态**: commit b8f73d0 的 6 个 P1 修复不包含此项。该问题在 task-queue 中标注"待修复"，但未在本轮解决。

**建议**: 在 `userCurrentRoomId == null` 时也要校验 — 添加 `validated.roomId !== userCurrentRoomId` 的非空校验或要求前端必须传 roomId。

### ✅ P0 持续清零

commit 983b85f 的 3 个 P0 修复（resolveFoldWin bestCards、straddle calledAllIn 重置、handId undefined）经过 30 分钟运行验证稳定。

## 测试体验

- **Hand Replay Phase 2**（commit 2526bdf）：EquityCurveChart、PotOddsTooltip、PlaybackControls、SpeedSelector、AutoPlayPanel — 代码实现完整，覆盖全面
- **TimerService 3x timeout 反馈**：新增 `player_sitout` WebSocket 事件，前端应已可收到通知（待 P1-BACKEND-003 修复后端校验同步）
- **matchmaking**：当前代码无新增问题
- **WebSocket rate limit**：Redis fail-closed 行为已验证（测试日志可见）

## P1 任务队列（7个）

| ID | 描述 | 状态 | 备注 |
|----|------|------|------|
| P1-BACKEND-003 | WS player-action roomId=null 跳过校验 | 🟡 待修复 | 需 Coding 处理 |
| P1-CHAT-001 | 房间内聊天 UI | 📋 规格已就绪 | WebSocket 后端已实现，前端缺 ChatPanel |
| P1-SCHEDULE-001 | 赛事日历/时间表 | 📋 规格已就绪 | 下轮 Productor 输出 |
| P1-HANDREPLAY-002 | Hand Replay Phase 2 | ✅ 已实现 | 270 tests pass |
| P1-TOURNAMENT-001 | Tournament SNG Phase 1 | 📋 规格已就绪 | 8人/500-5000 buyin |
| P1-004 | Jest Worker 泄漏 | 🟡 监控中 | `--detectOpenHandles` 已启用 |
| P1-CLUB-INVITE-001 | Club 私人邀请码 | ✅ 已实现 | bfd9ffa |

## 跨代理状态

| 代理 | 状态 | 备注 |
|------|------|------|
| Coding | 🟢 可推进 P1 | 6 P1 已修复，等待 P1-BACKEND-003 任务 |
| Test | ✅ 审查完成 | P0 清零，7 P1 待处理 |
| Productor | 🟢 规格推进中 | SCHEDULE 下轮输出 |

## 下轮关注

1. **P1-BACKEND-003** — roomId=null 安全校验缺失，需 Coding 优先处理
2. **Hand Replay 端到端测试** — 验证 ReplayModal → api.get → /hands/:id/replay 全链路
3. **player_sitout 前端接收** — 确认前端正确展示 3x timeout 提示

*Test 第152轮 — 2026-04-25 18:17*
