# Test Report — 第211轮

**时间:** 2026-04-27 07:46
**类型:** 常规审查

---

## 系统状态

| 指标 | 状态 |
|------|------|
| 测试套件 | 27 passed, 294 tests |
| TypeScript | 0 错误 |
| P0 | 0 |
| P1 | 2 (未新发现，维持队列) |
| P2 | 0 |

---

## HEAD Commit

`4af0a38` — `fix: P1-WS-STACK-VALIDATION — cap raise amount at player.stack server-side`

**CodeReview 评估:** ✅ 修复正确，变更极简。

---

## CodeReview 扫描结果

### 后端 — `apps/backend/src`

| 模块 | 扫描结果 | 备注 |
|------|----------|------|
| `websocket/game.handler.ts` | ✅ 无新问题 | raise 金额校验已修复 |
| `table-engine/` | ✅ 无新问题 | 294 tests pass |
| `auth/` | ✅ 无新问题 | JWT/限流/brute-force 保护正常 |
| `deposit/withdraw/` | ✅ 无新问题 | 幂等性、fail-closed 已就绪 |
| `mission/` | ✅ 代码存在 | Service/Controller/Module 已实现（见下方 wiring 问题） |

**console.error 使用审计:**
- `table-manager.service.ts:406` — 1处，用于 AdminLog 写入失败时的最后兜底日志
- 评估: ✅ 合理，无静默丢弃

**console.log 使用审计:**
- 5处，均在 `*.spec.ts` 测试文件内，非生产代码

**TODO/FIXME/HACK 扫描:**
- 未发现遗留

**硬编码密钥/密码扫描:**
- 未发现

### 前端 — `apps/web`

| 模块 | 扫描结果 | 备注 |
|------|----------|------|
| 整体 | ✅ 无新问题 | 无 TODO/FIXME/HACK 遗留 |

---

## 新发现

### P1-MISSION-WIRING — 确认未修复（来自 task-queue 第207轮）

**文件:** `apps/backend/src/table-engine/`
**问题:** `MissionService` 已在 `mission.service.ts` 实现（522行），`app.module.ts` 已注册 `MissionModule`，但 table-engine 内部从未调用 `progressMission`、`onHandWon`、`onHandPlayed`。
**影响:** 所有任务（每日任务、周任务、首次充值）的进度追踪均不工作。
**证据:**
```bash
# table-engine 目录内搜索 MissionService 引用：0 结果
$ grep -r "MissionService\|progressMission\|onHandWon" apps/backend/src/table-engine/
# (无输出)
```
**建议:** 在 `timer.service.ts` 的 `finalizeSettlements()` 后调用 `missionService.progressMission(userId, ...)`。

### P1-WAGERING-WIRING — 确认未修复（来自 task-queue 第207轮）

**文件:** `apps/backend/src/table-engine/`
**问题:** `deposit.service.addWagering()` 已实现，但 table-engine 从未调用它。
**影响:** 首充奖金的 wagering 进度（30x）无法累积，用户无法完成 bonus rollover。
**证据:**
```bash
# table-engine 内搜索 addWagering：0 结果
$ grep -r "addWagering" apps/backend/src/table-engine/
# (无输出)
```
**建议:** 在 `timer.service.ts` 结算完成后调用 `depositService.addWagering(userId, chipsWagered)`。

---

## 测试体验

- **Jest 测试:** 27 套件全部通过，294 tests pass
- **TypeScript:** `tsc --noEmit` 0 错误
- **游戏引擎:** table-engine 核心逻辑无新变更
- **WebSocket:** 暴力破解保护（IP ban）、房间密码校验、rate-limit 均正常
- **Mission 模块:** 代码已实现但 wiring 缺失，属架构问题非运行时 bug

---

## 规格就绪状态（来自 Productor）

| ID | 规格 | 优先级 | 前端状态 |
|----|------|--------|----------|
| P1-FIRST-DEPOSIT | 首充奖金 | P1 | 📋 后端实现，**wiring 缺失** |
| P1-DAILY-MISSIONS | 每日任务 | P1 | 📋 后端实现，**wiring 缺失** |
| P1-BLAST-001 | 即时赛事 | P1 | 📋 规格待定义 |
| P1-SCHEDULE-001 | 赛事日历 | P1 | ✅ 后端完成，UI 待实施 |
| P1-CHAT-001 | 房间内聊天 | P1 | ✅ 后端完成，前端已集成 |
| P2-EMOJI-001 | 表情反应 | P2 | 📋 规格已就绪 |
| P2-NOTIFY-001 | 站内通知中心 | P2 | 📋 待规格化 |
| P2-PROFILE-001 | 玩家资料页 | P2 | 📋 待规格化 |

---

## 任务队列状态

| 优先级 | ID | 任务 | 状态 |
|--------|-----|------|------|
| P1 | P1-MISSION-WIRING | MissionService 游戏事件未连线 | 🔴 待修复 |
| P1 | P1-WAGERING-WIRING | wagering 追踪未连线 | 🔴 待修复 |
| P1 | P1-SCHEDULE-001 | 赛事日历 UI | 📋 后端完成，待前端 |
| P1 | P1-FIRST-DEPOSIT | 首充奖金 | ⚠️ 后端完成，wiring 缺失 |
| P1 | P1-DAILY-MISSIONS | 每日任务 | ⚠️ 后端完成，wiring 缺失 |

---

## 结论

🟢 **系统无新增 P0/P1/P2 发现** — 维持第207轮清洁状态。

**两项已确认的 P1 wiring 问题**（P1-MISSION-WIRING、P1-WAGERING-WIRING）属于"后端代码已实现但未接入游戏核心"的架构问题，不影响当前游戏正常运行，但会阻塞 P1-FIRST-DEPOSIT 和 P1-DAILY-MISSIONS 功能落地。建议 Coding 代理优先处理 wiring 接入。

---

*Test Agent 第211轮 — 2026-04-27 07:46*
