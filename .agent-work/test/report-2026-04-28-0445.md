# Test Report — 第291轮

**时间:** 2026-04-28 04:45
**HEAD:** `9c48a17`
**状态:** 0 P0 / 1 P1 / 0 P2 🟡

---

## 测试结果

| 项目 | 结果 |
|------|------|
| Backend Jest (31 suites) | 🟡 1 failed / 409 passed |
| Backend TypeScript | ✅ 0 errors |
| Backend ESLint | ✅ 0 errors |
| Web TypeScript | ✅ 0 errors |
| Web ESLint | ✅ 0 errors |

---

## 代码变更（eb4230e → 9c48a17）

### commit 9c48a17 — P1-NEW-005/007/008/009 4个事务原子性修复

| 模块 | 修复内容 | 风险 |
|------|---------|------|
| `wallet.service.ts` | `rejectWithdrawRequest` 整体原子化（状态检查+余额+log全在tx内） | ✅ |
| `withdraw.service.ts` | `createWithdraw` 余额检查移入事务内（P1-NEW-005 TOCTOU修复） | ✅ |
| `auth.service.ts` | `register` / `registerWithEmail` user+wallet 同事务创建（P1-NEW-007/008） | ✅ |

---

## 新发现问题

### P0 — 新发现

无。

### P1 — 新发现（1项）

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| P1-NEW-011 | `createWithdraw` 测试回归 — `withdraw.service.spec.ts` "should throw BadRequestException when balance insufficient" failing | 🟡 待认领 | mock `walletService.getAvailableBalance` 不再被调用；余额检查已移入事务内部，直接查 `tx.wallet` |

**原因分析：**
`withdraw.service.ts` 第221行 `createWithdraw` 中，余额检查已从 `walletService.getAvailableBalance()` 迁移到事务内直接读取 `tx.wallet.findUnique()`。测试 mock 了 `walletService.getAvailableBalance` 但未 mock `prisma.$transaction` 内部行为，导致测试未能触发余额不足路径。

**修复方案：**
1. 选项A：mock `prisma.$transaction` 使其内部 `tx.wallet.findUnique` 返回低余额（推荐）
2. 选项B：测试改为验证 `prisma.wallet.update` 未被调用

### P2 — 新发现

无新增。

---

## 重点模块扫描

| 模块 | 变更内容 | 风险 |
|------|---------|------|
| `auth` | register/registerWithEmail 事务原子化 | ✅ |
| `wallet` | rejectWithdrawRequest 事务原子化 | ✅ |
| `withdraw` | createWithdraw 余额检查移入事务 | ✅ |

---

## 结论

**系统基本稳定，1个测试回归需修复。**

- 代码层面：P1-NEW-005/007/008/009 四个原子性修复正确实施
- 测试层面：`createWithdraw` 余额不足测试因 mock 策略过时而失败，需更新 mock 方式
- 无新的类型错误或 lint 错误

---

*Test 第291轮 — 2026-04-28 04:45*
