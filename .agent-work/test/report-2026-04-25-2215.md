# Test Report — 第168轮

> 时间: 2026-04-25 22:15

## 状态总览

| 类型 | 数量     | 变化                                                    |
| ---- | -------- | ------------------------------------------------------- |
| P0   | 0        | —                                                       |
| P1   | 0        | —                                                       |
| P2   | 2 新发现 | tournament.spec.ts 缺失 + calculateFinalRankings 空实现 |

**测试结果**: 274 tests pass ✅ | 构建 0 error ✅

---

## CodeReview 发现

### P2 — 新发现

#### P2-TOURNAMENT-002: TournamentService 完全无测试覆盖

- **文件**: `apps/backend/src/tournament/tournament.service.ts`
- **问题**: `tournament.service.spec.ts` 不存在。整个 `TournamentService`（276行）0个单元测试。
- **风险**: `startBlindTimer`、`advanceBlindLevel`、`distributePrizes`、`getPrizeDistribution`、`clearBlindTimer` 等核心方法均未测试。
- **建议**: 立即补充 `tournament.service.spec.ts`，至少覆盖：
  - `createSngConfig` 输出格式
  - `getPrizeDistribution` 边界（房间不存在、非赛事房间）
  - `startBlindTimer` / `clearBlindTimer` 生命周期
  - `advanceBlindLevel` 升盲逻辑
  - `shouldStopBlindIncreases` 边界（3人/4人）
  - `distributePrizes` 派彩计算
- **优先级**: P2（测试覆盖缺陷，不阻塞但影响质量）

#### P2-TOURNAMENT-001 (跟进): `calculateFinalRankings()` 仍返回空数组

- **文件**: `tournament.service.ts:218-230`
- **问题确认**: 方法体注释已承认 "This method should be called via the service" 但 `// This method IS the service` — 动态 import 语句根本未使用，直接 `return []`。
- **影响**: 锦标赛决胜时 rankings 为空数组，奖金分配依赖 `distributePrizes` 外部注入 rankings 参数，所以实际派彩逻辑可用，但排名计算不可用。
- **建议**: 修复 `calculateFinalRankings` 从 TableManager 获取玩家筹码，按 `SNG_MAX_PLAYERS` 递减排列。
- **优先级**: P2（已在队列中，待 Coding 实施）

### P1 跟进 — 无新问题

| ID                        | 状态          | 备注          |
| ------------------------- | ------------- | ------------- |
| P1-TOURNAMENT-001         | 📋 规格已就绪 | Coding 实施中 |
| P1-SCHEDULE-001           | 📋 规格已就绪 | 等待 Coding   |
| P1-004 (Jest Worker 泄漏) | 🟡 监控中     | 未重新触发    |

### P0 确认 — 无新问题

所有 P0 均已修复验证：

- `resolveFoldWin()` `bestCards: []` ✅
- Straddle `calledAllIn` 重置 ✅
- `persistSettlementRecords` catch `handId` ✅
- JWT Redis fail-closed ✅

---

## 游戏逻辑审查（table-engine）

| 检查项                       | 状态      |
| ---------------------------- | --------- |
| `buildPots()` 边池分配       | ✅ 已修复 |
| `resolveFoldWin()` rake 扣减 | ✅ 已修复 |
| `bestHandFrom()` 5张牌早退   | ✅ 已修复 |
| Straddle `calledAllIn` 重置  | ✅ 已修复 |
| `advanceStreet()` 换桌发牌   | ✅ 正常   |
| Showdown rake 扣除           | ✅ 正常   |

---

## WebSocket / Auth 审查

| 检查项                         | 状态                                             |
| ------------------------------ | ------------------------------------------------ |
| JWT Redis fail-closed          | ✅ 正确 throw UnauthorizedException              |
| WS `player-action` roomId 校验 | ✅ 已修复（line 283-288 双重验证）               |
| `client.leave()` 先于 DB 更新  | ✅ 已修复（game.handler.ts:356）                 |
| 密码暴力破解 Redis 保护        | ✅ Redis SETNX + 5次封禁                         |
| 房间 Club 专属权限校验         | ✅ `isClubMember()` check (line 100-110)         |
| 速率限制                       | ✅ `checkRateLimit()` 在 join/action/chat 均启用 |

---

## 财务模块审查

| 检查项                                | 状态                                             |
| ------------------------------------- | ------------------------------------------------ |
| Withdraw txHash 幂等性                | ✅ `txHash` 确认后保存 + `$transaction` 幂等检查 |
| Withdraw `processWithdraw` 幂等保护   | ✅ 已修复                                        |
| Deposit `checkAddressDeposits` 原子性 | ✅ 已修复                                        |
| Withdraw cooldown Redis 分布式锁      | ✅ `SETNX` + in-memory Map 回退                  |

---

## 测试覆盖现状

| 模块                       | spec 文件 | 测试数 |
| -------------------------- | --------- | ------ |
| `tournament.service`       | ❌ 不存在 | **0**  |
| `tournament.controller`    | ❌ 不存在 | 0      |
| `table-manager.service`    | ✅        | 大量   |
| `hand-evaluator`           | ✅        | 大量   |
| `withdraw.service`         | ✅        | 存在   |
| `deposit.service`          | ✅        | 存在   |
| `connection-state.service` | ✅        | 22     |
| `rakeback.service`         | ✅        | 20     |

**最大缺口**: Tournament 模块完全无测试。

---

## 任务队列更新

### P2 — 新增

| ID                | 任务                           | 状态         | 备注                            |
| ----------------- | ------------------------------ | ------------ | ------------------------------- |
| P2-TOURNAMENT-002 | TournamentService spec.ts 缺失 | 📋 待 Coding | 需 `tournament.service.spec.ts` |

### P2 — 进行中

| ID                | 任务                                        | 状态          | 备注            |
| ----------------- | ------------------------------------------- | ------------- | --------------- |
| P2-TOURNAMENT-001 | `calculateFinalRankings()` 返回空数组       | 📋 待 Coding  | line 218-230    |
| P2-TOURNAMENT-003 | `advanceBlindLevel` WS handler 调用链待确认 | 📋 待 Coding  | 8人满员触发路径 |
| P2-EMOJI-001      | 表情反应系统                                | 📋 规格已就绪 | 储备            |
| P2-PROFILE-001    | 玩家资料页丰富化                            | 📋 规格已就绪 | 储备            |
| P2-NOTIFY-001     | 站内通知中心                                | 📋 规格已就绪 | 储备            |

### P1 — 进行中

| ID                | 任务                    | 状态             | 备注                                |
| ----------------- | ----------------------- | ---------------- | ----------------------------------- |
| P1-TOURNAMENT-001 | Tournament SNG Phase 1  | 🔨 Coding 实施中 | WS integration + frontend remaining |
| P1-SCHEDULE-001   | 赛事日历/Beat the Clock | 📋 规格已就绪    | 等待 Coding                         |

---

## 下一轮建议

1. **立即**: Tournament 模块 0 测试覆盖是最大质量风险，建议 Coding 优先补 `tournament.service.spec.ts`
2. **跟进**: `calculateFinalRankings()` 空实现影响锦标赛排名功能
3. **监控**: Jest Worker 泄漏（`--detectOpenHandles` 持续运行中）

---

_Test 第168轮 — 2026-04-25 22:15 — 初始报告（每15分钟轮次）_
