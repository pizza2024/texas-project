# Test Report — 第116轮

> 更新时间: 2026-04-25 09:31

## 状态

| 状态 | P0   | P1  | P2  |
| ---- | ---- | --- | --- |
| 数量 | 0 🟢 | 1   | 2   |

---

## P0 — 全部清零 ✅

---

## P1 进行中

| ID              | 问题                                                     | 状态        | 备注                                                                                      |
| --------------- | -------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------- |
| P1-RAKEBACK-001 | Rakeback E2E 测试 `passwordHash` → `password`            | 🔴 新增问题 | 已提交 commit 6ec6c3a，TSC 编译仍失败                                                     |
| P1-RAKEBACK-003 | Rakeback service.spec.ts 13 tests failing（5-tier 迁移） | 🔴 新增问题 | 测试断言旧 3-tier 阈值 (BRONZE=10%, SILVER=20%)，代码已是 5-tier (BRONZE=15%, SILVER=22%) |

---

## P2 进行中

| ID         | 问题                                                 | 状态      | 备注                                                                                       |
| ---------- | ---------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------ |
| P2-NEW-001 | Web 9 React hooks exhaustive-deps warnings           | 📋 待修复 | room/page.tsx (4处), room-mobile/page.tsx (1处), rooms/page.tsx (2处), club/page.tsx (1处) |
| P2-NEW-002 | PlayerSeat.tsx `isSettlement` defined but never used | 📋 待修复 | apps/web/app/room/[id]/components/PlayerSeat.tsx:34                                        |
| P2-NEW-003 | rooms/page.tsx `_` assigned but never used           | 📋 待修复 | apps/web/app/rooms/page.tsx:705                                                            |

---

## CodeReview 发现

### P1 — `rakeback.e2e-spec.ts` passwordHash 字段错误（Commit 6ec6c3a）

**问题**: 该 commit 修复了 `passwordHash` → `password`，但 TSC 仍报错：

```
test/rakeback.e2e-spec.ts(37,11): error TS2561: Object literal may only specify known properties,
but 'passwordHash' does not exist in type...
```

**根因**: `jest-e2e.json` 的 `setupFiles` 引用了 `test/setup-dotenv.ts`，但当前工作的 `rakeback.e2e-spec.ts` 文件中仍存在 `passwordHash` 字段（在其他未合并的文件中）。当前 HEAD (d61e623) 的该文件已修复，但 HEAD 有 14 个未提交文件，实际测试时用的是 working tree 版本。

**影响**: E2E 测试无法通过 TSC 编译，阻塞 P1-RAKEBACK-001 验证。

**建议**: 确保 `rakeback.e2e-spec.ts` 的 `passwordHash` → `password` 修复被正确保存到磁盘，并验证 TSC 编译通过。

---

### P1 — Rakeback Service Spec 13 Tests Failing（5-tier 迁移未同步）

**问题**: `rakeback.service.spec.ts` 中 13 个测试失败，断言期望旧 3-tier 阈值：

```
Expected: 0.1  (BRONZE 10%)   → 实际: 0.15
Expected: 0.2  (SILVER 20%)   → 实际: 0.22
Expected: 0.3  (GOLD 30%)      → 实际: 0.3   ✅
Expected: BRONZE for non-existent user → rate=0.15 (新 BRONZE)
```

**根因**: `rakeback.service.ts` 已实现 5-tier（commit dc28855），但 `rakeback.service.spec.ts` 仍使用旧 3-tier 断言值。

**受影响的测试**:

- `getRakebackRate` 全部 8 个 tier 测试
- `creditRakeback` 4 个测试
- 1 个 non-existent user 测试

**建议**: 更新 `rakeback.service.spec.ts` 的断言值为新 5-tier：
| Tier | 旧 rate | 新 rate | 旧 minRake | 新 minRake |
|------|---------|---------|-----------|-----------|
| BRONZE | 10% | 15% | $0 | $0 |
| SILVER | 20% | 22% | $1,000 | $500 |
| GOLD | 30% | 30% | $5,000 | $2,000 |
| PLATINUM | — | 40% | — | $10,000 |
| DIAMOND | — | 50% | — | $50,000 |

---

### P2 — Web React Hooks exhaustive-deps Warnings

**发现 9 个 warnings，0 个 errors**：

| 文件                        | 位置               | 问题                                                                                                                             |
| --------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| `room/[id]/page.tsx`        | 424, 446, 571, 648 | useEffect missing deps (redirectForExpiredToken, redirectToLogin, t, queueDealAnimations, queuePayoutFlights, playCountdownTone) |
| `room-mobile/[id]/page.tsx` | 202                | useEffect missing dep: `doAutoAction`                                                                                            |
| `rooms/page.tsx`            | 769                | useEffect missing dep: `t`                                                                                                       |
| `club/page.tsx`             | 378                | useEffect missing dep: `loadInviteCodes`                                                                                         |

**另有 2 个 unused var**:

- `room/[id]/components/PlayerSeat.tsx:34` — `isSettlement` unused
- `rooms/page.tsx:705` — `_` unused

---

## 测试验证

- **Backend TSC**: 2 errors（rakeback.e2e-spec.ts passwordHash，test 环境）
- **Backend Jest**: 13 failed, 254 passed, 267 total
  - `rakeback.service.spec.ts` — 13 failures（5-tier 阈值不匹配）
  - 其余 25 suites pass ✅
- **Web ESLint**: 0 errors, 9 warnings ✅

---

## 下轮行动

1. **P1**: 修复 `rakeback.service.spec.ts` — 更新 5-tier 阈值断言（BRONZE 15%, SILVER 22%, 新增 PLATINUM/DIAMOND 测试）
2. **P1**: 验证 `rakeback.e2e-spec.ts` `passwordHash` → `password` 已保存，TSC 通过
3. **P2**: 修复 Web 9 个 exhaustive-deps warnings（使用 `useCallback` 包装依赖函数）
4. **P2**: 清理 2 个 unused variables（`isSettlement`, `_`）

---

## HEAD Commit

`d61e623` — 14 个未提交文件（来自 Productor 第115轮）

---

_Test 第116轮 — 13 rakeback.service.spec.ts tests failing（5-tier迁移未同步），E2E passwordHash TSC错误，Web 9 lint warnings，Backend TSC 2 errors_
