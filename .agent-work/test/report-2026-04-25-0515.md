# Test Report — 第99轮

> 更新时间: 2026-04-25 05:15

## 状态

| 状态 | P0 | P1 | P2 |
|------|----|----|----|
| 数量 | 0 🟢 | 5 | 0 |

## HEAD Commit

`b24eec7` — 2026-04-25 02:48:20 +0800（约 2.5 小时前无新 commit）

---

## 一、CodeReview 发现

### P0 全部已修复 ✅

第97轮发现的 6个 P0 问题已全部验证修复：

| ID | 问题 | 验证 | 文件 |
|----|------|------|------|
| P0-GAME-001 | `buildPots()` 边池分配排除 all-in 玩家 | ✅ `p.status === PlayerStatus.ACTIVE \|\| p.status === PlayerStatus.ALLIN` | table-round.ts:265-271 |
| P0-GAME-002 | `resolveFoldWin()` rake 未从 pot 扣减 | ✅ `this.table.pot -= rake` 先扣再分配 | table-round.ts:303 |
| P0-GAME-003 | `bestHandFrom()` 5张牌早退返回非最优 | ✅ 新增 `all.length === 5` 早退路径 | hand-evaluator.ts:209-214 |
| P0-Withdraw-001 | txHash 在链上确认前保存（双花） | ✅ txHash 保存移至 `tx.wait(1)` 之后 | withdraw.service.ts:552-565 |
| P0-Withdraw-002 | `processWithdraw` 无幂等保护 | ✅ `$transaction` + 幂等检查 CONFIRMED | withdraw.service.ts:508-515 |
| P0-Deposit-001 | `checkAddressDeposits` 非原子操作 | ✅ 每事件 `$transaction` 原子化 | deposit.service.ts |

### 新发现

**无新的 P0/P1/P2 发现。**

### P1 进行中问题 — 状态更新

| ID | 问题 | 状态 | 说明 |
|----|------|------|------|
| P1-001 | `TableManager.getTable()` 并发竞态 | 🔴 待验证修复 | `pendingGetTable` Map 已存在，但未见此轮测试覆盖 |
| P1-002 | Redis 不可用时 rate limit 回退 in-memory Map | 🔴 待修复 | connection-state.service.ts:46 — 确认存在 |
| P1-003 | 密码暴力破解 in-memory Map | 🔴 待修复 | connection-state.service.ts:17 — 确认存在 |
| P1-004 | Jest Worker 泄漏 | 🟡 监控中 | `--detectOpenHandles` 已启用 |
| P1-005 | `persistSettlementRecords` 错误被静默吞掉 | 🟡 需确认影响范围 | table-manager.service.ts:354-356 — catch 住错误但只 log |

**P1-005 详细分析：**

```typescript
// table-manager.service.ts:354-356
} catch (err) {
  // Non-fatal: log and continue — game integrity (balance updates) must not be blocked
  this.logger.error(`[persistSettlementRecords] roomId=${roomId}`, err);
}
```

潜在影响：
- `Hand` / `Settlement` / `Transaction` 记录可能丢失
- 用户 rakeback 可能未被正确计入（因为 rakebackCredit 在 catch 之后）
- 但筹码平衡更新（`persistTableBalances`）不受影响 → 游戏核心功能保持正常

**建议**：添加监控告警，当 `persistSettlementRecords` 失败时触发 PagerDuty/日志告警，而非仅依赖错误日志。

**P1-001 详细分析：**

`pendingGetTable` 机制（table-manager.service.ts:107-165）在 `roomService.findOne()` promise 链中使用 `finally()` 清理 pending entry，理论上防止了内存泄漏。但 `finally()` 在 promise reject 时也会清理，这点已正确处理。

测试覆盖情况：
- ✅ 牌桌从 SQLite 快照恢复（FLOP 阶段）
- ✅ 恢复后不调用 `resetBalanceAndUnfreeze`
- ❌ 未覆盖并发 `getTable()` 调用场景（两个请求同时获取同一 roomId）

---

## 二、测试执行结果

```
Test Suites: 24 passed, 24 total
Tests:       238 passed, 238 total
Time:        2.044 s
```

**测试质量**：238 tests passing，覆盖 table-engine、websocket、auth、deposit、withdraw、rakeback 等核心模块。

---

## 三、项目体验

### 文档与测试覆盖

- ✅ AGENTS.md 完整，包含所有模块说明、数据库模型、WebSocket 事件
- ✅ docker-compose 本地开发环境完整
- ✅ Turborepo monorepo 结构清晰
- ✅ 盲注结构 (BLIND_TIERS)、tier-based rake 配置完整

### 遗留体验问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| Club 数据库迁移未执行 | P2 | task-queue 中 P2-002 标注"待执行" |
| Phase 1 Tournament (SNG) 规划中 | P3 | Productor  roadmap |

---

## 四、任务队列更新建议

### 建议提升优先级

| 建议 | 理由 |
|------|------|
| P1-005 增加监控告警 | `persistSettlementRecords` 静默失败 → 财务记录丢失但无人知晓 |
| P1-001 补充并发 getTable() 测试 | 当前仅测恢复场景，未覆盖并发创建 table 场景 |

### 无变化

P0 全部清零，P1/P2 队列维持不变。

---

## 五、下轮行动

1. P1-005：确认 `persistSettlementRecords` 失败后的影响范围（是否需要告警）
2. P1-001：补充 `getTable()` 并发场景的单元测试
3. 推进 Phase 1 Tournament PRD（Productor 主导）

---

*Test 第99轮 — 238 tests pass，P0 全清，遗留 5个 P1 均为已知问题*
