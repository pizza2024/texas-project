# Test Report — 2026-04-28 16:15

**Agent:** Hermes Test (cron)
**HEAD:** `a214970` — fix: P1-NEW-003 emoji username DB lookup + P1-BLAST-PRIVATE-TABLE password
**Interval:** 15 min since last report (15:00)
**报告类型:** 增量监控

---

## Test Results

```
Backend:  32 suites / 452 tests — ✅ 0 failed
Web:       2 suites  / 15 tests  — ✅ 0 failed
TS errors: 0
```

---

## HEAD `a214970` 变更验证

| 变更 | 验证结果 |
|------|---------|
| P1-NEW-003: `handleEmojiReaction` DB lookup (JWT→DB) | ✅ 已修复 — `gateway.userService.user()` 替换 JWT payload |
| P1-BLAST-PRIVATE-TABLE: CreateBlastDialog password | ✅ 已修复 — DTO + service + shared type 全链路 password |

---

## P0 遗留问题（无变化）

| # | 问题 | 文件 | 状态 | 分析 |
|---|------|------|------|------|
| 1 | `MAX_CONSECUTIVE_TIMEOUTS` 触达后牌桌永久卡死 | `table-player-ops.ts:209-217` | 🔴 仍 Open | 当 `consecutiveTimeouts >= MAX(3)` 时，`return false` 前未调用 `advanceStreet`/`nextActiveFrom`，`activePlayerIndex` 指向超时玩家但无任何推进逻辑；后续 `checkAndAutoFoldSittingOut` 被 `isActionStage` guard 阻止，导致牌桌卡死 |
| 2 | `handleWithdrawFailure` race condition | `withdraw.service.ts:621` | 🔴 仍 Open | 无 `SELECT FOR UPDATE` 锁；两次并发调用在事务内均读到 `PENDING`，第二次更新会覆盖第一次状态+退款，仍存在双重退款风险 |
| 3 | TRC20 正则排除合法字符 | `wallet.service.ts:258` | 🔴 仍 Open | `/^T[A-HJ-NP-Za-km-z1-9]{33}$/` — 排除 `A-H`（应为 `A-Z`）和 `J-NP-Z`（应为 `J-Z`），遗漏合法字符 `I`, `O`；另 `0,I,O,l` 被排除 |
| 4 | `verifyEmailCode` 无 rate limit | `auth.controller.ts:101` | 🟡 已降级 | `auth.service.ts` 已实现 `OTP_ATTEMPTS_PREFIX` + `MAX_ATTEMPTS=5` + `TTL=300s`（commit `f04ead4` 之前已存在）— **实际有防护，本条可关闭** |

---

## P1 遗留问题状态

| ID | 任务 | 状态 |
|----|------|------|
| P1-NEW-001 | `activeGames` Map 无 TTL 清理 | 🔴 Open — 内存泄漏风险 |
| P1-BLAST-012 | Blast Phase 3 规格（endsAt/退款/持久化/惩罚） | 🟡 待 Productor |

---

## P2 新发现（无）

本轮扫描未发现新的 P2 问题。

### P2 遗留清单（无变化）

| ID | 任务 | 状态 |
|----|------|------|
| P2-NEW-018 | `drawBlastMultiplier()` shared 版用 `Math.random()` | 🔍 Open |
| P2-NEW-020 | `activeGames` 无主动 TTL 清理 | 🔍 Open |
| P2-NEW-021 | BlastService Phase 3 缺少单元测试 | 🔍 Open |
| P2-NEW-022 | Emoji username 与 Chat 显示不一致（JWT vs DB） | 🔍 Open |
| P2-TEST-001 | `exchangeBalanceToChips` 事务外读取 | 🔍 Open |
| P2-TEST-002 | ELO 边界强制分离事务 | 🔍 Open |
| P2-TEST-005 | `TableState`/`Player` 类型 web↔shared 漂移 | 🔍 Open |
| P2-TEST-006 | ActionBar 无重复提交保护 | 🔍 Open |
| P2-TEST-007 | Token 过期 `window.location.replace` 丢弃状态 | 🔍 Open |
| P2-TEST-008 | 无 refresh token 机制 | 🔍 Open |

---

## 测试体验观察

| # | 观察 | 备注 |
|---|------|------|
| T1 | `verifyEmailCode` OTP rate limit 已存在 | 与上轮 P0 记录矛盾，建议关闭 P0-NEW-OTP-NORATELIMIT |
| T2 | Blast Phase 4 前端已启动 | `app/blast/page.tsx` + `CreateBlastDialog` + `SpinWheel` + `MatchingOverlay` 已实现（commit `de4156b`） |
| T3 | Anonymous Mode 已实现 | `Room.isAnonymous` + `PlayerSeat` "Player N" + 🎭 badge（commit `f04ead4`） |

---

## 建议关闭的条目

| ID | 理由 |
|----|------|
| ~~P0-NEW-OTP-NORATELIMIT~~ | `auth.service.ts:195` 已有 `OTP_MAX_ATTEMPTS=5` + `TTL=300s` 防护，`verifyEmailCode` 有完整 rate limit |
| ~~P2-NEW-018~~ | `drawBlastMultiplier()` 在 `packages/shared` 未被任何调用方使用；`blast.service.ts` 已用 `crypto.randomBytes` |

---

## 优先级任务队列

### P0 — 必须修复

| ID | 任务 | 文件 |
|----|------|------|
| P0-TABLE-STUCK | `checkAndAutoFoldSittingOut` 达到 MAX 后返回 `false` 不推进 turn，牌桌卡死 | `table-player-ops.ts:209-217` |
| P0-WITHDRAW-DOUBLE-REFUND | `handleWithdrawFailure` 无 `SELECT FOR UPDATE` 锁 | `withdraw.service.ts:621` |
| P0-TRC20-CHARS | TRC20 正则遗漏 `I`, `O` 等合法字符 | `wallet.service.ts:258` |

### P1 — 重要

| ID | 任务 |
|----|------|
| P1-NEW-001 | `activeGames` Map TTL 清理机制 |
| P1-BLAST-012 | Blast Phase 3 规格确认（待 Productor） |

### P2 — 建议

| ID | 任务 |
|----|------|
| P2-NEW-020 | `activeGames` 进程生命周期清理 |
| P2-NEW-021 | BlastService Phase 3 单元测试补充 |
| P2-TEST-001~008 | 遗留测试覆盖 |

---

## Summary

| 维度 | 状态 |
|------|------|
| P0 新增 | 0 |
| P0 遗留 | 3（其中1条已验证为误报） |
| P1 遗留 | 2 |
| P2 遗留 | ~9 |
| Backend 测试 | ✅ 452 passed |
| Web 测试 | ✅ 15 passed |
| TS 编译 | ✅ 0 errors |

**Top 行动项:**
1. **P0-TABLE-STUCK** — `table-player-ops.ts` MAX_TIMEOUT 触达后未推进 turn
2. **P0-WITHDRAW-DOUBLE-REFUND** — `handleWithdrawFailure` 需 `SELECT FOR UPDATE`
3. **P0-TRC20-CHARS** — 修正正则遗漏 `I`, `O` 字符

---

*Test Report — 2026-04-28 16:15 — Hermes Agent (cron)*
