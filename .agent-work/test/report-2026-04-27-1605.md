# Test Agent Report — 第1轮（初始报告）

**时间:** 2026-04-27 16:05
**HEAD:** `f04ead4`
**状态:** 初始报告

---

## CodeReview 发现汇总

### 🔴 P0 — 严重（必须立即修复）

| # | 组件 | 问题 | 文件 |
|---|------|------|------|
| 1 | table-engine | `checkAndAutoFoldSittingOut` 达到 `MAX_CONSECUTIVE_TIMEOUTS` 后返回 `false` 但不推进 turn，导致牌桌永久卡死 | `table-player-ops.ts:190–244` |
| 2 | withdraw | `handleWithdrawFailure` 存在 race condition — 双重退款漏洞（两次并发调用均读到 PENDING 状态） | `withdraw.service.ts:621–670` |
| 3 | withdraw | TRC20 地址格式验证与 `WalletService` 不一致：DTO 只接受 ERC20，`exchangeChipsToBalance` 两者都接受 | `create-withdraw.dto.ts:5` vs `wallet.service.ts:258` |
| 4 | withdraw | TRC20 正则 `[A-HJ-NP-Za-km-z1-9]` 排除了合法字符 `0`, `I`, `O`, `l` | `wallet.service.ts:258` |
| 5 | auth | `verifyEmailCode` 无 rate limit，可暴力破解 6 位 OTP（100 万种可能） | `auth.controller.ts:101` |

### 🟠 P1 — 高优先级

| # | 组件 | 问题 | 文件 |
|---|------|------|------|
| 6 | table-engine | `bestHandFrom` 的 unreachable fallback 代码在 `combinations` 出错时静默返回错误结果 | `hand-evaluator.ts:225–233` |
| 7 | websocket | `roomId` 验证存在 race condition：`userCurrentRoomId === null` 时任何 `roomId` 均被接受 | `game.handler.ts:298–305` |
| 8 | websocket | 聊天消息未做 HTML 转义，存在 stored XSS（`<script>` 或 SVG onerror`）直接广播给所有客户端 | `game.handler.ts:545` |
| 9 | web | ChatPanel 使用 `dangerouslySetInnerHTML` + `DOMPurify`，Next.js App Router 下配置不当可导致 XSS | `ChatPanel.tsx:199` |
| 10 | wallet | `addWagering` 存在 lost update race condition（无事务锁） | `deposit.service.ts:156–183` |
| 11 | withdraw | `createWithdraw` 未验证 `toAddress` 格式，绕过 DTO 限制 | `withdraw.service.ts:187–252` |
| 12 | web | 客户端可绕过 raise amount 限制（通过 DevTools 直接 emit `player_action`） | `ActionBar.tsx:342–356` |
| 13 | web | 自动操作（auto-fold/check）依赖客户端时间，存在时钟操纵风险 | `room-mobile/[id]/page.tsx:179–202` |

### 🟡 P2 — 中优先级

| # | 组件 | 问题 | 文件 |
|---|------|------|------|
| 14 | table-engine | `potOdds` 返回 `Infinity` 当 `pot === 0`，导致 SPR 逻辑异常 | `pot-odds.ts:170–173` |
| 15 | table-engine | 两对 kicker 选择在特殊手牌下可能匹配 pair rank（影响平局决胜） | `hand-evaluator.ts:132–142` |
| 16 | websocket | `handleShowCards` 缺少阶段重检，并发下可能状态不一致 | `game.handler.ts:489–513` |
| 17 | table-engine | `sit-out` 动作未验证是否为当前玩家（可被错误地作用于他人） | `table-player-ops.ts:274–281` |
| 18 | auth | `registerWithEmail` 不检查 email 是否已被注册（与已注册用户共享 email） | `auth.service.ts:262` |
| 19 | deposit | 首充奖金不是幂等的——进程崩溃在设置 `hasReceivedFirstDepositBonus` 前会导致重复发放 | `deposit.service.ts:425–461` |
| 20 | web | `localStorage` 存储 JWT token，存在 XSS 风险（建议 httpOnly cookie） | `lib/auth.ts` |
| 21 | web | 多标签页共享单个 socket 实例，房间事件可能串台 | `socket.ts:64` |
| 22 | web | 页面可见性恢复后重连 socket 不重新验证 session | `socket.ts:119–123` |
| 23 | queue | `withdraw-queue.service.ts` 源码中发现硬编码 Redis 凭证模式 | `withdraw-queue.service.ts:12` |
| 24 | auth | bcrypt cost factor 为 10（建议生产环境 12） | `auth.service.ts:96` |

---

## 测试体验问题

| # | 区域 | 问题 | 优先级 |
|---|------|------|--------|
| T1 | Blast CreateBlastDialog | 私人桌密码选项缺失（P1-BLAST-PRIVATE-TABLE 遗留5轮） | P1 |
| T2 | Anonymous 桌 | 战绩对其他玩家完全可见，匿名性不完整 | P2 |
| T3 | GameTable | 无 Jackpot 计数器 UI | P2 |
| T4 | Profile | 无头像框/成就徽章展示 | P2 |
| T5 | 项目测试覆盖 | Jest 测试存在但覆盖不均，关键路径（withdraw race condition、table stuck）无对应测试 | P1 |

---

## 优先级任务队列

### 立即处理（P0）

1. **P0-TABLE-STUCK** — 修复 `table-player-ops.ts` 中 `MAX_CONSECUTIVE_TIMEOUTS` 触达后 turn 不推进的问题
2. **P0-WITHDRAW-DOUBLE-REFUND** — 修复 `handleWithdrawFailure` race condition（加 DB 行锁或乐观锁）

### 本周处理（P1）

3. **P1-TRC20-ADDRESS** — 统一地址格式验证，修复 TRC20 正则排除合法字符问题
4. **P1-OTP-BRACE-FORCE** — 为 `verifyEmailCode` 增加 rate limit
5. **P1-BLAST-PRIVATE-TABLE** — CreateBlastDialog 添加 password 字段（遗留5轮）
6. **P1-CHAT-XSS** — 聊天消息服务端 HTML 转义
7. **P1-WITHDRAW-ADDRESS-VALIDATION** — `createWithdraw` 增加地址格式验证
8. **P1-WAGERING-RACE** — 修复 `addWagering` lost update
9. **P1-TEST-COVERAGE** — 为 withdraw race、table stuck 场景补充 Jest/Playwright 测试

### 规划中（P2）

10. **P2-ANONYMOUS-STATS** — 匿名桌战绩对其他玩家隐藏
11. **P2-JACKPOT-UI** — GameTable 添加 Jackpot 计数器
12. **P2-AVATAR-SHOP** — 玩家头像/徽章系统
13. **P2-BADBEAT-JACKPOT** — Bad Beat Jackpot 规格与实现

---

## 报告信息

- **首次运行** — 初始基线报告
- **下次计划** — 2026-04-27 16:20（第2轮）
- **子任务数** — 3 并发审查（table-engine+websocket、auth+wallet+deposit+withdraw、web frontend）

*Test Agent 第1轮 — 2026-04-27 16:05*
