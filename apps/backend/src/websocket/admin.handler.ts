/**
 * Admin event handlers for the WebSocket gateway.
 *
 * Reserved for future admin-level events such as:
 *   - RoomCreate / RoomDelete / RoomEdit
 *   - AdminPauseResume
 *   - GetRoomSnapshot
 *
 * These handlers do not exist in the current codebase and are listed here
 * to match the target directory structure defined in the refactoring plan.
 *
 * No business logic is changed — only code movement from app.gateway.ts.
 */
import { AppGateway } from './app.gateway';

// TODO: Add admin event handlers when needed
// export async function handleRoomCreate(gateway: AppGateway, ...) { ... }
// export async function handleRoomDelete(gateway: AppGateway, ...) { ... }
// export async function handleRoomEdit(gateway: AppGateway, ...) { ... }
// export async function handleAdminPauseResume(gateway: AppGateway, ...) { ... }
// export async function handleGetRoomSnapshot(gateway: AppGateway, ...) { ... }
