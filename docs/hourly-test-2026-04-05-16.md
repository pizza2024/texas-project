# Texas Hold'em Hourly Test Report

**测试时间:** 2026-04-05 16:48 (Asia/Shanghai)  
**API 版本:** https://api.not-replaced-yet.com  
**测试人员:** pipi 🛠️

---

## 一、测试结果总览

| 端点 | 方法 | 状态码 | 延迟(ms) | 结果 |
|------|------|--------|----------|------|
| /auth/login | POST | 201 | 1.45 | ✅ 正常 |
| /rooms | GET | 200 | 0.73 | ✅ 正常 |
| /auth/profile | GET | 200 | 0.48 | ✅ 正常 |
| /user/stats | GET | 200 | 0.60 | ✅ 正常 |
| /user/hands | GET | 200 | 0.57 | ✅ 正常 |
| /withdraw/balance | GET | 200 | 0.59 | ✅ 正常 |
| /deposit/address | GET | 200 | 0.54 | ✅ 正常 |
| /rooms | POST | 201 | 0.53 | ✅ 正常 |
| /rooms/:id | DELETE | 404 | 0.67 | ❌ 未部署 |
| /ws | WebSocket | 404 | 0.50 | ❌ 未部署 |

**常规回归通过率: 8/8 (100%)**  
**新增功能通过率: 0/2 (0%) — 代码已提交但未部署**

---

## 二、本小时新增功能点

### 1. DELETE /rooms/:id 🔴 高优先级

**commit:** `eafa54c` — fix: add DELETE /rooms/:id endpoint

**测试结果:** ❌ 404 Not Found

**分析:** 代码已提交到 `apps/backend/src/room/room.controller.ts`，在 service 层调用 `roomService.deleteRoom(id)`。但服务器返回 404，说明 **最新代码未部署到生产环境**。

**修复方案:** 需要重新部署后端服务以加载新路由。

---

### 2. WebSocket namespace 配置修改 🔴 高优先级

**commit:** `5be2832` — fix: properly configure WebSocket gateway upgrade

**改动:** `@WebSocketGateway({ namespace: '/ws', ... })`

**测试结果:** ❌ 404 Not Found (尝试 /ws 和 /api/ws 均返回 404)

**分析:** 同样为未部署问题。原代码可能无 namespace 或使用默认 namespace，现在改为 `/ws` 但服务器未更新。

**修复方案:** 重新部署后端服务以加载新的 WebSocket Gateway 配置。

---

### 3. 邮件验证码 Redis 脚本 📋 非 API 变更

**commit:** `87fb8e8` — feat: add script to retrieve email verification code from Redis

**说明:** 仅为辅助脚本 (`scripts/get-dev-email-verified-code.sh`) 和前端页面修改，不影响 API 行为。

---

## 三、发现的问题

| 严重程度 | 问题描述 | 影响范围 | 状态 |
|----------|----------|----------|------|
| 🔴 **高** | DELETE /rooms/:id 返回 404 | 新增房间删除功能完全不可用 | 未部署 |
| 🔴 **高** | WebSocket /ws 返回 404 | 实时对战功能不可用 | 未部署 |
| 🟡 **中** | 战绩统计全为 0 | 用户从未有过游戏记录（test1 账户） | 数据问题 |

---

## 四、改进建议

1. **部署流程优化:** 新增 API 端点后应自动触发部署，或至少在 commit message 中明确标注需要手动部署
2. **WebSocket 路径文档化:** 建议在 API 文档中明确 WebSocket 连接路径（当前为 `/ws`）
3. **房间清理机制:** 测试创建的房间 `pipi-hourly-test-0405-16` 应在测试后自动清理

---

## 五、总结

- **测试覆盖率:** 10 个端点（8 个常规 + 2 个新增）
- **常规回归:** 8/8 通过 ✅
- **新增功能:** 0/2 通过（均为部署问题，非代码问题）
- **高严重问题:** 2 个（已识别为部署问题）
- **中严重问题:** 1 个（数据问题）
- **自动修复:** 否（部署相关，需运维介入）

---

*报告生成时间: 2026-04-05 16:48 CST*
