# Texas Hold'em 每小时质检报告

**测试时间:** 2026-04-05 15:48 CST (07:48 UTC)
**测试目标:** https://api.not-replaced-yet.com
**测试人:** pipi 🤖

---

## 📊 本小时新增功能点

### 1. Rate Limiting 增强 (commit: 7969e7d)

- 新增 `keyType` 配置：支持 `userOrIp | ip | user | emailOrIp`
- 新增开发环境 bypass 逻辑（dev 模式下可绕过）
- 文件: `apps/backend/src/auth/rate-limit.guard.ts`

### 2. WebSocket 服务器就绪检测 (commit: 10187ae)

- `main.ts` 新增启动就绪等待
- `app.gateway.ts` 新增 server readiness checks 和事件处理增强
- 文件: `apps/backend/src/websocket/app.gateway.ts`, `apps/backend/src/main.ts`

---

## ✅ API 测试结果

### 1. POST /auth/login

| 项目       | 结果                                                   |
| ---------- | ------------------------------------------------------ |
| HTTP 状态  | 201 (异常！应为 200)                                   |
| 响应延迟   | 1913ms                                                 |
| Token 字段 | `access_token` ✅                                      |
| 返回数据   | user 对象完整 (id, nickname, coinBalance, elo) ✅      |
| **评估**   | ⚠️ 状态码 201 不标准（创建成功才用 201，登录应用 200） |

### 2. GET /auth/profile

| 项目      | 结果            |
| --------- | --------------- |
| HTTP 状态 | 200 ✅          |
| 响应延迟  | 513ms           |
| 返回数据  | 个人信息完整 ✅ |
| **评估**  | ✅ 正常         |

### 3. GET /rooms

| 项目      | 结果                         |
| --------- | ---------------------------- |
| HTTP 状态 | 200 ✅                       |
| 响应延迟  | 488ms                        |
| 返回数据  | 列表返回正常，有 1 个房间 ✅ |
| **评估**  | ✅ 正常                      |

### 4. GET /user/stats

| 项目      | 结果                                                   |
| --------- | ------------------------------------------------------ |
| HTTP 状态 | 200 ✅                                                 |
| 响应延迟  | 522ms                                                  |
| 返回数据  | 统计字段完整 (handsPlayed, winRate, totalProfit 等) ✅ |
| **评估**  | ✅ 正常（test1 用户暂无游戏记录）                      |

### 5. GET /user/hands?limit=5

| 项目      | 结果                  |
| --------- | --------------------- |
| HTTP 状态 | 200 ✅                |
| 响应延迟  | 460ms                 |
| 返回数据  | 空数组 [] ✅          |
| **评估**  | ✅ 正常（无手牌历史） |

### 6. GET /withdraw/balance

| 项目      | 结果                                                           |
| --------- | -------------------------------------------------------------- |
| HTTP 状态 | 200 ✅                                                         |
| 响应延迟  | 426ms                                                          |
| 返回数据  | `{availableChips, minWithdrawChips, minWithdrawUsdt, rate}` ✅ |
| **评估**  | ✅ 正常                                                        |

### 7. GET /deposit/address

| 项目      | 结果                                                    |
| --------- | ------------------------------------------------------- |
| HTTP 状态 | 200 ✅                                                  |
| 响应延迟  | 1061ms                                                  |
| 返回数据  | Sepolia TRC20 地址 `{address, network, token, rate}` ✅ |
| **评估**  | ✅ 正常                                                 |

### 8. POST /rooms (创建房间)

| 项目      | 结果                                                |
| --------- | --------------------------------------------------- |
| HTTP 状态 | 400 (首次请求，DTO 字段错误)                        |
| 错误原因  | `smallBlind`/`bigBlind` 与 DTO 不匹配；上限限制过严 |
| 修正后    | 字段改为 `blindSmall`/`blindBig` 后成功创建 ✅      |
| 响应延迟  | 正常                                                |
| **评估**  | ⚠️ API 文档/DTO 与实际实现不一致                    |

### 9. DELETE /rooms/:id

| 项目      | 结果                                |
| --------- | ----------------------------------- |
| HTTP 状态 | 404 ❌                              |
| 响应延迟  | 938ms                               |
| **评估**  | ❌ **高优先级**: 路由不存在或未实现 |

### 10. WebSocket 握手

| 项目     | 结果                                                    |
| -------- | ------------------------------------------------------- |
| 握手方式 | HTTP GET with Upgrade headers                           |
| 响应     | `Hello World!` + HTTP 200 ❌                            |
| **评估** | ❌ **高优先级**: WebSocket Upgrade 未实现，返回普通文本 |

### 11. Rate Limit 验证（5 次快速请求）

| 项目     | 结果                                                                              |
| -------- | --------------------------------------------------------------------------------- |
| 请求 1-5 | 全部 200，无 429 ❌                                                               |
| **评估** | ⚠️ **中优先级**: Rate limit guard 可能未应用到此端点，或 Redis 连接失败时静默放行 |

---

## 🔍 问题汇总

### 🔴 高优先级 (需立即修复)

| #   | 问题                             | 位置                                                                                                   |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1   | **WebSocket Upgrade 失败**       | `apps/backend/src/websocket/app.gateway.ts` — 握手返回普通 HTTP 200 + 文本而非 101 Switching Protocols |
| 2   | **DELETE /rooms/:id 路由不存在** | 房间创建成功但删除返回 404，需确认路由是否实现                                                         |

### 🟡 中优先级

| #   | 问题                      | 位置                                                                         |
| --- | ------------------------- | ---------------------------------------------------------------------------- |
| 3   | **Rate Limit 未生效**     | `auth.controller.ts` — 5 次快速请求均未触发限流，可能 Guard 未挂载到相关路由 |
| 4   | **登录返回 201 而非 200** | `auth.controller.ts` — POST /auth/login 状态码不规范                         |

### 🟢 低优先级 / 改进建议

| #   | 问题                                                                                   |
| --- | -------------------------------------------------------------------------------------- |
| 5   | `/rooms` 创建时原 DTO 字段名与实际不符（`smallBlind` vs `blindSmall`），API 文档待更新 |
| 6   | 响应延迟普遍偏高（400ms-1900ms），可考虑加入缓存和索引优化                             |
| 7   | `GET /user/hands?limit=5` 返回空数组，应确认是「无数据」还是「查询逻辑错误」           |

---

## 🛠 自动修复计划

本次发现 2 个高优先级问题：

1. **WebSocket 握手问题** — 检查 `app.gateway.ts` 中的 `@WebSocketGateway` 配置和 `@SubscribeMessage` 装饰器
2. **DELETE /rooms/:id 404** — 检查 `rooms.controller.ts` 是否有 `@Delete(':id')` 路由

建议优先检查这两个路由是否正确导出和挂载。

---

## 📈 覆盖率统计

| 类别         | 覆盖率                             |
| ------------ | ---------------------------------- |
| 认证 (Auth)  | 3/4 端点 (缺少 Token Refresh 测试) |
| 房间 (Rooms) | 2/4 端点 (缺少进入房间/密码校验)   |
| 充值提现     | 2/2 端点 ✅                        |
| 战绩         | 2/2 端点 ✅                        |
| WebSocket    | 0/1 (握手失败) ❌                  |

**总体覆盖率:** ~65%

---

_报告生成时间: 2026-04-05 15:50 CST_
